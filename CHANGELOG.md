# Changelog

All notable changes to PlantCare will be documented in this file.

## [0.1.49.0] - 2026-05-08

E5-012 — Camera-flow integration tests covering the full capture → diagnose → result chain across `<PlantCareCameraView>` and `<CameraResultScreen>`. Stacks on PR #47 (E5-009 error variants); base for merge is `Anandsatch/e5-result-errors`, not `main`. The discipline this adds: real component composition (only module-boundary mocks for `expo-camera`, `useTheme`, `useReduceMotion`, the api-client, and `compressPhotoImpl`), every kind from the locked discriminated union exercised end-to-end, and explicit invariants we couldn't catch from unit tests alone — Strict-mode double-mount latches on both shutter and diagnose, cached-compression-on-retry (compression count must stay at 1 when "Try again" fires), reduce-motion init-at-end-state honored across the loading→variant transition, and AppState-resume permission re-query.

### Added
- `apps/mobile/src/__tests__/cameraFlow.integration.test.tsx` — 23 integration tests organized by flow:
  - Happy path: shutter → onCapture payload (uri/width/height) → result screen → success card → Save fires `onSave` with the full payload; capture payload shape matches the `<CameraView>` contract end-to-end (no payload mismatch can slip through unit tests).
  - Per-kind variant rendering: `low_confidence` (manual picker CTA), `timeout` (Try again re-fires with cached photo), `layer1_reject` (routes back to camera), `server_error` (Report CTA fires `onReportError` with kind/message/photoUri/mode), `parse_error` (single Try again, no Report — distinct from server), `queued` (ToastBanner pending surface, no retry CTA), `network` coercion (api-client throw renders queued banner per master plan line 346, not network/timeout variant).
  - Permission flow: denied → pre-prompt visible (composed from E5-003), shutter not rendered; permanently denied → no path forward without Settings; AppState resume → permission re-queried via `getPermission` on foreground (covers grant-in-Settings recovery).
  - Reduce-motion: success-card and error-variant reveals both initialize opacity at 1 with no `Animated.timing` fired (mirrors the canonical pattern from E9-003 / E9-004 / E7-005).
  - Strict-mode latches: shutter does NOT fire `takePictureAsync` twice on a single tap; result screen fires diagnose exactly once across mount→unmount→mount.
  - Cancel paths: tapping camera close pre-shutter does NOT fire `onSave`; result screen receives no save when diagnose is still in-flight (covers the navigate-away-mid-loading hazard).
  - Cached compression on retry: "Try again" on `server_error` AND on `parse_error` re-fires diagnose with the same compressed photo — compression count stays at 1 across both retry tests (the canonical contract; recompressing on retry would burn battery and waste storage).
  - Mode toggle: switching identify ↔ diagnose pre-shutter threads `mode` through to `onSave` payload.
  - Discriminated-union sweep: a single test asserts the Save CTA is hidden on every non-success kind, so a future kind addition that accidentally re-enables Save trips immediately.

### Adversarial review (codex)
No P1 findings. One P2 (test name accuracy) addressed inline during the build pass. The 23 tests genuinely exercise real component composition rather than mocking the boundaries away — `<PlantCareCameraView>` and `<CameraResultScreen>` are both real, only their module dependencies are mocked. Discriminated-union preservation verified: every kind has its own test path with distinct `testID` + headline assertion. Cached-compression-on-retry asserted in three retry tests (timeout, server, parse_error). Strict-mode latches verified on both shutter (1 takePictureAsync) and diagnose (1 diagnose). Reduce-motion tests assert opacity initialized to 1 with no `Animated.timing` fired.

### V1 scope locks (rejected)
- Maestro / Detox here — this is jest+RTL integration; E5-013 is the Maestro ticket.
- A new test runner / library — sticking with jest+RTL+jest-expo.
- Compressing the discriminated union in any assertion — every kind preserved.
- Reaching into the api-client internals — only its module export is mocked.

### Notes
- The integration test file lives at `apps/mobile/src/__tests__/cameraFlow.integration.test.tsx` (not under any single component's `__tests__/` folder) because it spans the camera composite + result screen + diagnose hook + photo-compress helper. The naming convention `*.integration.test.tsx` is intentional — future integration suites should mirror it so they're easy to filter (`bun test integration` if needed). Matches the convention in `apps/backend/test/router.test.ts` for cross-route integration on the backend.
- The `network → queued` coercion test asserts master-plan line 346 explicitly: `useDiagnoseRequest` coerces api-client `network` errors into `queued` for V1's offline UX — so the `'network'` kind never surfaces to `<CameraResultScreen>` today and the network-variant rendering remains verified at the unit level (E5-009 resolver test) plus this coercion test, not via a live render.

## [0.1.46.0] - 2026-05-08

E6-005 — `useWateringEngine` v2 + new `useWeather` hook. The pure decision function that drives the A-1 list chip and the A-2 detail chip now optionally consumes a 2-day forecast snapshot and adjusts the species interval before the bias-zone math runs. Three precedence rules are locked structurally rather than checked-at-call-site, so any future refactor that re-orders the branches breaks compilation rather than ships a silent bug: (1) a non-null `override_interval_days` short-circuits everything — weather and `is_indoor` are ignored because the user explicitly chose a custom interval and second-guessing them defeats the override; (2) `is_indoor === true` (the schema default; also the safe fallback when the field is absent on a v1 plant row) skips the weather modifier because indoor microclimates are stable — rain doesn't reach the pot and indoor temperature tracks the thermostat, not the outdoor max; (3) only when `is_indoor === false` AND override is null AND a `weather` snapshot is non-null is the modifier applied. Three threshold values are locked-by-test as exported constants — `PRECIPITATION_THRESHOLD_MM = 5`, `HEAT_THRESHOLD_C = 30`, `MIN_INTERVAL_DAYS = 1`, `MAX_INTERVAL_DAYS = 30` — so a future "let's tune the threshold" change has to be a documented unlock rather than a quiet drift away from the master plan + ticket spec. The cap [1, 30] is structural protection: even though no V1 species + modifier combination naturally reaches it (V1 species table is 5..14 days; modifier range is -1 to +2; realized band is [4, 16]), the clamp prevents a future species-table or threshold change from underflowing a sub-day interval (would constantly fire 'water' for a freshly-watered plant) or overflowing a month-long interval (defeats the engine's purpose; if the species really wants 35 days, the user sets an override). The thresholds are STRICT `>` not `>=` — exactly 5mm precipitation or exactly 30°C heat does NOT trigger; the engine only modifies on values that strictly exceed.

This branch stacks on `Anandsatch/e6-weather-endpoint` (PR #44, E6-002), which itself stacks on `Anandsatch/e6-open-meteo` (PR #40, E6-001). GitHub will auto-rebase the PR onto main when E6-001 + E6-002 land; squash-merge will collapse the WIP + the codex-fix commit + this VERSION/CHANGELOG bump into one feat() at land time.

This entry completes a WIP commit started in a prior session that exited mid-workflow before adversarial review. The finishing pass added a follow-up commit on top of the WIP after a fresh adversarial review surfaced one P1 (a documentation/implementation contradiction in `useWeather`'s rate_limited classification — the docstring claimed the kind was reserved/forward-compat but the test fixtures asserted active emission, and the implementation followed the docstring, breaking 2 tests that the WIP would have shipped red) and two P2s from a `codex exec` review (no runtime shape validation on the upstream weather payload + a race-guard that compared resolution order to the latest-committed call instead of the latest-started call).

### Added
- `apps/mobile/src/watering/engine.ts` — v2 weather modifier. New exported pure function `applyWeatherModifier(baseIntervalDays, weather)` does the threshold + clamp math; tuple-typed `WateringEngineWeather = { daily: [WateringEngineWeatherDaily, WateringEngineWeatherDaily] }` locks the V1 "current + 2 days" scope at compile time (indexing past `[1]` is a compile error). `WateringEnginePlant.is_indoor?: boolean` is optional with `?? true` defaulting at the engine boundary so v1 callers (E4-001) that don't pass the field get the safer no-modifier path. New private `resolveEffectiveIntervalDays(plant, weather)` owns the 3-step precedence (override returns early; indoor returns early; outdoor + null weather degrades to species default; outdoor + weather applies the modifier). The interval-to-status math (boundary cases, half-interval bias zone, full-interval water trigger, sub-day precision) is unchanged from v1 — the engine layer composes a fresh interval and feeds it into the same comparator that E4-002's regression suite locks.
- `apps/mobile/src/watering/__tests__/engine.test.ts` — adds the E6-005 v2 surface (16 new tests on top of v1's 11): locked-threshold-constants assertions, `applyWeatherModifier` boundary cases (no rain + no heat → unchanged; rain only → +2; heat only → -1; both → net +1; cap floor on base 1 + heat → 1 not 0; cap ceiling on base 30 + rain → 30 not 32; STRICT > thresholds at exactly 5mm and exactly 30°C → no modifier), `computeWateringStatus` precedence (override + outdoor + storm uses override, indoor + storm uses species default, outdoor + null weather degrades, plant without `is_indoor` defaults to indoor), and a v1-compatibility row that proves an unmigrated row without `is_indoor` produces the same verdict as E4-001.
- `apps/mobile/src/hooks/useWateringEngine.ts` — adds an optional `weather: WateringEngineWeather | null = null` second argument; passes both `is_indoor` (from the plant row) and `weather` straight through to the engine. The async SQLite load window still returns 'check_soil' as the safe default and the `cancelled` flag still discards stale reads if `plant.id` changes mid-resolve. The `weather` argument is intentionally NOT in the effect's deps array — it's threaded through render, not used to trigger a re-fetch (only `plant.id` does), so callers feeding `weather` from `useWeather()` don't burn a SQLite read on every weather refresh.
- `apps/mobile/src/hooks/__tests__/useWateringEngine.test.tsx` — 27 tests across the load-window fallback, v1 backward compatibility (no weather arg, undefined weather, explicit null weather, plant without `is_indoor` field — every E4-001 verdict still matches), override precedence (override beats indoor, override beats weather, override unlocks species_unknown), the indoor branch (indoor + storm uses species default), the outdoor + weather modifier path (rain → +2, heat → -1, both → +1, exactly-5mm → no modifier, exactly-30°C → no modifier, max-across-both-days for heat, cumulative-across-both-days for rain), natural interval ranges (smallest species + heat → 4, largest species + rain → 16, both well within the [1, 30] cap), DST + IDL fixtures (the E4-002 invariants flow through the wrapper unchanged), the canonical 3d 12h 1ms sub-day-precision case, and the just-added-plant null-history fallback (no history beats both override and weather; rules engine refuses to claim 'water' without evidence).
- `apps/mobile/src/hooks/useWeather.ts` (~370 lines) — `useWeather({ apiClient, netInfo? })` returning `{ fetchWeather, status, lastResult }`. Discriminated union locked at six kinds: `ok` (200 fresh from upstream, runtime shape-validated), `cache_hit` (RESERVED forward-compat — the wire signal is the `x-cache: hit` header which the api client doesn't propagate today, so this kind is unreachable in V1 but kept in the union so a future api-client header passthrough is not a breaking type change), `rate_limited` (emitted when the api client surfaces `kind: 'server'` with `retry_after` populated; client.ts ALWAYS sets `retry_after` for 429 and OPTIONALLY for 5xx + Retry-After header — we accept the rare 5xx-with-Retry-After misclassification because the rules engine treats both cases the same and consumers that fork on `rate_limited` want the retry hint either way), `network_error` (transient upstream failure with no retry hint), `parse_error` (deploy skew between mobile + backend — also surfaced when the upstream ok payload fails the runtime shape check), `queued` (offline / network-degraded path — same coercion as `useDiagnoseRequest`: `network` and `timeout` both fold to queued so the rules engine sees a "skip the modifier" signal rather than an error card). Race semantics: a monotonic call-counter tags each `fetchWeather()` and the commit guard compares against the **latest started** call id (not the latest committed) so a stale older call resolving while a newer is still in-flight gets dropped, preventing the brief render-window where stale data would have committed and then been overwritten. Mount safety via `mountedRef`. No NetInfo dependency yet — `netInfo` defaults to "always online" because the api client's `kind: 'network'` already handles the actual offline detection; E7-003 will wire a real `@react-native-community/netinfo` subscription via the same prop. No retry/backoff in this layer (E7 owns retry semantics). No second cache here (the backend's KV cache from E6-001 is the only cache).
- `apps/mobile/src/hooks/__tests__/useWeather.test.tsx` — 17 tests across the happy path, every error kind in the union, the `network`/`timeout` → `queued` coercion, the `server + retry_after` → `rate_limited` re-extraction, the `server` (no `retry_after`) → `network_error` fallback, the offline `netInfo.isConnected() === false` short-circuit (returns queued without calling the api client), the api-client-throws defensive coercion, the layer1_reject defensive map, a discriminated-union sweep that locks all six kinds emit distinctly (the "no collapse" contract), the lat/lon flow-through to the api client unchanged, the race-by-call-order test (older call resolving later does NOT clobber newer call), the new race-by-start-order test (older call resolving FIRST while a newer is in-flight does NOT win the commit — locks the codex P2 fix), and three malformed-ok-payload tests (missing daily[1] → parse_error, NaN precipitation_sum_mm → parse_error, daily entry not an object → parse_error — locks the runtime shape validator).
- `apps/mobile/src/api/types.ts` + `apps/mobile/src/api/client.ts` + `apps/mobile/src/api/index.ts` — `WeatherRequest = { latitude: number; longitude: number }` and `weather()` method on the api client that builds `GET /api/weather?lat=&lon=` with manual `encodeURIComponent` (URLSearchParams adds bundle weight and its number encoding semantics are identical to manual encoding for this case).
- `apps/mobile/src/watering/index.ts` + `apps/mobile/src/hooks/index.ts` — barrel exports for the new types + the new hook.

### Adversarial review (this finishing pass)
One **P1** + two **P2s** caught + fixed before ship — the WIP would have shipped 2 red tests, an unhandled crash path, and a stale-data race window:
- **P1** — `useWeather`'s `rate_limited` kind: the module header docstring claimed the kind was RESERVED forward-compat ("the hook NEVER emits rate_limited today") and the implementation followed that docstring, mapping every `server` kind unconditionally to `network_error`. But two tests in `useWeather.test.tsx` asserted the opposite contract: `server + retry_after = 42` MUST emit `{ kind: 'rate_limited', retry_after_seconds: 42 }`, and the discriminated-union sweep test required all six kinds to emit distinctly. The WIP would have shipped these tests red. **Fix:** classifier now returns `{ kind: 'rate_limited', retry_after_seconds }` when `server + retry_after` is numeric; falls through to `network_error` otherwise. Docstring rewritten to describe what the function actually does, including the explicit acceptance of the rare 5xx-with-Retry-After misclassification (the rules engine treats `rate_limited` and `network_error` the same — skip the modifier on this call — so the misclass is benign for the V1 consumer; consumers that surface a "try again in N seconds" hint get the retry_after_seconds either way).
- **P2** — `useWeather`'s `classifyApiResult` returned `kind: 'ok'` unconditionally on `raw.ok === true`, trusting the wire shape end-to-end. A deploy-skew payload missing `daily[1]` would reach `engine.ts` and crash on `weather.daily[1].precipitation_sum_mm`; a payload with `NaN` in a numeric field would slip through `typeof === 'number'` and pollute the threshold comparators. **Fix:** new `isValidWeatherShape()` validator runs before returning `kind: 'ok'`. It checks both daily entries are objects with `Number.isFinite()` `precipitation_sum_mm` and `temperature_max_c` — the exact subset the rules engine reads. Malformed payloads now downgrade to `kind: 'parse_error'`; the rules engine sees the engine-level null guard and degrades gracefully. Three new tests lock the contract.
- **P2** — `useWeather`'s race guard compared resolution order against `lastCommittedCallIdRef` (the latest call that already won the commit), which let a stale older call briefly win the commit when it resolved while a newer call was still in-flight (sequence: call#1 starts → callCounter=1, call#2 starts → callCounter=2, call#1 resolves first → 1 ≥ lastCommitted=0 → commits stale, call#2 resolves later → 2 ≥ 1 → corrects it). The original `race-by-call-order` test only exercised the inverse scenario (older resolves AFTER newer). **Fix:** comparison is now against `callCounterRef.current` (the latest STARTED call id), so any non-latest resolution drops silently. The fix is one line of substance + one new regression test that drives the codex-flagged race directly: both calls in-flight, older resolves first → must NOT commit; newer resolves → commits. Status stays `'requesting'` between the two resolutions, locking the no-stale-render-window contract.

### V1 scope locks (rejected reviewer suggestions)
- **No date-fns / dayjs / luxon / Temporal.** The engine uses plain UTC ms math; importing a date library would re-introduce the calendar-walking failure modes E4-002 was written to prevent.
- **No calendar walking.** `Date.getDate`, `setHours(0, 0, 0, 0)`, `Math.floor((now - then) / 86400000)` — all banned in the rules engine. The DST + IDL regression suite (E4-002) enforces this.
- **No more than 2 forecast days.** V1 = current + 2 only, locked by `WateringEngineWeather.daily: [WateringEngineWeatherDaily, WateringEngineWeatherDaily]` tuple typing.
- **No different threshold values.** 5mm precipitation, 30°C heat are locked by master plan + ticket spec, asserted as exported constants in tests.
- **No separate weather provider abstraction.** Open-Meteo is the V1 provider — same lock as E6-001 and E6-002.
- **No removal of v1 backward compatibility.** A v1 caller passing only `{ id, species_slug, override_interval_days }` and no weather arg gets identical behavior to E4-001; `is_indoor` defaults to `true` at the engine boundary.
- **No compression of the WateringStatus enum.** `'water' | 'skip' | 'check_soil'` is the locked surface, fed into `<StatusChip>` and the A-1/A-2 chips.
- **No collapse of the `useWeather` discriminated union.** Six kinds are tested distinctly; collapsing `network_error` and `parse_error` into a generic 'error' was rejected — they have different upstream causes (transport vs schema) and a future telemetry consumer needs to fork on which one fired.

### Notes
- **Why the modifier is additive (+2 / -1) and not multiplicative:** the master plan sketches both an additive form (`if rain_in_next_2_days: adjusted_target += 2`) and a multiplicative form (`humidity_modifier`, `temp_modifier` per species). E6-005 lands the additive form because (a) the species table doesn't yet carry per-species humidity/temp modifiers — the multiplicative path needs E2-002's species table extended, which is post-V1, and (b) additive math has clearer integer-rounded edges, which the bias-zone math depends on (a 0.7-day shift would land the half-interval boundary at unintuitive timestamps).
- **Why the engine layer is a pure function (no React) and the hook is the wrapper:** mirrors the v1 split. Pure functions are trivially testable without a render harness, and the React hook just plumbs SQLite + state into the pure boundary. The pure function is the math; the hook is the plumbing.
- **Why `useWeather` exposes `lastResult` AND `status`:** `status` is for screens that just want a state-machine string ("idle" / "requesting" / "success" / "rate_limited" / etc.); `lastResult` is for screens that need to fork on the discriminated union (e.g. surface a Retry-After hint to the user). Same shape as `useDiagnoseRequest` for consistency.
- **Stack pattern:** this branch is direct off `Anandsatch/e6-weather-endpoint` (PR #44, E6-002). The PR's base is set to that branch, not main. GitHub will auto-rebase the PR onto main when PRs #40 + #44 land; squash-merge will collapse the WIP + the codex-fix commit + this VERSION/CHANGELOG bump into one feat() at land time.
- **Test count:** mobile suite is now 416 (was 353 before this ticket). Backend suite unchanged at 208. Mobile `tsc --noEmit` clean.

## [0.1.48.0] - 2026-05-08

E10-001 — dark-mode rendering verification across every primitive in the Component Garden manifest, plus the one primitive fix the verification surfaced. Stacks on E2-013 (PR #32, the Component Garden screen) and treats that screen's manifest as the coverage anchor: every entry in `PRIMITIVE_SECTIONS` now has a corresponding dark-mode test that renders the primitive twice via `<ForcedTheme>` (once with `lightTheme`, once with `darkTheme`) and asserts three structural invariants — token-driven background swaps between schemes, no color resolves to a forbidden fallback (`undefined` / `null` / empty string / `NaN`), and text-on-bg passes the WCAG-AA 4.5:1 contrast floor in BOTH themes. The contrast helper is inline (~30 lines of WCAG 2.1 relative-luminance math) rather than pulled in as a library — V1 scope lock against new deps holds. Sanity assertions on the helper itself anchor it against the known Conservatory/Midnight `text` × `bg` ratios from DESIGN.md (~13.32:1 and ~16.55:1 respectively) so a regression in the helper fails loudly before any primitive assertion runs.

The verification surfaced one real bug in `ToastBanner`. The banner uses `theme.colors.tan` / `theme.colors.sage` for its background — both intentionally constant across Conservatory and Midnight per the DESIGN.md "Status icon system" lock — but the message text was reading `theme.colors.text`, which inverts to cream (`#FAF6EE`) in dark mode. Cream-on-tan computes to 2.09:1; cream-on-sage to 1.68:1. Both are well under the 4.5:1 AA floor and would have shipped as a real legibility failure for any user who flipped their phone into dark mode. The fix locks the on-accent text color to a fixed deep ink (`#2A2A2A`) — the same value as `lightColors.text` — for both message and action labels. Light-mode ratios stay green at 6.38:1 (tan) and 7.91:1 (sage); dark-mode ratios now match (tan/sage are constants, so the ink-on-warm-paper relationship is identical in both schemes). No new "onAccent" token was introduced into the theme tables — V1 token tables are locked per DESIGN.md and the master plan, and this is a localized override on a primitive that has the only text-on-warm-accent surface in the system.

### Added
- `apps/mobile/src/screens/__tests__/ComponentGardenScreen.dark.test.tsx` — 30 tests across 12 describe blocks. Three contrast-helper sanity assertions (Conservatory text-on-bg ~13.32:1, Midnight text-on-bg ~16.55:1, invalid hex returns null), one manifest-parity assertion (the dark suite must enumerate every `PRIMITIVE_SECTIONS` entry — fails when a future primitive ships without a dark-mode test), and one block per primitive (Droplet, LeafIcon, HandOnSoilIcon, StatusChip, EditorialButton, FAB, HeroPhoto, EditorialBottomSheet, ToastBanner). Each block exercises its primitive's full variant set: StatusChip in all three types (water/skip/soil), EditorialButton in both variants (filled/outline), ToastBanner in all three types (warn/info/pending), EditorialBottomSheet in both open and closed states. The shared helpers `collectColorStrings`, `findFirstBg`, `findFirstTextColor`, and `unpackToHex` walk the JSON test-tree; `unpackToHex` is load-bearing because react-native-svg packs Path fill/stroke as `{ type: 0, payload: 0xFFRRGGBB >>> 0 }` objects rather than hex strings — without unpacking, the SVG-fill swap assertions for Droplet/LeafIcon/HandOnSoilIcon would silently pass against an empty array.
- `apps/mobile/src/components/primitives/__tests__/ToastBanner.test.tsx` — 2 new regression tests (E10-001 callouts in the test names) pinning the message-color and action-label-color contracts to `#2A2A2A` in both themes. These guard against a future refactor that "fixes" the apparent inconsistency by re-introducing `theme.colors.text` and silently regressing dark-mode contrast.

### Changed
- `apps/mobile/src/components/primitives/ToastBanner.tsx` — message text and action-button label now use a module-local `ON_ACCENT_INK = '#2A2A2A'` constant instead of `theme.colors.text`. The constant is documented inline with the contrast math (cream-on-tan = 2.09:1, ink-on-tan = 6.38:1) and a pointer to the DESIGN.md "Status icon system" lock that makes tan/sage theme-invariant. No other theme token usages changed — `bannerColor()` still reads `theme.colors.tan` / `theme.colors.sage` so a future palette adjustment to either accent threads through automatically.

### Verification
- Mobile: 423 tests across 28 suites (was 391 across 27 — +32 tests, +1 suite). Typecheck clean.
- Backend: 148 tests across 8 suites (unchanged from E2-013 baseline).
- Total workspace: 571 tests (was 539; +32 from this PR).

### Adversarial review (codex + Claude subagent)
- **P1 (addressed)** — the verification itself surfaced the ToastBanner dark-mode contrast bug: `theme.colors.text` on tan/sage in dark mode = 1.68:1, well below WCAG-AA. Resolution: locked text-on-accent to `#2A2A2A` in `ToastBanner.tsx` + added regression tests in both `ToastBanner.test.tsx` and the new dark suite. Finding it confirms the test surface works.
- **P2 (addressed)** — the test walker initially crashed on Text leaves (raw strings in the JSON tree have no `props`). Resolution: `walk()` now guards with `if (!n || typeof n !== 'object' || !n.props) continue` so the walker stays robust against future RN renderer changes that may emit string children differently.
- **P2 (addressed)** — first draft of HandOnSoilIcon's swap test rendered LeafIcon by accident (copy-paste). Resolution: caught and corrected before ship.
- **No P1s beyond the ToastBanner regression.** Token verification: every `theme.colors.*` access in the new test file references a real `ColorTokens` field. No assumed `theme.warn` (V1 token table holds). The `ForcedTheme` composition pattern matches E2-013's prior choice — the master plan's V1 scope lock against a global `<ThemeProvider>` is preserved.

### V1 scope locks (rejected reviewer suggestions)
- No `react-native-a11y` or `wcag-contrast` dependency. Inline luminance math (~30 lines) is sufficient and keeps the workspace dep tree at the V1 lock.
- No image-snapshot runner. V1 is structural-only per the master plan GSTACK REVIEW REPORT footer; pixel comparison is post-V1.
- No new theme token (no `colors.onAccent` etc.). The localized `ON_ACCENT_INK` constant lives inside `ToastBanner.tsx` because that primitive is the only text-on-warm-accent surface in V1; introducing a system-wide token would require a DESIGN.md amendment we haven't budgeted.
- No global `<ThemeProvider>` at the app root. `<ForcedTheme>` (the existing E2-013 shim) is the only theme-override surface; production code still falls through to `useColorScheme()`.

## [0.1.44.0] - 2026-05-08

E3-003 — `<PlantsListScreen>` is the A-1 Garden Home that composes the E3 epic's pieces into one screen: `<EmptyGardenWelcome>` (E3-002, in main) for the Day-1 zero-plant card, `<PlantCard>` (E3-001, this stack's base) for each row, and `<FAB>` (E2-009, in main) for the bottom-right "+". Status is pre-computed at the SCREEN layer via a single `MAX(watered_at) GROUP BY plant_id` query against `idx_water_plant_date`, then routed through the pure `computeWateringStatus` per plant — one SQL round-trip, N pure decisions, no per-card hooks (the contract `<PlantCard>` already shipped against in E3-001). 30 screen tests, 410 mobile tests total (was 380 on E3-001's stack base). Stacks on `Anandsatch/e3-plantcard` (PR #31).

### Added
- `apps/mobile/src/screens/PlantsListScreen.tsx` — A-1 home screen. Header: Fraunces "PlantCare" wordmark (32px) + small all-caps Inter "MY GARDEN — N PLANTS" eyebrow under it (matches the light A-1 mockup, light → dark inversion is symmetric — both layouts read the wordmark as the dominant title and the eyebrow as supporting copy). Zero-plant state hides the FAB and centers `<EmptyGardenWelcome>` (master plan: "The floating '+' FAB is hidden until 1+ plants exist (one CTA, no choice paralysis)"). 1+-plant state renders a `<FlatList>` of `<PlantCard>` rows keyed by `plant.id` (UUID, never index — list-key stability across sort/filter), with a hairline forest separator between rows (`StyleSheet.hairlineWidth` × 0.2 opacity, mirrors DESIGN.md "Hairline dividers between list rows"). FAB anchored bottom-right (16px right / 24px bottom inset) inside an absolutely-positioned `pointerEvents='box-none'` overlay so the FlatList scroll surface stays uninterrupted. Per-render `Date.now()` snapshot captured once and forwarded to every PlantCard via `nowMs` so two cards never disagree on "today" because a millisecond ticked between row renders. AppState `'active'` re-fetches plants (overnight-wake-up case from the master plan's "open the app in the morning, see the chips" path). Refresh callback exposed via `getMostRecentRefresh()` test seam for the future A-1 tan-banner Retry affordance (no pull-to-refresh — master plan: "Scroll: vertical only, no pull-to-refresh in V1 (data is local)"). Loading window: cream surface frame, no spinner (master plan: "loads instantly from local SQLite — no spinner, no friction"). DB read failure: defensive empty-state fallthrough so the FAB stays available.
- `apps/mobile/src/screens/__tests__/PlantsListScreen.test.tsx` — 30 tests. Coverage: empty state (no FlatList, no FAB, CTA fires `onAddPlant`); populated state (1 / 2 / N rows, header pluralization for `MY GARDEN — 1 PLANT` / `… 2 PLANTS`, header has `accessibilityRole='header'`); status batching (single `db.getAllAsync` call for N plants, SQL contains `MAX(watered_at)` + `GROUP BY plant_id`, `resolvePlantViewModels` routes plants without history through the engine fallback, `fetchWateringSummary` defensively skips null-valued rows); FAB tap → `onAddPlant`, FAB long-press → `onLongPressFAB` (E3-004 contract — independent of tap), FAB has `accessibilityRole='button'` + `accessibilityLabel='Add a plant'`; row tap → `onPlantPress(plant)` with the right `Plant`; refresh handle re-runs `usePlants().list()` + `fetchWateringSummary` in tandem, AppState transition to `'active'` re-fetches; **resume-race regression** (codex P2 fix): a stale pre-resume load that resolves AFTER a resume load is dropped via the generation-counter guard, fresh state stays on screen; refresh handle hygiene (clears on unmount, no leak across tests); loading state renders skeleton with `accessibilityLabel='Loading your garden'`, no FAB / list / empty during the skeleton window; DB read failure routes to empty state; reduce-motion passes through (no screen-level animations, only `<PlantCard>`'s pressed-state gate from E3-001); dark theme tokens swap on the screen background; eyebrow renders the literal `'MY GARDEN — 1 PLANT'`; **DST/IDL forwarding regression** — both rows in a 2-card render see the same `nowMs` reference and emit the same relative copy (`'Last watered 3 days ago'`); unmount safety (resolves an in-flight `list()` AFTER unmount → no React setState warning).
- `apps/mobile/src/screens/` directory — first screen in the project. Future screens (PlantDetail / CameraCapture / CameraResult / AddPlant / WeeklyReview) land alongside as their epics ship.

### Adversarial review (codex)
One P2 + two P3s caught and fixed.

- **P2 (fixed)** — AppState resume race. The original implementation re-used the same `signal` object for the initial-mount load and every AppState resume load. `signal.cancelled` only flipped on unmount, so an older pre-resume load could resolve AFTER a fresh resume load and clobber the post-resume state. Fixed by switching to a `loadGenerationRef` counter: every load invocation captures the current generation, AppState resume bumps the counter to supersede prior loads, and `setPlants` / `setLastWateredByPlantId` only commits if `loadGenerationRef.current === generation` at resolution time. New regression test `'a stale pre-resume load that resolves AFTER a resume load does not clobber state'` constructs the exact race (initial load + AppState resume + initial-load resolves last) and asserts the screen shows the fresh world, not the stale.
- **P3 (fixed)** — `RefreshHandle` mutated a module-scoped `Map` during render with no cleanup. Tests could leak stale `refresh` callbacks between cases, and a Strict-mode-discarded render could expose a callback for an uncommitted tree. Fixed by moving registration into a `useEffect` (committed-tree only) keyed on a per-mount `Symbol` ref, with a cleanup that deletes the entry. Added `__resetRefreshHandlesForTests()` for `afterEach` paranoia. New test `'clears the refresh handle on unmount (no leak across tests)'` asserts `getMostRecentRefresh()` returns `undefined` post-unmount.
- **P3 (fixed)** — Strict-mode comment was stronger than the code: it claimed "subscribing twice is a no-op" but `usePlants` is a per-mount `useMemo`, not a module singleton. Comment rewritten to match the actual behavior (the `loadGenerationRef` bump on cleanup invalidates double-mount loads; the `openDb()` connection is module-memoized; per-mount `list()` calls are harmless because the result map is keyed by plant.id).

**Verified, no action** (codex confirmed):
- Status batching is screen-level with one `MAX(watered_at) GROUP BY plant_id` query backed by `idx_water_plant_date`.
- FlatList key extraction uses `plant.id` (UUID), not the index.
- FAB tap and long-press are independently wired (no path-sharing).
- No screen-level animations introduced; reduce-motion is honored via `<PlantCard>`'s pressed-state gate (E3-001).
- Header / list / FAB accessibility order is structurally correct (header announces first, list rows in document order, FAB last).
- `effectiveNow` is captured once per render and forwarded consistently to `computeWateringStatus` and to every `<PlantCard>` via `nowMs`.
- Empty state hides the FAB; no pull-to-refresh wired (master-plan locked).

### V1 scope locks (rejected reviewer suggestions)
- No virtualization library beyond FlatList. RN's built-in windowing covers the V1 row count target (≤20 plants).
- No `react-query` / `swr` / data-fetching lib. The screen owns its own load + AppState resume cycle; the upcoming offline queue (E10) is the right layer for refetch orchestration.
- No `date-fns` / `dayjs` / `luxon` / Temporal. The local-tz calendar walk lives in `<PlantCard>` (E3-001) and the watering engine (E4-001); the screen does no date math beyond the per-render `Date.now()` snapshot.
- No ORM. Raw parameterized SQL is the V1 storage contract.
- **No pull-to-refresh.** Master plan A-1 § Scroll: "vertical only, no pull-to-refresh in V1 (data is local)." Refresh happens implicitly on AppState `'active'` for the overnight-wake-up case; explicit Retry lands as a tan banner per the master plan's DB-read-failure interaction state.
- No server-side sort / pagination. Local SQLite + `ORDER BY added_at DESC` (in `usePlants.list()`) is the V1 sort contract.
- No collapsing the `WateringStatus` enum. The three-color metaphor lock from DESIGN.md ("water / skip / check_soil") stays.

### Notes
- `RefreshHandle` is a side-channel for tests, not a public API. The component renders nothing; it exists purely to register `refresh` inside an effect (committed-tree only) so test seams don't expose stale callbacks. The future A-1 tan-banner Retry affordance can either consume `getMostRecentRefresh()` directly or be migrated to a ref forwarded from the parent shell.
- The screen does not pre-resolve photo URIs from the `photos` table — `<PlantCard>` receives `photoUri={null}` for V1 list rendering, and `<HeroPhoto>`'s cream-skeleton fallback covers the queued-identify and brand-new-plant shapes correctly. Photo loading lands when E5 (Camera) ships and the `hero_photo_id` → URI resolver becomes a needed surface.
- The header layout (Fraunces title above all-caps eyebrow) matches the light A-1 mockup. The dark D-1 mockup inverts the order (eyebrow above title); both readings are valid editorial choices and the light layout is canonical per `DESIGN.md` § Approved mockups.
- Stacks on `Anandsatch/e3-plantcard` (PR #31, the E3-001 PlantCard composite). Auto-rebases to main when E3-001 lands.

## [0.1.43.0] - 2026-05-08

E7-002 — `SyncDrainer` orchestrator. Stacks on E7-001's CRUD layer; drives queued LLM requests through the api-client and updates per-row state via the typed mutators. Locked behaviors: serial drain (one in-flight at a time — V1 lock against parallel drainer / job-runner / batch dispatch), idempotent `drainNow()` (concurrent calls share one promise reference), graceful `stop()` that finishes the current row before breaking the loop, 7-day TTL sweep + 5-min stuck-in_flight recovery on app launch, and 429 retry-after taking precedence over the locked backoff schedule (custom delay; attempt count NOT incremented). Hooks for E7-003 (NetInfo + AppState wiring) and E7-006 (Tap to retry) are not wired in this PR — that's E7-004's job. UTC ms only; no calendar math (DST/IDL static contract enforced by source-grep, mirroring the watering engine's E4-002 critical-regression boundary and the CRUD layer's same contract).

### Added
- `apps/mobile/src/sync/SyncDrainer.ts` — drainer module. `createSyncDrainer({ db, apiClient, now?, batchLimit?, stuckInFlightThresholdMs? })` returns `{ drainNow, runStartupSweep, stop, isDraining }`. `drainNow()` pulls a `selectReadyForRetry({ nowMs, limit })` batch (default 50, the 5×8-day worst-case offline ceiling with headroom) and processes rows serially in `next_attempt_at ASC` order: `markInFlight` → dispatch → outcome-classify → terminal mutator. Outcome taxonomy: success → `markDone`; network/timeout/server-without-retry-after → `scheduleBackoff` (burns one schedule slot; 6th attempt terminal-fails per CRUD contract); 429 with `retry_after` → custom delay UPDATE that bypasses the locked schedule and does NOT increment attempt_count (rate-limit ≠ failure); parse_error / layer1_reject / low_confidence → `markFailedTerminal`; paradoxical `kind: 'queued'` → `markFailedTerminal` with diagnostic message; unknown endpoint string or unparseable `payload_json` → `markFailedTerminal` without an api call. `runStartupSweep()` runs `sweepStaleEntries` first (cheap, deletes terminal rows past 7 days) then `recoverStuckInFlight` (in_flight rows older than 5 min reset to pending with attempt_count++ via `scheduleBackoff` so the schedule still terminates; iOS suspend can keep an app silent up to 30s, so 5 min is 10x headroom against false positives). `stop()` flips a flag the per-row loop checks between rows — the active row finishes (no mid-row kill, partial state would mean an unparseable in_flight row whose API status is unknown). `drainNow()` is synchronous-bodied (the function is not declared `async`) so concurrent callers receive the SAME promise reference, not separate wrappers — referential equality is the idempotency contract callers and tests rely on.
- `apps/mobile/src/sync/__tests__/SyncDrainer.test.ts` — 31 tests across empty drain (no api calls, zero summary), single-row happy path (markInFlight → api → markDone), FIFO ordering (out-of-insertion-order rows drained by `next_attempt_at` ASC), serial enforcement (delayed mock confirms row N+1 doesn't start before row N finishes), retryable kinds with attempt-count bumps (network / timeout / server-no-retry-after / sequential 6-attempt walk to terminal failure), the 429 retry-after override (single + 10-in-a-row attempt_count stays 0), terminal kinds (parse_error / layer1_reject / paradoxical queued / unknown endpoint string / unparseable payload_json), `stop()` (mid-drain halt with in-flight row finishing, stop flag resets between drains), `drainNow()` idempotency (concurrent calls share one promise reference; isDraining toggles), 7-day TTL sweep (terminal-only deletion preserves pending + in_flight regardless of age, sweep runs before first drain), stuck-in_flight recovery (1-hour-old row reset to pending with attempt++, the 5-min boundary regression where 4m 59s is preserved and 5m 1s is recovered, schedule respect where attempt_count=5 + recovery → terminal failure), all four endpoint kinds round-trip through the api-client, and the DST/IDL source-grep static contract.

### Adversarial review (codex)
Two P2s and one P3 caught + addressed before ship; mobile test count went 391 → 429 (+38) net of new tests added to lock the fixes.

- **P2 — `markInFlight` claim race in `processRow`.** Codex pointed out that `markInFlight` returns void and silently no-ops when the row is already in_flight, so two `SyncDrainer` instances that select the same pending row could both dispatch the API call. The first claim wins at the SQL layer but the loser still hit the network. V1 ships a single drainer instance per app so the race is theoretical, but it also makes the 429 direct UPDATE racy: a losing drainer could flip the row back to `pending` after the winner had already called `markDone`, except `markDone`'s state guard prevents that — but the wasted API call is real. **Fixed** by adding a typed `claimInFlight(db, id): Promise<boolean>` to the CRUD layer that runs the read+write inside `withExclusiveTransactionAsync` and returns whether the caller actually flipped the row. The drainer's `processRow` now calls `claimInFlight` instead of `markInFlight`; on `false` we skip the dispatch entirely (no API call, no markDone, no scheduleBackoff) and decrement the processed counter so the summary reflects actual work. New tests: `drainer skips dispatch when row was claimed by a sibling between select and claim` (drainer side) and four CRUD tests (`returns true on first call`, `returns false on second`, `returns false on done/failed/missing`, `two concurrent claims — exactly one wins`).
- **P2 — 5xx-with-Retry-After loops without burning attempts.** Codex pointed out that the api-client (`apps/mobile/src/api/client.ts:298-300`) attaches `retry_after` to 5xx responses too, not only 429. The drainer treats every `server + retry_after` outcome as `rate_limited` and bypasses the schedule, so a backend stuck on 503 + Retry-After would never increment `attempt_count` and never terminal-fail. **Fixed** by capping consecutive rate-limit responses per row in-memory at `MAX_CONSECUTIVE_RATE_LIMITS = 10`. After the cap, the drainer demotes to `scheduleBackoff` (which burns an attempt and is on the path to terminal failure at attempt 6). The counter resets on any non-rate-limit outcome and on app restart (the drainer is constructed fresh) — acceptable because a backend stuck on this pattern across sessions hits the schedule's terminal-fail at session boundary. The right long-term fix is the api-client distinguishing 429 from 5xx-with-Retry-After in its kind taxonomy; that's a wider type-surface change deferred to its own ticket. New tests: `multiple 429s in a row never advance attempt_count up to the consecutive cap` (renames the prior test to clarify the bound), `after MAX_CONSECUTIVE_RATE_LIMITS hits, drainer demotes to schedule (burns an attempt)`, `a successful response between rate limits resets the consecutive counter`. The cap is wired via `maxConsecutiveRateLimits` in `SyncDrainerConfig` so tests can use a small value (3) to hit the demotion edge cheaply.
- **P3 (acknowledged, not deferred) — Parseable but malformed payloads still hit the API.** Codex noted that a payload like `{}` for `diagnose` round-trips as a successful JSON.parse but the backend would reject it as `missing_image`, which the api-client maps to `server`, which the drainer routes through `scheduleBackoff` until terminal-fail. This isn't an infinite loop (the schedule terminates at attempt 6) and the comment "payload that fails schema reconstruction" was directional rather than promising runtime validation. **Acknowledged** in the file header; per-endpoint payload validators are E7-004's job (the hooks that *enqueue* know the payload shape and can reject malformed input at enqueue time, before it ever sits in the queue). Adding shape validators to the drainer would duplicate logic.

### V1 scope locks (rejected)
- Parallel drainer / multiple in-flight rows. Serial drain is the master-plan contract — see file header for the four reasons (rate-limit fairness, backoff fidelity, lost-update defense, bandwidth/battery on weak cell).
- Job-runner library (queue-promise, p-queue, bull). The state machine is a 30-line for-loop over CRUD mutators.
- ORM (Drizzle / Prisma / Kysely). Mirrors the CRUD layer.
- date-fns / dayjs / luxon / Temporal. UTC ms only.
- Wiring this into existing hooks (`useDiagnoseRequest` / future identify / consult). That's E7-004's job; pulling persistence into hooks now would tangle two epics.
- A different backoff schedule. The 1m / 5m / 30m / 2h / 8h / fail sequence is master-plan-locked.
- Cloud-side acknowledgement / sync infrastructure. The queue is mobile-side only in V1.
- A "custom delay" entry point on the CRUD layer. Adding one would surface a foot-gun (callers could bypass the locked schedule); the 429 override stays drainer-local so the precedence is explicit at the call site.

### Notes
- **Stuck-in_flight 5-min threshold proxy:** the schema has no `last_attempt_started_at` column. The drainer reuses `next_attempt_at` as a proxy — it's the value at `markInFlight` time (markInFlight doesn't write it; only `scheduleBackoff` / the 429-rescheduler do). A row with `status='in_flight' AND next_attempt_at <= nowMs - threshold` means the drainer never reported terminal state, i.e. crash/kill/suspend. The threshold (5 min) covers a slow upload over weak cell (api-client default timeout is 20s; 5 min is 15x headroom). Future schema rev (post-V1) can split this into a dedicated column.
- **Recovery counts as one attempt:** stuck rows are routed through `scheduleBackoff` (with the row's existing in_flight status satisfying the state guard) rather than directly reset to pending. Reason: a row repeatedly crashing the app mid-drain otherwise loops forever; counting recovery against MAX_ATTEMPTS lets the schedule terminate. A row stuck on its 5th attempt → recovery → 6th attempt → terminal-fail.
- **Unknown endpoint = terminal, not retryable:** a row whose endpoint we don't recognize is deploy skew (the producer wrote a kind we don't know how to drive). Retrying won't fix it; we `markFailedTerminal` with diagnostic message and move on. Same logic for unparseable `payload_json` — the row's *shape* is the problem.
- **The 429 override is a direct UPDATE, not a CRUD call:** the CRUD's `scheduleBackoff` is locked to the master-plan schedule; adding a "schedule with custom delay" entry point would surface a foot-gun. The drainer's `rescheduleAfterRateLimit` writes directly with `WHERE status='in_flight'` to defend against concurrent terminal mutations.
- **drainNow() must be synchronous-bodied to preserve promise identity:** an outer `async function` would wrap each return value in a fresh promise, breaking referential equality on concurrent calls. The implementation cache-and-returns `activeDrain` directly so `drainNow() === drainNow()` while a drain is in flight.

## [0.1.42.0] - 2026-05-08

E5-009 — `<CameraResultScreen>` A-3 error variants. Replaces the E5-008 `// TODO E5-009` placeholder with distinct UI per kind from the `ApiResult<DiagnoseResponse>` discriminated union, plus the screen-internal `compress-failed` phase. Stacks on PR #33 (E5-008 success path); base for merge is `Anandsatch/e5-camera-result`, not `main`. Every locked kind from the master plan stays distinct in the type, in the resolver, and in the rendered JSX — no kind collapses into another. 33 new screen tests (mobile suite 410, was 376; the 5 placeholder tests from E5-008 are updated to assert the new variant testIDs rather than the placeholder copy).

### Added
- **`low_confidence`** — editorial Fraunces "We're not quite sure" headline, Inter narrative explaining the model wasn't confident, primary "Pick from list" CTA → `onPickFromList()` (opens manual species picker — same shape E5-010 will consume), secondary "Try a different photo" → `onRetake()`. **Does NOT auto-save anything** (master plan: low confidence is a deliberate user choice point, not a degraded auto-save).
- **`timeout`** — "Diagnose is taking longer than usual." Primary "Try again" → re-fires `diagnose()` against `phase.compressed.uri` (never recompresses). Secondary "Save photo for now" → `onSavePhotoOnly(payload)` for parent-owned persistence (the photo + the unresolved discriminated-union `result` are both forwarded so the parent can record the failure kind alongside the photo for later retry).
- **`network`** — distinct copy from timeout: "We can't reach the server." Same retry + save-for-now CTA pair as timeout. Note: `useDiagnoseRequest` coerces api-client `network` → `queued` for the V1 offline UX (master plan, line 346), so this variant rarely surfaces directly today; the rendering path is still load-bearing because (a) the kind is on the locked union, (b) the resolver returns it, (c) a future netInfo-aware path or hook change could surface it. Variant testID + copy unit-tested via `__testing.resolveErrorVariant`.
- **`server`** — generic "Something went wrong on our end." Primary "Try again" → re-fires diagnose. Secondary "Report" → `onReportError({ kind, message, photoUri, mode })` so parent forwards a redacted payload to telemetry. The union's `retry_after` is parsed by the api client but not consumed by the UI yet (V1 leaves backoff to the user).
- **`parse_error`** — distinct from `server` because the failure mode is local-parse, not upstream: "We got a response we couldn't understand." Single "Try again" CTA → re-fires diagnose. No save-for-now / report (the failure mode is transient and self-clearing).
- **`layer1_reject`** — kind editorial card: Fraunces "We're focused on plant care", Inter body explaining the photo doesn't appear to be a plant. Single "Try a different photo" CTA → `onRetake()`. Voice mirrors the E8-003 AddNoteSheet's reject card per the brief.
- **`queued`** — pending toast-style state: "We'll save when you're back online." Renders `<ToastBanner type='pending'>` from the primitives barrel (E2 ship). No retry CTA — the sync layer (E7) drains the queue when connectivity returns. `accessibilityRole='status'` (non-disruptive) per the master plan.
- **`compress-failed`** (screen-internal) — same editorial tone as `parse_error`: "We had trouble preparing your photo." Single "Try a different photo" CTA → `onRetake()`. This kind isn't on the API union; it's a screen-local phase from the compression pipeline that gets first-class UI alongside the API kinds.
- New props on `CameraResultScreenProps`: `onRetake`, `onPickFromList`, `onSavePhotoOnly`, `onReportError`, `announceForAccessibilityImpl` (test seam). Each callback is optional — when omitted, the corresponding CTA is hidden so the screen doesn't promise an action it can't fulfill (omitted `onSavePhotoOnly` hides the "Save photo for now" button on timeout/network; omitted `onReportError` hides the "Report" button on server). New payload types `CameraResultSavePhotoOnlyPayload` + `CameraResultReportErrorPayload` exported via `apps/mobile/src/screens/index.ts` so navigation/route registries import them from a single surface.
- `resolveErrorVariant(phase, lastResult)` helper centralizes the per-kind UI mapping. Precedence is locked: `compress-failed` always wins over a stale API result. Returned variant carries headline + body + optional message (forwarded from the union for telemetry). Exhaustiveness is enforced via `never` in the switch default so a future kind addition is a compile error, not a runtime no-op.
- Reduce-motion: every error variant uses the same audit-defensible compliance pattern as the success card and DiagnoseLoadingState. `errorOpacity = useRef(new Animated.Value(reduceMotion ? 1 : 0)).current` initialized at end-state under reduce-motion; `Animated.timing` is skipped entirely (not damped) when reduce-motion is on.
- A11y: each error variant has `accessibilityRole='alert'` (or `'status'` for queued — non-disruptive); `accessibilityLabel={variant.copy.headline}`; on transition into a non-success state, `AccessibilityInfo.announceForAccessibility(headline)` is called once per kind (idempotent on re-render via a `lastAnnouncedKeyRef` latch). The `announceForAccessibility` choice over `setAccessibilityFocus` is documented in the file header — `findNodeHandle` is fragile under React strict-mode and adds noise to the test harness.
- 33 new screen tests in `apps/mobile/src/screens/__tests__/CameraResultScreen.test.tsx` (410 total mobile, was 376). Coverage: every kind renders its distinct UI (asserts headline copy + CTA labels per kind); "Try again" on timeout/server/parse_error re-fires diagnose with the SAME compressed photo (compressSpy stays at 1); "Save photo for now" fires `onSavePhotoOnly` with the right payload including the unresolved result; "Pick from list" on low_confidence fires `onPickFromList`; "Try a different photo" on layer1_reject + low_confidence + compress-failed fires `onRetake`; "Report" on server fires `onReportError({ kind, message, photoUri, mode })`; reduce-motion hard-disables the variant fade (opacity is 1 immediately, no Animated.timing); dark theme: error headline uses `darkTheme.colors.text`; a11y roles per kind (alert / status); `announceForAccessibility` called with the headline on transition; announce is idempotent on re-render; strict-mode double-mount: diagnose fires exactly once + the error variant renders exactly once; discriminated-union sweep asserts every locked kind (sans hook-coerced network) yields its own testID suffix; `resolveErrorVariant` unit tests cover null cases + every kind + compress-failed precedence.

### Adversarial review
- **No P1 findings.** Codex CLI walked the 8 documented risk surfaces explicitly briefed: (a) discriminated union STAYS distinct — `resolveErrorVariant` covers every locked kind, no two kinds render the same JSX; (b) "Try again" reads `phase.compressed.uri` directly + feeds it back into `diagnose()`, compression spies stay at 1 across every retry test; (c) reduce-motion init pattern matches DiagnoseLoadingState/E5-008 — `Animated.Value(reduceMotion ? 1 : 0)` + skip `Animated.timing`, no damped animation; (d) a11y role + announce on transition, idempotent on re-render; (e) ToastBanner is a hard import — verified the symbol is in `origin/Anandsatch/e5-camera-result` so the stack is consistent (codex flagged this conditionally; documented in the file header); (f) callbacks named consistently with E5-008's `onSave` voice; (g) `network → queued` hook coercion is intentional offline UX, not a screen bug — the resolver still routes `network` cleanly for unit tests + future hook changes; (h) strict-mode safety — diagnose latch holds; compression effect re-runs but cancellable; new effects (errorFadedRef + lastAnnouncedKeyRef) latch idempotently.
- **One P2 caught + fixed before ship:**
  - **P2** — Stale error card co-rendered with the loading dock during "Try again". `useDiagnoseRequest` flips `status='requesting'` on retry but keeps the previous error in `lastResult`, so the original render path called `resolveErrorVariant` unconditionally and the stale timeout/server card stayed visible alongside the spinner — would double-announce on a screen reader and let the user re-press the already-pressed CTA. Fixed by gating the entire error-variant render block on the `isErrorVariantVisible` boolean (which already excludes `status === 'requesting'` in its own definition, used for the fade effect). Locked under the new `codex-P2: stale error card hidden while a retry is in flight` test.

### V1 scope locks (rejected reviewer suggestions)
- **No collapsing of the discriminated union.** Every kind from `ApiResult<DiagnoseResponse>` (`network`, `timeout`, `server`, `layer1_reject`, `low_confidence`, `parse_error`, `queued`) plus the screen-internal `compress-failed` phase gets distinct headline copy, distinct testID suffix, and distinct CTA stack. The discriminated-union sweep test asserts every kind has its own `r-error-{kind}` testID.
- **No date-fns / dayjs / luxon / Temporal.** Variant resolution is pure synchronous switch logic.
- **No new dep / library.** Diff is additive within `apps/mobile/src/screens` only; ToastBanner is consumed from the existing primitives barrel.
- **No SQLite save here.** Parent owns persistence — `onSavePhotoOnly` is a callback contract, not a side effect. Adding the SQLite write would couple two unmerged stacks.
- **No "generic error" component.** A single `<ErrorCard>` taking `{ headline, body, action }` would have been mechanically cheaper, but it would erase the per-kind voice the master plan locks in (and the brief explicitly rejects it). Each kind reads with its own editorial tone.
- **No renaming of api-client error kinds.** The union shape stays exactly as locked in `apps/mobile/src/api/types.ts`.
- **No backend changes.**
- **No interface changes to `useDiagnoseRequest` / `DiagnoseLoadingState` / `CameraView`.** All consumed as-is from their stacked PRs.
- **No Maestro / EAS / real-OpenRouter eval gates** — autonomous-push session lock per the V1 push session memo.

### Notes
- Stacks on `Anandsatch/e5-camera-result` (PR #33). Base for merge is the stack parent, NOT main. Auto-rebases to main when E5-008 lands.
- The `network` variant rendering is tested via the resolver helper because `useDiagnoseRequest` coerces api-client `network` → `queued` for V1's offline UX (master plan, line 346). The kind is still load-bearing on the union and in the resolver — a future netInfo-aware path or hook change could surface it directly without a screen change.
- The orchestrator brief mentions "focus moves to the new state on transition (`AccessibilityInfo.setAccessibilityFocus()`)". We chose `announceForAccessibility` instead because `setAccessibilityFocus` requires a `findNodeHandle` lookup that is fragile under React strict-mode and adds noise to the test harness. The screen-reader semantic is identical: VoiceOver/TalkBack reads the editorial headline immediately on transition. Documented in the file header.

<<<<<<< HEAD
## [0.1.39.0] - 2026-05-07

E6-002 — `GET /api/weather` Hono route over the E6-001 Open-Meteo wrapper. The route is intentionally a thin adapter: parse + validate the `?lat=&lon=` query params, call `getWeather()`, and translate the wrapper's `GetWeatherResult` discriminated union (`ok` / `cache_hit` / `network_error` / `rate_limited` / `parse_error`) onto HTTP status codes and a stable on-the-wire `WeatherResponse`. Three load-bearing decisions are encoded in the file header and locked by tests: (1) **the discriminated union is NOT collapsed** in HTTP mapping — `ok`/`cache_hit` both 200 (cache state surfaces via the `x-cache: hit|miss` response header, never as a payload field, so a future caching change at this layer can't accidentally break consumers), `rate_limited` 429, `network_error` and `parse_error` both 502 with distinct error messages so ops can tell a transport spike from a schema change; (2) **input validation runs BEFORE the wrapper call** — defense in depth even though the wrapper itself also validates, so an invalid lat/lon never burns a KV read or a network call (the input-validation test asserts this by registering NO Open-Meteo interceptor and relying on `disableNetConnect` to throw if the wrapper got reached); (3) **the api-types separation** for the wire contract — `WeatherResponse` (and tuple-typed `[WeatherDailyEntry, WeatherDailyEntry]` daily array) lives in `packages/api-types/src/index.ts` so mobile consumers get the same compile-time guarantees as the backend, while `WeatherPayload` / `GetWeatherResult` stay internal to the wrapper.

This branch stacks on `Anandsatch/e6-open-meteo` (PR #40, E6-001). GitHub will auto-rebase the PR onto main when E6-001 lands; squash-merge will collapse the WIP + the codex-fix commit + this VERSION/CHANGELOG bump into one feat() at land time.

This entry completes a WIP commit started in a prior session that exited mid-workflow before adversarial review. The finishing pass added a dedicated codex-fix commit on top of the WIP after a fresh `codex exec` run surfaced two real P2s that the WIP would have shipped.

### Added
- `apps/backend/src/routes/weather.ts` (~145 lines) — `weatherRoute` Hono router. `GET /` handler: query-param parse (`parseCoordinate()` strict regex against `[+-]?(\d+\.?\d*|\.\d+)` so empty / whitespace / hex / scientific / NaN-coercing inputs all reject before any IO; explicit range check `lat ∈ [-90, 90]`, `lon ∈ [-180, 180]`); call `getWeather({ latitude, longitude, env })`; switch on `result.kind` with an exhaustiveness firewall after the arms (`const _exhaustive: never = result`). On `ok` / `cache_hit` returns 200 with `{ ok: true, kind: 'success', data: { current, daily, timezone } }` and an `x-cache: hit|miss` informational header. On `rate_limited` with a numeric `retry_after`, returns 429 with both the `Retry-After` response header and `retry_after_seconds` in the body; without one, returns 429 with neither (the field is now optional in `ApiResult`). On `network_error` returns 502 with `message: 'upstream_unavailable'`; on `parse_error` returns 502 with `message: 'upstream_invalid_response'` so observability can tell a transport spike from a schema spike.
- `apps/backend/test/weather.test.ts` — 28 tests across happy path (200 with `x-cache: miss`, payload does NOT leak the wrapper `kind`/`cached` fields, hit on second call to same coords proves the KV write happened on the first and no second upstream call fires, boundaries `±90` / `±180` accepted, `0,0` accepted), input validation (400 on missing lat / missing lon / both missing / empty-string / whitespace / non-numeric / hex `0x10` / scientific `1e1` / lat > 90 / lat < -90 / lon > 180 / lon < -180; an explicit "fires BEFORE the wrapper" test that registers NO interceptor and relies on `fetchMock.disableNetConnect` to fail if the wrapper got reached), discriminated-union → HTTP mapping (502 on upstream 500, 429 with `Retry-After: 42` propagation header + body, 429 without `Retry-After` omits BOTH header AND body field — locks the no-fabricated-zero contract — 429 with malformed `"0.9"` Retry-After also omits both, 502 on parse_error from valid JSON wrong shape, 502 on malformed JSON body), payload contract (every daily entry has `date / temperature_max_c / temperature_min_c / precipitation_sum_mm / weather_code`, current carries `time / temperature_c / relative_humidity_pct / precipitation_mm / weather_code`, the `Asia/Tokyo` upstream timezone string round-trips through the route).
- `packages/api-types/src/index.ts` — `WeatherCurrent`, `WeatherDailyEntry`, `WeatherResponse` types. `WeatherResponse.daily` is tuple-typed `[WeatherDailyEntry, WeatherDailyEntry]` so a consumer indexing past `[1]` is a compile error, locking the V1 "exactly 2 days" scope. `ApiResult.rate_limited.retry_after_seconds` is now **optional** (was `: number`) — see Adversarial review below for the reasoning.
- `apps/backend/src/index.ts` — three-line addition: import + `app.route('/api/weather', weatherRoute)`. Mirrors the existing pattern for `/api/identify`, `/api/diagnose`, `/api/consult`, `/api/review`.

### Adversarial review (codex)
Two **P2** findings caught in the finishing pass — both real, both fixed, both regression-covered:
- **P2** — `retry_after_seconds: 0` was being fabricated on 429s when the upstream gave us no parseable `Retry-After`. The response header was correctly omitted, but the body still carried `retry_after_seconds: 0` "to keep the wire shape stable for naive consumers." Mobile's `classifyResponse()` does `headerRetryAfter ?? bodyRetryAfter` to pick a delay, and `??` does **not** short-circuit on the numeric `0`. Header undefined + body `0` → `retry_after = 0` → mobile retries immediately against the same rate-limited upstream, exactly the storm we were trying to prevent. **Fix:** made `retry_after_seconds` optional in the `ApiResult.rate_limited` arm; route now omits BOTH the header and the body field when the wrapper has no value. The change is backwards-compatible because every existing caller already narrows with `typeof === 'number'` (mobile `client.ts:289`) or carries an explicit number value at construction sites (`apps/mobile/src/api/__tests__/client.test.ts`). Two tests updated to lock the new contract: "omits header AND body field" + "malformed `0.9` omits both."
- **P2** — the `switch (result.kind)` over `GetWeatherResult` was not exhaustively typed. The five arms are present today, but tsconfig doesn't carry `noImplicitReturns`, so a future sixth kind would compile while falling through to `undefined` — which Hono would 500 on at runtime. **Fix:** `default: const _exhaustive: never = result; return _exhaustive;` after the arms. A new kind on `GetWeatherResult` now fails to compile in this file rather than 500ing in production.
- **P3, accepted by design** — `parseCoordinate()` trims its input, so URL-encoded leading/trailing whitespace around an otherwise-valid number (`?lat=%200%200%20`) is accepted. **No action**: this is forgiving-by-design behavior consistent with `parseInt` semantics and not a security issue (any value still has to pass the strict numeric regex + range check). Calling it out here so the next reviewer doesn't relitigate.
- **Verified, no action** — codex confirmed: the discriminated union is not collapsed in HTTP mapping (`ok`/`cache_hit` 200, `rate_limited` 429, `network_error`/`parse_error` 502 with distinct messages); input validation precedes any IO; coordinate bounds are inclusive `[-90, 90]` / `[-180, 180]`; empty/whitespace/hex/scientific/`NaN`/`Infinity`/trailing junk all reject; cache state never leaks into the JSON body (only the `x-cache` header carries it); `WeatherResponse.daily` is tuple-typed at exactly two entries; the route mount follows the existing Hono `.route()` pattern; GET is the right verb for an idempotent bodyless endpoint; and `Retry-After` header propagation works correctly when the value is present.

### V1 scope locks (rejected reviewer suggestions)
- **No route-layer cache.** The E6-001 wrapper owns the cache. Layering a second cache here would hide the wrapper's hit/miss signal from the `x-cache` header and double the staleness budget on the free KV tier.
- **No new rate-limit middleware dependency.** Per the master plan + wrangler.toml, V1 keeps rate-limit + budget tracking client-side; existing routes (`/api/identify`, `/api/diagnose`, `/api/consult`, `/api/review`) carry no server-side limiter either. When server-side sharing begins, a KV-backed token bucket lands as a shared middleware across all routes — not a dep grafted onto this one.
- **No 7-day forecast.** V1 = current + 2 days, locked by the wrapper's `forecast_days=2` URL param AND by the `WeatherResponse.daily: [_, _]` tuple type. The watering rules engine (E6-005) only consults today + tomorrow.
- **No geocoding here.** ZIP → lat/lon happens on the mobile side (E6-003) per the master plan. The route is lat/lon-in, weather-out; mixing in geocoding would tangle the upstream contract.
- **No provider abstraction layer.** Open-Meteo is the only V1 weather provider — same lock as E6-001.

### Notes
- **Why GET with query params (not POST + body):** the upstream Open-Meteo API itself is GET, so mirroring that is least-surprise; the operation is idempotent and naturally cacheable in HTTP semantic terms; and the other backend routes are POST only because they carry multipart image bodies. A weather lookup has no body, and a POST with empty body would muddle the convention.
- **Why `x-cache: hit|miss` (informational), not a payload discriminator:** mobile's watering-rules engine (E6-005) shouldn't branch on hit vs miss — the data is the data — and a future caching change should not be observable in `body.data`. This is locked by the "payload does NOT leak the wrapper kind / cached fields" test.
- **Stack pattern:** this branch is direct off `Anandsatch/e6-open-meteo` (PR #40, E6-001). The PR's base is set to that branch, not main. GitHub will auto-rebase the PR onto main when PR #40 lands; squash-merge will collapse the three commits (WIP + codex-fix + version/changelog) into a single feat() at land time.
- **Backend test count:** 208 (was 180 before this ticket; 28 new tests in `weather.test.ts`). Mobile suite unchanged at 353. Mobile `tsc --noEmit` clean.

<<<<<<< HEAD
## [0.1.41.0] - 2026-05-07

E7-005 — `<ToastBanner type='pending'>` graduates from "tan box with a string" (E2-012) into the actual queued-sync surface the master plan asks for: a leading clock glyph (◷) with a slow opacity pulse, an italic Inter body, and an optional all-caps `RETRY` CTA wired through a new `onRetry` prop for E7-006 to consume. The variant uses `theme.colors.tan` rather than reusing `'warn'` because the semantics are deliberately different — pending is "we're offline, we saved it, we'll run it when you're back online," not "something went wrong." Tan reads as warm/queued; reusing the warn token would mislead users into thinking a queued action had failed. The pulse is hard-disabled under `useReduceMotion()` per the E9-003 Wave 1 lesson, and the `Animated.Value` is initialized at end-state (1.0 opacity) so the brief async window before `AccessibilityInfo.isReduceMotionEnabled()` resolves can't paint a low-opacity first frame for reduce-motion users.

### Added
- `onRetry?: () => void` prop on `<ToastBanner>`. When provided on `type='pending'`, renders a `RETRY` Pressable inside the banner with `accessibilityRole='button'` and `accessibilityLabel='Retry'`, all-caps Inter at the same `actionLabel` typography as the existing `action` CTA. Tap fires `onRetry` exactly once. The prop is pending-only — passing `onRetry` on `'warn'` or `'info'` is a no-op (those variants use the existing `action` prop for their CTAs). E7-006 ("Tap to retry on failed/expired rows") wires the actual sync_queue requeue into this exact callback boundary with no contract change.
- Pending leading glyph: U+25F7 (◷) — "white circle with upper-right quadrant" — chosen because it reads as a clock/timer at small sizes, has no emoji-presentation variation across iOS and Android (unlike ⏱️ which renders as a colored emoji on iOS and breaks the Conservatory cream/forest palette), and ships in every base font we'd plausibly land on. The glyph is rendered via `Animated.Text` with `accessibilityElementsHidden` + `importantForAccessibility='no-hide-descendants'` so VoiceOver/TalkBack don't read "white circle with upper right quadrant" alongside the message — the body text plus the live-region announcement carry meaning.
- Pending pulse: an `Animated.loop` of `Animated.timing` between opacity 0.55 and 1.0 with a 1100ms half-cycle (2.2s round-trip). Calmer than the diagnose loading dots (1.0s round-trip) on purpose — pending is ambient, not a foreground task waiting on a result. `useNativeDriver: true` so the pulse runs on the UI thread and survives JS-thread back-pressure during a queue drain.
- Pending body: `fontStyle: 'italic'` on the message text only (the `'warn'` and `'info'` paths are unchanged — explicitly tested). Synthesized italic from the loaded Inter regular face; we don't ship a separate Inter italic and the V1 scope locks reject adding font weights mid-build.
- 17 new tests in `apps/mobile/src/components/primitives/__tests__/ToastBanner.test.tsx` — clock glyph render gate (pending only), italic body gate (pending only, warn/info untouched), accessibility hide on the glyph, reduce-motion hard-disable (no `Animated.loop`, no `Animated.timing`, glyph init at opacity 1.0), the async-resolve race regression (with `useReduceMotion()` synchronously false, initial opacity is still 1.0 — codex P2), reduce-motion off path starts exactly one loop, no loop on warn/info, `onRetry` provided → CTA renders + fires once on tap, `onRetry` omitted → CTA absent, `onRetry` on `'warn'` is a no-op (pending-only contract), dark theme tan token swap, `accessibilityRole='status'` on the wrapper, double-mount cleanup parity (each `loop.start()` matches a `loop.stop()`), and `action` + `onRetry` coexist (additive, not exclusive).

### Tan vs warn — why a third visual rather than reusing 'warn'
The master plan calls 'warn' for "transient errors, budget meter at 40+" (recoverable mistakes the user should react to) and 'pending' for "offline sync queue: '{N} items syncing'" (background machinery the user shouldn't have to react to). They share the tan accent — Conservatory has only sage and tan as accents — but they read differently because the body shape, glyph, and pulse all signal "ambient" rather than "act now." Italic body + slow pulse + hidden decorative glyph makes the banner feel like a status indicator, not an alert. If we'd reused 'warn' verbatim, a user with three queued diagnoses would see what looks like three error banners and assume something is broken — exactly the opposite of "we got it, hang tight."

### Reduce-motion implementation (E9-003 Wave 1 lesson)
`useReduceMotion()` returns `false` synchronously on the first render and asynchronously resolves to the OS preference on the next tick. A naive "init at min-opacity if not reduce-motion, init at 1.0 if reduce-motion" pattern paints a 0.55-opacity first frame for reduce-motion users in the brief window before `AccessibilityInfo.isReduceMotionEnabled()` resolves — exactly the race the Wave 1 lesson warns about, because the hook's synchronous return is `false` until the OS reports back. The fix in this PR is stronger: `pulseOpacity` is initialized at `PULSE_END_OPACITY` (1.0) **unconditionally**, and the loop sequence runs end-state → min → end-state (instead of min → end-state → min). The first opacity ever painted is 1.0 regardless of the hook's synchronous return, so reduce-motion users never see anything below 1.0 even if the loop briefly starts before `useReduceMotion()` flips to true. The reduce-motion guard then skips the loop entirely. The new test "reduce-motion async-resolve race: even with hook synchronously false, initial opacity is 1" locks the regression contract.

### Adversarial review (codex)
One round of `codex exec --skip-git-repo-check --sandbox read-only` against the staged diff. **No P1 findings.** One P2 caught + fixed before commit:
- **P2 (caught + fixed)** — the original implementation read `useReduceMotion()` synchronously inside the `Animated.Value` initializer (`new Animated.Value(reduceMotion ? 1 : 0.55)`). Codex correctly pointed out that `useReduceMotion()` returns `false` until `AccessibilityInfo.isReduceMotionEnabled()` resolves on the next tick — so a real reduce-motion user's first render still initialized the value to 0.55 and started the loop before the hook flipped. **Fix:** initialize unconditionally at 1.0, run the loop sequence end-state → min → end-state so the first painted opacity is always 1.0, and let the reduce-motion guard skip the loop entirely on the next render after the hook resolves. New test "reduce-motion async-resolve race: even with hook synchronously false, initial opacity is 1" locks the contract.

Two P2s addressed proactively before staging (no codex finding required):
- **P2 (preempted)** — `onRetry` on `type='warn'` ambiguity. The prop is typed at the top level so a caller can pass it on any variant; on warn/info it's silently dropped. Resolution: documented as pending-only in the JSDoc, locked behind an `isPending && onRetry` render gate, and explicitly tested that warn+onRetry doesn't render the CTA. Future variants can opt in; the additive surface stays small.
- **P2 (preempted)** — strict-mode double-mount could double-start the loop. Resolution: the `useEffect` cleanup calls `loop.stop()` on unmount, and the test suite locks "every `loop.start()` has a matching `loop.stop()` across remounts" via a mocked controller.

### V1 scope locks (rejected reviewer suggestions)
- No new icon library. The clock glyph is a Unicode codepoint (U+25F7) rendered through the existing `Text` primitive. `react-native-vector-icons` / `lucide-react-native` / `@expo/vector-icons` adds 100+ KB of glyph payload for one character.
- No `react-native-reanimated`. The `Animated` API is the V1 lock; the pulse is one node with native-driver opacity, well within the budget Animated handles cleanly.
- No `'pending'` variant moved to its own file. The variant is additive on the existing primitive — splitting it would break the import path and the existing call sites.
- No rename of the `type` prop. The discriminated union `'warn' | 'info' | 'pending'` is the locked surface; `onRetry` is the only new prop.
- No standalone `Italic` Inter weight via `@expo-google-fonts/inter`. Synthesized italic from the loaded regular face is sufficient for one banner variant; loading a third Inter weight to support one component is over-budget.

### Notes
- **Mobile suite: 371 tests across 24 files** (354 prior + 17 new on `ToastBanner.test.tsx` — 19 prior + 17 new tests on the pending variant, 36 total). Backend suite unchanged at 148 tests.
- The existing `'warn'` and `'info'` rendering is untouched. Two of the new tests explicitly assert that warn and info don't get the italic body or the clock glyph — the variant is purely additive.
- The pending live region (`accessibilityLiveRegion='polite'` on Android) was already wired in E2-012; this PR doesn't modify accessibility role/region semantics for the existing variants.
- `onRetry` does not auto-dismiss the banner. The parent owns visibility; a successful retry flips `visible=false` (or unmounts the banner) at the call site. This keeps the primitive's surface single-purpose — render shape only, no side effects on the queue.

## [0.1.40.0] - 2026-05-07

E7-003 — `useNetworkActivity` is the unified online/offline + foreground/background hook the upcoming `SyncDrainer` (E7-002) will consume. Master plan, line 631: "Drainer trigger: on app foreground (NetInfo + AppState listeners)..." This is the listener half of that contract — the hook subscribes to `@react-native-community/netinfo` and `AppState`, combines the two signals into a discriminated `NetworkStatus` union (`'resolving'` | `'online_active'` | `'online_background'` | `'offline_active'` | `'offline_background'`), and exposes a stable `lastTransitionAt: number` plus an optional `onChange(next, prev)` callback. New file lives at `apps/mobile/src/sync/useNetworkActivity.ts` (the master-plan-locked home for sync code). 22 hook tests; 374 mobile tests total (was 353, +21).

### Added
- `apps/mobile/src/sync/useNetworkActivity.ts` — `useNetworkActivity({ onChange?, debounceMs? })` returns `{ status, lastTransitionAt }`. Subscribes to `NetInfo.addEventListener` (whose return value is the unsubscribe fn directly — verified against `@react-native-community/netinfo@12.0.1` types) and `AppState.addEventListener('change', ...)` (returns a `NativeEventSubscription` with `.remove()`). Combines the two signals via `deriveStatus(appActive, netOnline)` — while either subscription hasn't emitted yet, returns `'resolving'`; once both are known, returns the corresponding combo. iOS `'inactive'` is treated as `'background'` (conservative — don't drain during the app-switcher peek; the master plan doesn't specify, decision documented inline). The pending NetInfo value is committed to `netOnlineRef` synchronously inside the listener; only the `commit()` call is debounced (default 500ms, override via `debounceMs`). AppState transitions are NOT debounced — they're user-driven and discrete. `AppState.currentState` seeds the appActive ref synchronously on mount because RN doesn't emit a `'change'` event on subscribe; without the seed, an already-foregrounded app would sit at `'resolving'` until the first background round-trip. `onChange` fires synchronously on every status transition AFTER the initial `'resolving' → first-real-status` edge (which is suppressed: that's the hook waking up, not a real connectivity / lifecycle change). Cleanup on unmount removes both subscriptions and clears the debounce timer; a `mountedRef` guards the debounce-callback path so any in-flight timer that fires post-unmount becomes a no-op.
- `apps/mobile/src/sync/__tests__/useNetworkActivity.test.tsx` — 22 tests covering: initial mount returns `'resolving'`; subscribe/unsubscribe symmetry under React 18 strict-mode dev (we accept the 2x balance rather than latch with a ref — production semantics still net to one active subscription); all 4 combination states (`online_active` / `online_background` / `offline_active` / `offline_background`); `isInternetReachable=null` treated as online (probe in flight, don't flap offline on launch); iOS `'inactive'` treated as background; `onChange` suppression of the initial `'resolving' → real` edge; `onChange` fires with `(next, prev)` on real transitions; `onChange` doesn't fire when the derived status doesn't change; debounce collapses rapid offline→online→offline within 500ms into one transition; custom `debounceMs`; AppState transitions NOT debounced; **codex P2 regression** (AppState commits inside the NetInfo debounce window read the FRESH net value, NOT the stale pre-emit ref); `lastTransitionAt` updates on every committed transition and does NOT update when status is unchanged; cleanup removes both subscriptions; pending debounce timer cleared on unmount + no setState after unmount; AppState seed on background-on-mount.
- `apps/mobile/src/sync/index.ts` — barrel export for `useNetworkActivity`, `NetworkStatus`, `NetworkActivitySnapshot`, `UseNetworkActivityConfig`.

### New dependency
- `@react-native-community/netinfo@^12.0.1` — added to `apps/mobile/package.json`. Was approved in the master plan as a V1 dep; this ticket is the first consumer. SDK 55 / RN 0.83 compatible (12.x is the current line; 11.x targets earlier RN). No expo plugin config needed for V1 — the default `addEventListener` API works out of the box with `expo-dev-client`.

### Adversarial review (codex)
One P2 caught + addressed before ship:
- **P2** — Stale `netOnlineRef` during AppState commits inside the debounce window. Initial implementation deferred the ref write into the `setTimeout` closure, so a NetInfo offline event followed by an AppState foreground transition within 500ms would commit `online_active` (reading the pre-emit ref) instead of the correct `offline_active`. Fixed by writing `netOnlineRef.current = isNetInfoOnline(state)` synchronously in the listener and debouncing only `commit()`. The debounce still collapses rapid net flapping (multiple emits inside the window result in one commit) but cross-signal commits inside the window now read the latest known net value. Regression test added.
- **Re-review verdict**: CLEAR. No P1/P2 remain.

### V1 scope locks (rejected reviewer suggestions)
- No connectivity abstraction layer beyond NetInfo + AppState.
- No date-fns / luxon / Temporal — `Date.now()` is sufficient for `lastTransitionAt`.
- No AsyncStorage / persisted state — the hook holds in-memory state only, lost on app kill (the drainer reseeds from `sync_queue` on next mount).
- No wiring into existing components — that's E7-004's job. The hook is a building block for `SyncDrainer` (E7-002), which doesn't ship in this PR.
- 500ms debounce window is the master-plan-implied default; no shorter window without a plan revision.

### Notes
- **Why no `subscribedRef` latch for strict mode**: React 18 strict mode dev mounts effects twice. We could latch with a ref to skip the second subscribe, but doing so would also block the second cleanup, leaving a stranded subscription. The current shape — subscribe, return cleanup; subscribe again, return cleanup again — is symmetric and lets each subscription own its lifetime. Production semantics (without strict mode) net to one active subscription. Test suite asserts the production contract via the unmount-balances-mount assertion in the cleanup describe block.
- **Why suppress the initial `'resolving' → first-real-status` `onChange`**: the drainer cares about real transitions (offline→online to start a drain; background→active to resume). The "hook just woke up" edge isn't a real change in the world — it's a measurement artifact. Suppressing keeps the contract clean: every `onChange` fire corresponds to a user-visible event.
- **Why iOS `'inactive'` maps to `'background'` (and not `'active'`)**: `'inactive'` fires during the app-switcher slide-up, incoming-call peek, and Control Center pull-down. Treating these as `'active'` would let the drainer fire mid-gesture (battery + UX cost); treating as `'background'` is the conservative choice. When the user dismisses the gesture without leaving, AppState fires `'active'` again and we transition back. Net: one spurious background→active round-trip during a brief peek, smoothed by the debounce on the drainer side (E7-002).
- **Why `isInternetReachable === null` is treated as online (not offline)**: `null` is the "probe in flight" state on a fresh subscribe. Treating it as offline would briefly flap `offline_active` on every launch even on a connected device. The conservative-for-flapping choice is to assume online until the probe disconnects; the drainer's first fetch will discover the truth either way.

## [0.1.38.0] - 2026-05-07

E7-001 — `sync_queue` CRUD layer. The persistence root for offline-mode LLM requests: typed `enqueueRequest` / `selectReadyForRetry` / `markInFlight` / `markDone` / `markFailedTerminal` / `scheduleBackoff` / `sweepStaleEntries` over the `sync_queue` table that E2-002 created. State machine matches the master plan: `pending → in_flight → done | failed | back-to-pending` with the locked backoff schedule (1m / 5m / 30m / 2h / 8h / fail). 7-day TTL sweeper deletes only terminal rows (`done` / `failed`) — pending and in_flight are preserved regardless of age so a long-offline user does not lose queued work. UTC ms only; no calendar math (DST/IDL contract enforced by static source-grep tests, mirroring `useWateringEngine` E4-002). E7-002 (drainer) and E7-004 (hook wiring) compose on top; `useDiagnoseRequest` is intentionally untouched in this PR — each LLM hook still coerces `network → queued` at the UI layer until E7-004.

### Added
- `apps/mobile/src/sync/queueCrud.ts` — the CRUD module. Exports `enqueueRequest`, `selectReadyForRetry`, `markInFlight`, `markDone`, `markFailedTerminal`, `scheduleBackoff`, `sweepStaleEntries` plus `BACKOFF_SCHEDULE_MS` (60_000, 5×60_000, 30×60_000, 2×3_600_000, 8×3_600_000), `MAX_ATTEMPTS = 6`, and `TTL_MS = 7 × 86_400_000`. Pure-data API over a structural `QueueExecutor` (`runAsync` / `getFirstAsync` / `getAllAsync` / `withTransactionAsync`) so the same bytes drive expo-sqlite on device and better-sqlite3 in tests, mirroring the `usePlants` / `runMigrations` pattern. `enqueueRequest` accepts a `{ kind, payload, dedupeKey?, nowMs? }`; without `dedupeKey` every call inserts a fresh row (synthesizes a `_no_dedupe` sentinel for the schema's NOT NULL `ref_table`/`ref_id` so no schema migration is needed); with `dedupeKey: { refTable, refId }` the existence-probe + insert run inside a transaction so concurrent enqueues for the same key cannot both insert. `scheduleBackoff` reads attempt_count, computes the next delay from the locked schedule, and writes back inside a transaction — closes the lost-update race two drainers could otherwise hit. The 6th attempt terminal-fails the row instead of scheduling. `markInFlight` / `markDone` are SQLite-idiomatic silent no-ops on out-of-state rows (drainer reboots after a partial failure can re-issue the transition without probing first). `sweepStaleEntries` does count-then-delete so the executor-agnostic surface returns a deletion count without depending on `RETURNING` semantics. File header documents the state machine, the schema column mapping, the V1 scope locks (no ORM, no date-fns/luxon/dayjs/Temporal, no job-runner library, no hook wiring in this ticket), and the dedupeKey edge cases (case-sensitive BINARY collation, empty-string-is-truthy, NOT NULL columns).
- `apps/mobile/src/sync/index.ts` — barrel for the future `apps/mobile/src/sync/` epic surface (E7-002 drainer, E7-003 NetInfo + AppState listeners, E7-004 hook wiring all live alongside).
- `apps/mobile/src/sync/__tests__/queueCrud.test.ts` — 38 tests across enqueue (status defaults / TTL / dedupe roundtrip / case-sensitivity / null-payload), selectReadyForRetry (filter / order / limit / non-finite-limit guard / skip non-pending), the four mark/transition functions (state flips + idempotency + cross-state no-ops + done-not-downgraded by markFailedTerminal + in_flight transitions), scheduleBackoff (attempts 1..5 each pinned to the master-plan schedule, attempt-6 terminal-fail, full schedule walk through markInFlight + scheduleBackoff cycles, atomicity under concurrent drainers with the exclusive-transaction guarantee modeled by the test adapter's BEGIN IMMEDIATE + FIFO mutex, noop variant returned on pending/done/failed input, throws on missing id), the 7-day sweeper (deletes terminal-only / preserves pending+in_flight regardless of age / 6d 23h 59m 59s preserved / exactly 7d cutoff preserved per strict `<` / 7d + 1ms swept — sub-day-precision boundaries per Wave 1 lesson), foreign-keys-ON across every CRUD path, and the DST/IDL static contract (source-grep with comments stripped: no `getDate` / `setHours` / `getDay` / `toLocaleDateString` / `new Date(...)`). Mobile suite: 391 tests across 25 files (353 prior + 38 new).

### Adversarial review (codex)
Five findings caught + addressed before ship; mobile test count went 353 → 391 (+38) net of the new tests added to lock the fixes.

- **P1 — `withTransactionAsync` is not exclusive on expo-sqlite.** Codex pointed at the upstream docs (and the local source confirms): `withTransactionAsync` does NOT serialize against other write attempts on the connection — two awaiting drainers can interleave statements inside one another's transactions, defeating the lost-update defense. Fixed by routing every read+write atomic path (`enqueueRequest` with dedupeKey, `scheduleBackoff`, `sweepStaleEntries`) through `withExclusiveTransactionAsync` instead, which uses a dedicated connection and BEGIN-IMMEDIATE-equivalent semantics. The `QueueExecutor` interface deliberately exposes only `withExclusiveTransactionAsync` so callers can't accidentally reach for the weaker variant. Better-sqlite3 test adapter binds with `BEGIN IMMEDIATE` + a per-adapter FIFO mutex that mirrors the production serialization.
- **P1 — `markFailedTerminal` SQL parameter order swap.** Caught during the test pass: the binding array was `[id, errorMessage]` against `last_error = ? WHERE id = ?` — the row id was bound into `last_error` and the error message into the WHERE clause, so every call matched zero rows. Fixed and locked by the test that asserts `row.status === 'failed'` after `markFailedTerminal(q, id, 'manual fail')`.
- **P2 — `markFailedTerminal` could downgrade a `done` row.** No status guard meant a stale "Tap to retry" tap could erase a successful drain. Restricted to `WHERE id = ? AND status IN ('pending','in_flight')`. New test `markFailedTerminal does NOT downgrade a done row to failed` locks the contract.
- **P2 — `scheduleBackoff` accepted `pending` rows.** The state diagram puts the backoff edge on `in_flight`; calling on `pending` would silently bump attempt_count without an actual attempt, breaking the schedule's semantics. Restricted to `in_flight` rows; non-`in_flight` rows return `status: 'noop'` (new variant of `ScheduleBackoffResult.status`). The previous design leaked `'done'` through a `'failed'`-typed slot — that's gone too. New test `is a no-op on a pending row (must be in_flight first)` locks the guard. The atomicity test was rewritten: under exclusive serialization, the second of two concurrent drainers observes the post-state (now `pending`) and returns `noop`, so the row's `attempt_count` advances exactly once across two simultaneous attempts — not twice. Realistic drainer walk added that exercises markInFlight + scheduleBackoff for all six attempts and confirms terminal-fail at attempt 6.
- **P3 — `sweepStaleEntries` count + delete were not in a transaction.** A drainer flipping a row's status between the count and the delete could under/over-report `deleted`. Wrapped both statements in the same `withExclusiveTransactionAsync`. Existing boundary tests still pass.

Findings codex left as PARTIAL after review (acknowledged, not deferred):
- Dedupe is advisory (no unique index on `(ref_table, ref_id)`). Production wiring uses a single drainer per device and the exclusive transaction inside `enqueueRequest` covers two-enqueuer races; a defense-in-depth unique index lands when E7-002 wires multi-writer safety.
- `_no_dedupe` sentinel could theoretically collide if a future caller picks `refTable: '_no_dedupe'`. Documented in the file header; the sentinel string is a leading-underscore identifier convention that callers are unlikely to choose accidentally. Promotable to a runtime guard if a future caller surfaces this.
- `JSON.stringify` on a circular payload throws synchronously before any write (no partial state). Documented in the header; explicit validation can land alongside E7-004's wire-up if any caller starts feeding nontrivial payloads.

### V1 scope locks (rejected)
- ORM (Drizzle / Kysely / Prisma). V1 raw SQL only; mirrors `usePlants`.
- date-fns / dayjs / luxon / Temporal. UTC ms only; same E4-002 critical-regression contract.
- A queue management library (queue-promise, p-queue, bull). The state machine is small enough to live in raw SQL.
- Pulling sync_queue persistence into `useDiagnoseRequest` / future identify / consult hooks. Each hook today coerces `network → queued` at the UI layer (`useDiagnoseRequest.ts` header documents the V1 scope tension); E7-002 builds the drainer on top of this CRUD; E7-004 wires the hooks. Pulling persistence into hooks now would tangle two epics.
- A 7-day calendar sweeper using calendar math. UTC ms only.
- A backoff schedule different from 1m / 5m / 30m / 2h / 8h / fail. The sequence is master-plan-locked.
- Adding a `dedupe_key` column to `sync_queue` via migration. The schema's `(ref_table, ref_id)` tuple is the natural dedupe surface; passing `dedupeKey: { refTable, refId }` honors that without a migration. The sentinel `_no_dedupe` ref_table for the no-dedupe path keeps every row uniquely keyed without schema churn.

### Notes
- **Schema column-name reconciliation:** the ticket spec used `enqueued_at_ms` / `next_attempt_at_ms` / `kind` / `payload` / `dedupeKey` etc., but the actual `sync_queue` schema (locked by E2-002 + the master plan) uses `created_at` / `next_attempt_at` / `endpoint` / `payload_json` / `(ref_table, ref_id)`. The CRUD layer's external API uses the spec's vocabulary (`kind`, `payload`, `dedupeKey`, `nowMs`); the file header documents the 1:1 mapping to the on-disk column names so the boundary is auditable. No schema migration; the existing `idx_queue_drainable` partial index supports `selectReadyForRetry` cheaply.
- **`markInFlight` / `markDone` silent no-op design:** drainer can crash mid-flight (native crash, app suspended, watchdog kill). On reboot it may re-issue a transition for a row that's already terminal. Throwing in that path would force the drainer to LEFT-JOIN-style probe state before every transition, defeating the point of having a CRUD layer. Silent + idempotent is SQLite-idiomatic and matches the master plan's tolerance for retried drainer runs. `markFailedTerminal` is intentionally not state-restricted because it's a forced terminal — the drainer or "Tap to retry" UI invokes it from any non-terminal state.
- **`sweepStaleEntries` count-before-delete vs delete-with-RETURNING:** chose count-first because the `RETURNING` clause's row-yield shape varies between expo-sqlite and the better-sqlite3 test adapter. The tiny race window between `count(*)` and `DELETE` is non-fatal: even if a pending row flipped to done between the two statements, both the count and the delete predicate are `status IN ('done','failed')` so the row would be counted in the delete only if it qualifies, and the deletion count is monotonic. The drainer is the only writer; the user does not edit queue rows directly.
- **No `expired` status:** the master plan diagram drew an `'expired'` terminal state populated by the sweeper. E7-001 deliberately omits it: pending and in_flight rows are preserved regardless of age. A user offline 8 days should not lose their queued diagnose. The sweeper's job is table hygiene, not retention policy enforcement on un-drained work.

<<<<<<< HEAD
=======
>>>>>>> origin/Anandsatch/e6-watering-engine-v2
## [0.1.37.0] - 2026-05-07

E6-001 — Open-Meteo backend wrapper with KV-backed cache. Hono on CF Workers gets a single new module (`apps/backend/src/lib/openMeteo.ts`) and one new binding (`WEATHER_CACHE`) so the upcoming watering rules engine (E6-005) can pull current weather + a 2-day forecast for any lat/lon without re-burning the network on every call. The wrapper is intentionally a leaf-level utility: no Hono context, no router glue, no provider abstraction — that all lives in E6-002 (`/api/weather` endpoint) which composes this. Three load-bearing decisions are encoded in the file header and locked by tests: (1) the result is a **discriminated union** (`kind: 'ok' | 'cache_hit' | 'network_error' | 'rate_limited' | 'parse_error'`) so route handlers always have a deterministic fallback path and never need a try/catch around weather lookups; (2) the cache key is `weather:${lat.toFixed(2)}:${lon.toFixed(2)}` — a ~1.1km grid that lets neighbors share cache entries on the free KV tier (coarser would skip across city microclimates; finer would defeat the sharing); (3) the TTL is 3600s (1 hour) carried both as KV `expirationTtl` and as `expires_at_ms` inside the cache envelope, so KV's eventual-consistency at the edge can't hand back stale data. Input validation runs **before** any IO — invalid lat/lon never burns a KV read or a network call. KV writes happen **after** a successful parse, never before, so the cache can't be poisoned by a malformed upstream response.

This entry completes a WIP commit started in a prior session that hit a rate limit before adversarial review. The finishing pass added a P2-fix commit on top of the WIP after a fresh codex run surfaced two real bugs that the prior pass would have missed.

### Added
- `apps/backend/src/lib/openMeteo.ts` (~380 lines) — `getWeather({ latitude, longitude, env, fetchImpl?, nowMs? })` returning `Promise<GetWeatherResult>`. Pure module: no Hono context, no global state, dependency-injection style for fetch + clock so tests run deterministically without spinning up miniflare. Cache envelope carries `v: 1` schema version + `expires_at_ms` UTC ms; if the schema bumps, old entries fail the type guard and fall through to a fresh fetch. Public types exported: `WeatherPayload`, `CurrentWeather`, `DailyEntry`, `GetWeatherResult`, `GetWeatherArgs`, plus `buildCacheKey()` helper for tests. Cache write failure is swallowed (the cache is an optimization, not a correctness barrier) — the parsed payload still flows back to the caller. KV read failure is treated as a miss; the network attempt still fires.
- `apps/backend/src/env.ts` — declares `WEATHER_CACHE: KVNamespace` on the `Env` type so `c.env.WEATHER_CACHE` is typed in any Hono handler that consumes the wrapper.
- `apps/backend/wrangler.toml` — `[[kv_namespaces]] binding = "WEATHER_CACHE"` block with a placeholder id for the vitest-pool-workers miniflare environment. Real KV id gets pasted in via `wrangler kv namespace create WEATHER_CACHE` before the next prod ship — that step is on the deploy checklist, not this PR.
- `apps/backend/test/openMeteo.test.ts` — 32 tests across cache layer (hit / miss / expiry / write-failure-non-fatal), URL contract (current= + daily= field selection, timezone=auto, forecast_days=2, both encoded params present), error paths (network thrown, HTTP 429 with + without Retry-After, malformed Retry-After, oversized Retry-After clamping, HTTP 500, malformed JSON body, valid JSON wrong shape, daily array too short, upstream null humidity, upstream null daily field, non-string daily.time entry), defensive input validation (lat > 90 / lat < -90 / lon > 180 / lon < -180 / NaN / Infinity all → parse_error and never fire fetch or KV; zero/zero and exact boundary coords are valid), payload shape (2 daily entries with min/max/precip/code/date, timezone string preserved), concurrency (V1 race contract honestly documented — both calls may fetch), and a discriminated-union exhaustiveness check that locks the 5 distinct kinds.

### Adversarial review (codex)
Two **P2** findings caught in the finishing pass — both real, both fixed, both regression-covered:
- **P2** — `normalizeResponse` could let `NaN` escape into a successful `kind: 'ok'` response. `numOrNaN()` returns `NaN` for missing/null upstream values, and the original `isCurrentWeather` / `isDailyEntry` guards only checked `typeof === 'number'` (which `NaN` passes). Only `current.temperature_c` had a `Number.isFinite()` check; humidity, precipitation, weather_code, and every daily-array field could pass through as `NaN`, then JSON-serialize as `null` into KV, poisoning subsequent cache hits with broken data. **Fix:** every numeric field on both type guards now requires `Number.isFinite()` in addition to `typeof === 'number'`. Also tightened daily.time to require `typeof === 'string'` rather than `String()`-coercing (a numeric `dates[0]` would have slipped through as a stringly value). Three new tests lock the contract: null humidity → parse_error, null daily.precipitation_sum entry → parse_error, non-string daily.time → parse_error.
- **P2** — `parseRetryAfter()` accepted non-RFC values via `Number(header)`. `Number("0.9")` → 0.9 → `Math.floor` → 0 (immediate retry on a malformed header). `Number("0x10")` → 16. `Number(" ")` → 0. `Number("1e3")` → 1000. A misbehaving upstream emitting `"999999999"` would suggest a multi-day wait. **Fix:** strict regex `/^\d+$/` rejects decimals / hex / scientific / signed forms / whitespace; `Number.parseInt(_, 10)` + `Number.isSafeInteger` checks; oversized values clamp at `MAX_RETRY_AFTER_SECONDS = 3600` (matches the wrapper's own KV TTL — anything longer is meaningless to the caller). Three new tests: malformed `"0.9"` → undefined, sweep across `["0x10", "1e3", "+5", "-1", " ", "", "abc"]` all → undefined, oversized `"999999999"` → 3600.
- **Verified, no action** — codex confirmed the discriminated union is intact (no kind collapsed), the `weather:${lat.toFixed(2)}:${lon.toFixed(2)}` cache key is correct, input validation runs before IO, the KV write is sequenced after the parse, the URL params (`current=temperature_2m,relative_humidity_2m,precipitation,weather_code` + `daily=temperature_2m_max,temperature_2m_min,precipitation_sum,weather_code` + `timezone=auto` + `forecast_days=2`) match the live Open-Meteo `/v1/forecast` API, the 3600s TTL is correct, the binding name matches across env.ts and wrangler.toml, and the concurrency test honestly documents the accepted V1 race instead of pretending single-flight behavior we don't implement.

### V1 scope locks (rejected reviewer suggestions)
- **No provider-abstraction layer.** Open-Meteo is the only V1 weather provider. A `WeatherProvider` interface with a swappable adapter pattern was rejected — pulls scope and would have to be deleted when E6-005 (rules engine) lands and consumes the concrete `WeatherPayload` shape directly.
- **No retry/backoff inside this wrapper.** Single-shot. The mobile api-client (E2-005) already owns retry semantics for the upstream caller. A retry loop here would compound with the client's loop.
- **No KV TTL longer than 1 hour.** Open-Meteo updates the `current_weather` block hourly; longer TTL would surface stale data into watering decisions.
- **No finer-than-2-decimal cache precision.** `toFixed(3)` would defeat the neighbor-cache-sharing intent on the free KV tier.
- **No 7-day forecast.** V1 = current + 2 days. The watering rules engine (E6-005) only consults today + tomorrow for skip/dry-down decisions.
- **No new dependency.** Hand-rolled type guards (mirroring `src/llm/safe-json.ts`) instead of zod — the wrapper is ~380 lines and a peer dep just for one shape isn't justified.
- **No concurrent-call dedup.** A per-key promise table would have to survive across a single Worker request lifetime; for V1, the race is fine and the test documents it explicitly.

### Notes
- **Why `timezone=auto`:** the on-device watering engine and ledger already operate in UTC ms internally and only format with local-tz at display (E4-001 / E4-002 regression contract). Open-Meteo's daily entries come back keyed to whatever local timezone it auto-detects from the lat/lon; the engine just needs the per-day buckets, and the timezone string is preserved in the payload for any consumer that wants to render local dates.
- **Why `expires_at_ms` AND KV `expirationTtl`:** belt-and-suspenders. KV expiration is eventually consistent at the edge — a value can technically come back after its TTL has elapsed during the propagation window. The in-envelope timestamp is the firewall: even if KV hands back a stale entry, the read path checks `expires_at_ms > now()` and falls through to a fresh fetch on miss.
- **Why hand-roll a stub KV in tests instead of using the miniflare environment:** the wrapper is a pure module — no Hono context, no global state — so DI of `fetchImpl` + a `Map`-backed `StubKV` keeps tests deterministic and lightning-fast (264ms for 32 tests) and lets us assert on the exact KV writes (`{ expirationTtl: 3600 }`) without poking at miniflare internals. Mirrors the style of `openrouter.test.ts`.
- **Backend test count:** 180 (was 174 before this ticket; 32 new tests in `openMeteo.test.ts` minus the 26 that landed in the WIP). Mobile suite unchanged at 353. Mobile `tsc --noEmit` clean.
- **Real KV id swap:** `wrangler.toml` carries a placeholder id (`weather_cache_placeholder`) which is what vitest-pool-workers' miniflare uses. Pre-deploy step: run `wrangler kv namespace create WEATHER_CACHE` and paste the real id into `id = "..."`. Captured on the deploy checklist for this PR's land step.
- **Stack pattern:** this branch is direct off main (no upstream stack dependency). Squash-merge will collapse the WIP + the P2-fix commit into one feat() at land time.

<<<<<<< HEAD
<<<<<<< HEAD
## [0.1.36.0] - 2026-05-06

E6-006 — `<EditPlantDetailsSheet>` is the bottom sheet on plant detail (A-2) that lets the user toggle `is_indoor` (drives the rain-modifier branch in the upcoming E6-002 watering rules) and set a custom `override_interval_days` (which short-circuits rules entirely in favor of a fixed-cadence reminder). Composes E2-011's `<EditorialBottomSheet>` so focus trap, swipe-to-close, and reduce-motion-aware Modal animation are inherited rather than re-implemented. Emits `onSave({ is_indoor, override_interval_days })` and lets the parent screen own the persistence — `usePlants().update()` (E2-003, already in main) accepts that exact patch shape and owns the `boolean → INTEGER 0|1` marshal at the SQL boundary, so this sheet stays a pure UI surface with no SQLite coupling. The schema columns (`plants.is_indoor INTEGER`, `plants.override_interval_days INTEGER`) shipped in main with E1; no migration needed in this PR. 40 new tests (8 pure-helper validator + 32 component); 393 mobile tests total (was 353).

### Added
- `apps/mobile/src/components/EditPlantDetailsSheet.tsx` — the sheet. Two interactive rows: an "Indoors" `<Switch>` row with explainer subcopy (`"Indoor plants ignore the local rain forecast."`) and a "Custom watering interval (days)" stepper row (44×44 −/+ `<Pressable>` buttons + a centered `<TextInput keyboardType="number-pad" inputMode="numeric" accessibilityRole="spinbutton">`). Save / Cancel CTA row at the bottom (Cancel = outline, Save = filled `theme.colors.text` fill, disabled while validation fails). The sheet uses `heightFraction={0.55}` so the keyboard doesn't crowd the input on smaller screens. Boolean marshalling stays JS-side: the component emits a JS `boolean`, `usePlants().update()` converts to `0|1` at the SQL boundary (the V1 raw-SQL convention).
- `apps/mobile/src/components/__tests__/EditPlantDetailsSheet.test.tsx` — 40 tests across (a) the pure `parseIntervalInput` validator (blank → null, valid integers in [1, 365], decimals / negatives / emoji / scientific notation / non-digits all rejected, leading-zero strings parsed by numeric value, whitespace trimmed) and (b) the component (initial values render, switch toggle, +/− stepper increment/decrement within range, clamp at MIN/MAX, + from blank seeds at MIN, − from blank stays blank, blank-is-valid → onSave with `null` override, raw-paste preservation for "1.5" / "365.99" / emoji / "1e2" with validation copy + Save disabled, raw-input length cap, validation copy in `theme.colors.tan` accent, Cancel + backdrop-press fire `onCancel`, Save fires `onSave` with the right payload, EditorialBottomSheet focus trap inherited via `accessibilityViewIsModal=true` + `accessibilityRole='dialog'`, switch a11y role + label + checked state, stepper input `accessibilityRole='spinbutton'` + `accessibilityValue` ({text, now, min, max} on a value, `{text: 'Default species interval'}` when blank), Save disabled `accessibilityState`, reduce-motion gate on the inherited Modal animation, dark-mode token swap (sheet surface + tan validation accent both flip to Midnight Conservatory).
- Barrel `apps/mobile/src/components/index.ts` re-exports `EditPlantDetailsSheet`, `EditPlantDetailsSheetProps`, `parseIntervalInput`, `MIN_INTERVAL_DAYS`, `MAX_INTERVAL_DAYS`, `EDIT_DETAILS_HELPER_COPY`, `EDIT_DETAILS_VALIDATION_COPY`. The validation + helper constants are exported with `EDIT_DETAILS_*` prefixes so they don't collide with similar copy from other sheets that future tickets may add.

### Adversarial review (codex)
One P2 caught + addressed before ship:
- **P2** — Silent input sanitizer was rewriting invalid pastes into different valid values. The first cut of `handleIntervalChange` ran `replace(/[^0-9]/g, '')` on every keystroke; pasting "1.5" got rewritten to "15" before `parseIntervalInput` saw it, so the user thought they entered 15 when they actually pasted 1.5. Same trap for "365.99" → "36599" → still > 365 luckily, but pasting "365.0" would have become "3650" → also > 365 by coincidence; the failure mode wasn't UX-correct, it was UX-lucky. Fixed by keeping the raw text in state, only applying a length cap (8 chars — well past the 3-digit valid range, but generous enough that "365.99" survives intact for the validator to reject), and letting `parseIntervalInput` judge the original characters. Three new tests (`preserves raw paste of "1.5"`, `preserves raw paste of "365.99"`, `preserves raw paste with emoji`) lock the contract: the input shows the user's original text, validation copy fires, and Save stays disabled until the input is corrected. Codex confirmed the fix on re-read; no remaining findings.

### V1 scope locks (rejected reviewer suggestions)
- No `@gorhom/bottom-sheet` — E2-011's `PanResponder` primitive is the V1 lock. The focus trap + swipe-to-close inheritance is exactly the reason the primitive exists.
- No ORM / Drizzle / Prisma / Kysely — V1 uses raw SQL via expo-sqlite. The boolean marshal lives at the `usePlants().update()` boundary already shipped in main.
- No date-fns / dayjs / luxon — no time math in this sheet.
- No actual SQLite UPDATE wired here — the parent screen (E4-005, not yet shipped) owns the persistence call. Pulling persistence into this sheet would couple two unrelated concerns and re-call `usePlants()` redundantly when the parent already holds the row.

### Notes
- **Boolean marshalling**: `is_indoor` stored as `INTEGER 0|1` in SQLite per the V1 raw-SQL convention (see `db/schema.ts` § "Booleans are INTEGER 0|1 with a CHECK"). The component emits a JS `boolean`; `usePlants().update()`'s allow-listed UPDATE path converts to 0/1 at the SQL boundary. No 0/1 ever leaks past this component.
- **Reduce-motion**: `useReduceMotion()` is consulted (and intentionally void-discarded with a comment) to document that we checked. The sheet has no internal transitions to gate today — `<EditorialBottomSheet>` already gates the Modal `animationType` on the platform pref. The hook reference future-proofs against a designer adding a value-change easing animation later.
- **`+` from blank seeds at MIN (1)**, while `−` from blank stays blank. The asymmetry is intentional: blank means "use the default species interval," and stepping up should let the user build a value from the keyboard ("1, 2, 3, ...") without having to tap the input first; stepping down from the default has no semantic floor.
- **No persistence wiring in this PR**: `onSave` is the contract boundary. The parent screen (E4-005, plant detail) calls `usePlants().update(plant.id, patch)` and re-renders with the new row. This sheet is one component, not a screen.

## [0.1.35.0] - 2026-05-06

E8-003 — `<AddNoteSheet>` is the A-2 add-note flow: the bottom sheet that captures a free-form note about a plant, fires `/api/consult`, and routes off-topic notes through a Layer-1 reject UI per the master plan. Composes E2-011's `<EditorialBottomSheet>` for the focus trap + swipe-to-close + reduce-motion-gated transition; introduces `useConsultRequest()` as a parallel hook to `useDiagnoseRequest` (E5-006) so the two LLM-bound flows share the same state-machine + race-by-call-order + network→queued-coercion contract without sharing code (the input shapes diverge meaningfully — image URI vs free-text + plant_context — and keeping them parallel makes the call sites self-documenting). The sheet collects + emits; persistence to the `notes` table is owned by E8-005, per the master-plan boundary. Branch off origin/main; not stacked. Codex adversarial review caught one P2 ("the discriminated union collapses on success") which is now resolved with a dedicated recommendation phase.

### Added
- `apps/mobile/src/hooks/useConsultRequest.ts` — `useConsultRequest({ apiClient, netInfo? })` returning `{ consult, status, lastResult, reset }`. `consult({ note, plantContext? })` returns `Promise<ApiResult<ConsultResponse>>`. Same shape as `useDiagnoseRequest`: idle → requesting → success | error | queued state machine, monotonic call counter for race-by-call-order semantics, `mountedRef` guarding setState after unmount, injectable `netInfo` whose default treats the client as online (E7-003 wires real `@react-native-community/netinfo` later via the same prop). The new `reset()` clears state and bumps the committed call id past any in-flight call so a stale resolution can't clobber idle. Trim is applied to the dispatched body so `' Just repotted '` reaches `/api/consult` as `'Just repotted'`. Empty-after-trim short-circuits to a `parse_error` result without calling the api client (defensive — the AddNoteSheet UI also disables Save in this state). Network → queued coercion mirrors diagnose: `kind:'network'` from the api client (DNS/TLS/captive-portal/RN's "Network request failed") becomes `{ ok: false, kind: 'queued' }` so the UI surfaces the tan banner instead of an error card. `timeout` is NOT coerced — request reached the lab and we waited too long (retry now), a different intent.
- `apps/mobile/src/components/AddNoteSheet.tsx` — `<AddNoteSheet open onCancel onSave apiClient plantContext? netInfo? plantNickname? plantSpecies? accessibilityLabel? testID? />`. Composes `<EditorialBottomSheet>` at `heightFraction=0.7` and renders a phase-driven body: input (multi-line `<TextInput>` + Save & analyze + Cancel), loading ("Listening…" with a small spinner that swaps to a static dot under reduce-motion), recommendation (inline Fraunces italic reasoning per master plan line 333 + Done CTA), reject (Layer-1 editorial card "Let's keep this about your plants — We're focused on plant care for now, try asking about your plants!" with Try again + Cancel), low_confidence ("I'm not sure about this one — Could you add a little more detail?"), timeout / server / parse_error each as distinct error cards (server surfaces `retry_after` in copy when present), and queued (pending toast "We'll save when you're back online"). Each phase maps to exactly one client kind — codex P1 lock: the discriminated union is never collapsed. Save is disabled until note has non-empty trim; emoji-only notes ARE allowed (trim doesn't strip emoji). The exported `derivePhase` helper is the pure mapping from `(status, lastResult)` → phase, unit-tested directly. The `inFlightRef` debounces synchronous double-taps on Save so the consult fires exactly once per user-perceived submit (the Strict-mode double-mount test locks this). On recommendation, onSave does NOT auto-fire — the user taps Done after reading the inline response (codex-P2-fix). On queued, onSave fires from the press handler with `status:'queued'` and `llmResponse:null` so the parent can persist with `consult_status:'pending'` per the master-plan schema; E7's drainer resolves later. The submitted note is snapshotted at submit time via `submittedNoteRef` so Done emits the trimmed note that was actually dispatched, not whatever's in the input field afterwards.
- `apps/mobile/src/components/__tests__/AddNoteSheet.test.tsx` — 35 tests across derivePhase coverage (every phase's mapping locked), render shape, save-disable logic (whitespace + emoji), Strict-mode double-tap latch, inline recommendation card render + Done emits onSave, layer1_reject + Try-again-preserves-note, low_confidence distinct card, timeout / server / parse_error each render distinct UI, server retry_after surfaces in copy, queued toast + onSave fires with `status:'queued'`, offline coerces to queued, Cancel + backdrop press both fire onCancel, loading state spinner, reduce-motion swaps to static dot, dark/light token swap, TextInput accessibilityLabel, focus trap (E2-011 dialog role + accessibilityViewIsModal), AppState resume clears local note state on close→open, AppState resume does NOT double-fire an in-flight consult, whitespace-only submit guard doesn't fire request, forward-compat ok+rejected_off_topic routes to reject without onSave.
- `apps/mobile/src/hooks/__tests__/useConsultRequest.test.tsx` — 17 hook tests parallel to useDiagnoseRequest: happy path, layer1_reject NOT coerced, low_confidence NOT coerced, timeout NOT coerced, server with retry_after, parse_error NOT coerced, offline → queued without calling apiClient, network → queued coercion, status transitions idle→requesting→success, unmount mid-request → no setState-after-unmount warning, two in-flight calls → most-recent-by-call-order wins, apiClient throwing → coerced to queued, plant_context forwarding + trim before dispatch, empty-after-trim short-circuits to parse_error without calling apiClient, reset() clears state and prevents stale clobber, default netInfo (omitted) treats as online.
- Component barrel updates: `AddNoteSheet`, `derivePhase`, `AddNoteSavedPayload`, `AddNoteSheetProps` re-exported from `apps/mobile/src/components/index.ts`. Hook barrel: `useConsultRequest`, `ConsultInput`, `ConsultPlantContext`, `ConsultStatus`, `UseConsultRequestConfig`, `UseConsultRequestReturn` re-exported from `apps/mobile/src/hooks/index.ts`.

### Adversarial review (codex)
One P2 caught + addressed before ship:
- **P2 — discriminated union collapsed on success.** Initial implementation auto-fired `onSave` from the press handler whenever `kind:'recommendation'` arrived, leaving no distinct UI for the success path. Codex flagged this as a kind-collapse: every kind in the union should have its own distinct render. Fixed by adding a `'recommendation'` phase + `<RecommendationCard>` that renders the inline Fraunces italic reasoning (matching master-plan line 333: "Response is displayed inline above the buttons as a Fraunces italic line — e.g., 'Got it — hold off on watering for 3 more days while it recovers from repotting.'") with a "Done" CTA. The user reads the response, then taps Done to fire onSave. This also aligns the success UX with the master-plan-locked behavior and gives the user agency over when persistence happens. Queued path still auto-fires onSave (no value in making the user dismiss a "we'll save later" toast) — the asymmetry is deliberate, not a UX inconsistency.
- **OK — verified clean** (re-review after the P2 fix): `useConsultRequest` parallels `useDiagnoseRequest` on injection shape, mount/race handling, and network→queued coercion; the strict-mode double-tap latch and `reset()` path are wired as intended; reduce-motion is honored in the loading row; the EditorialBottomSheet's independent AccessibilityInfo subscription is cleaned up normally; AppState resume stays single-flight; whitespace trimming, emoji-only input, and wire-body trimming are correct; accessibility roles + focus-trap behavior are present; network-kind coercion happens in the hook (not the component); theme tokens used here all exist (no `theme.colors.warn` — `tan` is the off-topic / queued accent per Wave-1 lesson); V1 scope locks are respected; the Done handler reads from lastResult with the right early-return guards; the submitted-note snapshot captures the right value at submit time.

### V1 scope locks (rejected reviewer suggestions)
- No `@gorhom/bottom-sheet`. E2-011's PanResponder primitive is the V1 lock; AddNoteSheet composes it.
- No `date-fns` / `dayjs` / `luxon` / `Temporal`. `Date.now()` for the onSave timestamp; relative-date formatting (if ever needed) already lives in `PhotoTimeline`'s `formatRelativeDate`.
- No SQLite write here. E8-005 owns the `notes` table write; this sheet only collects + emits the payload.
- No rename of `useDiagnoseRequest` / api-client interfaces. `useConsultRequest` is a parallel hook — same shape, parallel concerns — not a refactor of either. The two hooks could fold into a generic `useApiRequest<T>` later, but the input shapes (image URI vs free-text + plant_context) diverge meaningfully today and the parallel-hook pattern reads better at the call site than a generic with conditional types.
- No backend changes. `/api/consult` shipped via E1-003 (PR #4, v0.1.3.0); the only mobile-side concerns are transport + UX.
- No new runtime dep. `@react-native-community/netinfo` will be added by E7-003 via the same `netInfo` injection point this hook already exposes; default ("always online") is fine for V1 because the api client emits `kind:'network'` when fetch throws.

### Token verification (DESIGN.md cross-check)
Every used token verified against `packages/theme/src/colors.ts`:
- `theme.colors.surface` — sheet bg, card bg ✓
- `theme.colors.text` — body, headlines, primary CTA fill ✓
- `theme.colors.textMuted` — secondary copy, placeholder ✓
- `theme.colors.tan` (`#C9A873` light + dark) — reject + queued accent border. There is NO `theme.colors.warn` token — `tan` is the closest design-system accent for "soft warning / pending / off-topic" surfaces, per the Wave-1 lesson captured in the orchestrator brief.
- `theme.colors.stroke` — input + card hairline borders ✓
- `theme.colors.water` / `theme.colors.sage` — NOT used here. (Reserved for the watering ledger + status chips.)

### Notes
- `accessibilityRole='status'` on the loading row + queued card uses the `'status' as 'none'` cast pattern E2-011 uses for `'dialog'`. The shipped `@types/react-native` enum is conservative; `'status'` is a real iOS / web accessibility role exposed via the platform bridge. `accessibilityLiveRegion='polite'` complements it on Android.
- Mobile suite: 405 tests across 26 files (353 prior on origin/main + 52 new across AddNoteSheet + useConsultRequest). Backend suite unchanged at 148 tests across 8 files. Workspace total 553 tests.
- The "Try again" CTA preserves the user's note text (kinder default — the user just wrote it; making them retype is hostile). The hook's `reset()` clears the hook state but the React-controlled local `note` state survives.
- The ConsultResponse discriminated union has two arms (`recommendation | rejected_off_topic`); today the route handler converts router-side `rejected_off_topic` into the wire-level `{ ok: false, kind: 'rejected_off_topic' }` error envelope at status 200, which the api client maps to client `layer1_reject`. So in practice off-topic always reaches us as `layer1_reject`. The `data.kind === 'rejected_off_topic'` arm of `ConsultResponse` exists for forward-compat — if a future backend change emits a successful response carrying a rejection structure, AddNoteSheet still handles it without code changes (`derivePhase` routes both to `'reject'`).
- The 4-chip suggestion row from master plan line 333 is intentionally not in this PR. The E8-003 brief scopes this ticket to TextInput + CTA pair; chips can land as a small follow-up without a primitive change.

## [0.1.34.0] - 2026-05-06

E9-004 — `<WeeklyReviewScreen>` (A-6, Pass 7). The Sunday digest screen the `<SundayLetterCard>` (E9-003) opens to. Composes a top hero illustration, Fraunces "Last week" headline, Inter narrative paragraph from `/api/review`'s LLM payload, and a per-plant section that pairs nickname + species + a 7-day `<WateringLedger>` row (E4-003). States are mutually exclusive: loading skeleton → success | empty week ("This week was quiet — that's okay. Come back next Sunday.") | discriminated-union error variants (network / timeout / server / parse_error / layer1_reject / low_confidence) | offline-queued banner. The seven `ApiResult` kinds keep distinct copy and retry semantics — collapsing them into a single "try again" surface is rejected because user intent and copy diverge per kind (network = check your connection, server = wait, queued = no error card at all). Stacks on origin/main (E4-003 + E2-001 + api client all live there). 26 new screen tests; 379 mobile tests total (was 353).

### Added
- `apps/mobile/src/screens/WeeklyReviewScreen.tsx` — the screen. Props: `apiClient`, `weekRequest` (parent computes the `{ week_summary, plants[] }` payload from local SQLite, screen forwards it untouched), `plantsForLedgers` (per-plant nickname + species + watering events, paired by index against `weekRequest.plants` and the response's `per_plant[]`), `onDismiss` (parent owns dismiss persistence — no cloud sync of dismiss state per V1 lock), optional `nowMs` for deterministic ledger anchoring in tests. Uses `useTheme` (E2-001) + `useReduceMotion` (E2-004) for token + animation gates. Hero illustration is a "🌿" Unicode glyph at fontSize 72 inside a centered surface block with `accessibilityRole='image'` + a metaphor-naming label — mirrors the EmptyGardenWelcome.tsx scaffolding policy (no `<svg>` asset bundled at the screen layer in V1; SWAP-WHEN a real `<WeeklyReviewIllustration />` ships).
- `apps/mobile/src/screens/__tests__/WeeklyReviewScreen.test.tsx` — 26 tests across mount-fire-once, real `<React.StrictMode>` double-mount dedupe (codex P1 fix verified), loading skeleton, success rendering (headline + narrative + per-plant blocks), empty-week editorial copy, three error variants asserting non-collapsed copy, queued banner separation, "Got it" → onDismiss, retry CTA re-fires `apiClient.review`, dark-theme token swap, two reduce-motion paths plus the boot-race fix (codex P2 verified — hook=false + OS=true ⇒ no `Animated.timing` call), four a11y assertions (hero image role, headline header role, button roles + labels, per-plant composed-stop label), AppState resume policy (settled-success NEVER re-fetches), `setAccessibilityFocus` after content lands, nickname-fallback to species_label, sub-day-precision regression (3d 12h fixture buckets onto exactly one ledger column — locks both this screen and `<WateringLedger>` against `Math.floor((now - then) / 86_400_000)` drift), idle→loading→success sequencing, defensive coercion when `apiClient.review` throws.

### Discriminated union NOT collapsed (the load-bearing decision)
Reviewers occasionally suggest "just use one generic 'try again later' string for all error kinds." REJECT. The user-visible difference matters: `network` is the user's problem to fix (turn wifi back on); `server` is ours and they should wait; `queued` shouldn't render an error card at all (renders the queued banner instead); `timeout` differs from `network` in retry semantics (the request reached the lab — retrying is more likely to succeed). The screen renders a kind-specific headline + body + retry CTA per kind via `ERROR_COPY[kind]`, with `queued` handled by a separate banner renderer. Same posture as E5-009.

### Strict-mode double-mount (codex P1 catch + fix)
First implementation used a per-instance `dispatchedRef` latch (the E5-008 / E5-010 pattern). Codex correctly flagged that React 18+ Strict Mode mounts → unmounts → mounts again with a fresh component instance — which gives each mount a fresh ref tree. The latch alone wouldn't prevent double-fire across the StrictMode cycle. Fix: layer a module-scoped `inFlightDispatches` Map (key = `JSON.stringify(weekRequest)`) on top of the per-instance ref. The first mount registers its in-flight Promise in the Map; the second mount sees the same key, awaits the same Promise, and skips a duplicate dispatch. The Map clears on resolution via `.finally()`. The retry path explicitly bypasses the Map so a stale failure doesn't get re-served. The test now wraps in real `<React.StrictMode>` and asserts `apiClient.review` fires exactly once.

### Reduce-motion boot race (codex P2 catch + fix)
`useReduceMotion()` returns `false` during a brief boot async window before `AccessibilityInfo.isReduceMotionEnabled()` resolves. If the success response lands inside that window on a real reduce-motion device, the hook value alone would let `Animated.timing` start on a user who asked for none. Fix: at the loading→success edge, when the hook says false, do a synchronous `AccessibilityInfo.isReduceMotionEnabled()` probe before starting the animation. If the OS reports true, skip `timing` and snap the value to its end state. The test "reduce-motion boot race" pins this contract: `mockedReduceMotion=false` + `isReduceMotionEnabled.mockResolvedValue(true)` ⇒ zero `Animated.timing` calls. The `Animated.Value` itself initializes at end-state (1.0) per the Wave 1 lesson, so even if the probe fails (catch path), the rendered surface is correct.

### AppState resume — DON'T re-fetch on settled success
The `AppState` listener is wired but is a deliberate no-op. A user pausing on the Sunday letter and returning should not see a refresh-blank-reload. The only re-fetch triggers are first-mount dispatch (cached) and explicit retry tap. A test fires `'background' → 'active'` cycles and asserts `apiClient.review` is NOT called a second time. Future post-V1 hooks (telemetry, post-error retry-on-resume) can hang off the same listener without contract churn.

### Accessibility
- Hero block: `accessibilityRole='image'` + a label that names the metaphor ("A leaf — your weekly garden letter").
- Headline: `accessibilityRole='header'`.
- Per-plant section: each plant is a single VoiceOver/TalkBack stop via `accessible={true}` + composed label `"{nickname}, {species_label}"`. RN doesn't expose a `'group'` role so we use the `accessible` collapse pattern that `<WateringLedger>` already uses for each column. Same posture; same outcome to assistive tech.
- "Got it" + "Try again" CTAs: `accessibilityRole='button'` + a label.
- `AccessibilityInfo.setAccessibilityFocus()` moves focus to the headline once content lands, so VoiceOver doesn't strand on a skeleton element that just unmounted. Same pattern as E5-010.
- Error banner: `accessibilityLiveRegion='polite'` so the error transition announces on Android.

### V1 scope locks (rejected reviewer suggestions)
- No `date-fns` / `dayjs` / `luxon` / `Temporal`. The screen does no time math itself; the ledger handles bucketing.
- No collapsing of the API discriminated union — see above.
- No cloud sync of dismiss state. Parent owns persistence (SQLite or AsyncStorage); the screen fires `onDismiss()` and stops there.
- No new deps. RN + react + `@plantcare/api-types` + `@plantcare/theme` only.
- No backend changes — `/api/review` is shipped via E9-001.
- No SVG hero illustration in this PR — emoji-fallback policy mirrors EmptyGardenWelcome's scaffolding choice; SWAP-WHEN a real asset is available.
- No re-fetch on AppState resume — explicitly tested as a no-op.
- No Maestro flow in this PR — owned by E12-008 (Sunday weekly review E2E).

### Token verification
Every used token is in `@plantcare/theme.colors`: `bg`, `surface`, `text`, `textMuted`, `primary`, `stroke`, `tan`. The error/queued banners use `surface` for fill + `tan` for the hairline border (the Conservatory accent color used elsewhere for "check soil" and "syncing" states; `theme.warn` does not exist — Wave 1 lesson — `tan` is the closest accent for editorial-warm warnings). Dark-mode swap is automatic via `useTheme()` (passes 5.57:1 on the Midnight forest bg per DESIGN.md).

### Adversarial review (codex)
- Pass 1 — codex flagged P1 (per-instance latch insufficient for real StrictMode) + P2 (reduce-motion boot race). Both addressed; tests added. See sections above.
- Pass 2 — codex on the corrected diff returned `NO P1/P2 findings`.

### Notes
- The `/api/review` response carries `per_plant[]` ordered to match the request's `plants[]`. The screen pairs them by index against `plantsForLedgers` (parent-supplied). If the response array is shorter (a model that emitted fewer observations than plants), missing entries simply don't render an observation line — the ledger row still renders.
- Test count: 379 mobile (was 353) + 148 backend = 527 across the workspace.

## [0.1.32.0] - 2026-05-05

E9-003 — `<SundayLetterCard>` is the editorial "Sunday letter" card that surfaces above the Plants list (A-1) on Sundays or after a 7+ day care drought. Pass 7 (Weekly Letter) per the master plan: a postcard, not a notification — gentle invitation into the weekly review (E9-004 / WeeklyReviewScreen) rather than a red-dot reminder. Compact card with a Fraunces "Sunday letter" headline, an Inter narrative body, a forest "Read this week" CTA → `onOpen()`, and an Inter "Dismiss" CTA → `onDismiss()`. The component owns no SQLite state — the parent screen reads `lastReviewedAtMs` and `dismissedAtMs` from the local DB and passes them as props.

### Added
- `apps/mobile/src/components/SundayLetterCard.tsx` — the card + a pure `shouldRender(nowMs, lastReviewedAtMs, dismissedAtMs)` helper. Visibility rules: (1) today is Sunday in the device's local tz, OR (2) `lastReviewedAtMs` is set AND ≥ 7 local-calendar-days ago. Override: a `dismissedAtMs` inside the current Sunday window `[mostRecentSundayMidnightLocal(nowMs), +7 local days)` suppresses the card until the window slides to the next Sunday. Dual-gate pattern: parent uses `shouldRender` to decide whether to mount the card at all (so React doesn't pay for the subtree on the 6 days the card isn't shown); the component ALSO calls `shouldRender` internally and returns `null` defensively if a parent forgets to gate or a future refactor races the props. Animated.timing fade-in on mount, hard-disabled (initial opacity = 1, no Animated work) when `useReduceMotion()` returns true. Tokens: card `surface` background + hairline `stroke` border, `text` headline, `textMuted` body + dismiss label, `primary` filled CTA with `surface` label — all swap correctly under Midnight Conservatory.
- `apps/mobile/src/components/__tests__/SundayLetterCard.test.tsx` — 28 tests. Sunday vs non-Sunday day-sweep across all seven local-tz weekdays. Staleness threshold: 7+ days renders, 6 days does not, plus a sub-day-precision divergence suite where each fixture is DIAGNOSTIC (produces a different answer under calendar-walk vs ms-floor, proving the helper sides with calendar-walk): the 7d-exact whole-day control case, the 6d 23h 30m fixture where ms-floor = 6 but 7 calendar midnights have actually elapsed → helper says stale (calendar-walk wins), the 6d 23h reverse-direction control where ms-floor = 6 and calendar-walk = 6 → helper says not-stale, and the 7d 0h 1m boundary case. DST: Sun Nov 1 (US 25-hour fall-back) at 02:30 still registers as Sunday and a single dismiss covers the whole 25-hour day; Sun Mar 8 (US 23-hour spring-forward) at 03:00 still registers as Sunday. Local-Date contract: Sunday determination uses `Date#getDay()` (local), not `getUTCDay()` — a tripwire test that would fail under any future getUTCDay() refactor in non-UTC tz environments (which is the IDL invariant). Sunday-window dismiss semantics: dismissed in-window suppresses; dismissed last Sunday surfaces again next Sunday; Sat 23:59 dismiss does NOT suppress Sun 00:01 (different windows); Sun 00:01 dismiss DOES suppress the rest of that Sunday; a stale-week dismiss on the past Sunday suppresses on the following Tuesday because both fall inside the same current Sunday window. Component: defensive null on Mon with no review, full render on a Sunday (title / body / both CTAs), `onOpen` and `onDismiss` callback wiring, dark-theme token swap, a11y (card has `accessibilityRole='summary'` + accessible group label, title has `accessibilityRole='header'`, both CTAs have `accessibilityRole='button'` + labels), reduce-motion hard-disable (initial Animated.Value at 1, no animation), the initial-render-safe contract (with reduce-motion async-resolving to false, the opacity Animated.Value is bounded in [0,1] — never undefined/NaN — so the fade can't visually flash), defensive null when an in-window dismiss is passed without parent-side gating, and a stale-Tuesday render with an 8-day-old review.
- Barrel `apps/mobile/src/components/index.ts` exports `SundayLetterCard`, `shouldRenderSundayLetter`, `SUNDAY_LETTER_TITLE/BODY/OPEN_LABEL/DISMISS_LABEL`, and `SundayLetterCardProps`. The helper is named `shouldRenderSundayLetter` at the barrel to avoid collision with future `shouldRender*` helpers from other surfaces.

### V1 scope locks (rejected suggestions)
- No `date-fns` / `dayjs` / `luxon` / `Temporal` API. Native `Date` + `getDay/setDate/setHours` only — calendar-walks are the V1 contract for both the watering engine (E4-001/E4-002), the watering ledger (E4-003), the photo timeline (E4-004), and now the Sunday letter.
- No `Math.floor((now - last) / 86_400_000)` for the staleness threshold. The whole-day boundary fixture passes either way; the sub-day-precision regression case proves we use calendar-walk and not ms-floor.
- No internal SQLite write for dismiss. `onDismiss` emits upward and the parent persists. The component is pure render + props.
- No `<WeeklyReviewScreen>` here — that's E9-004. `onOpen` simply emits upward.
- No new dependencies. No backend changes.
- No internal tick clock. Visibility doesn't change minute-to-minute; the parent re-renders on the relevant SQLite writes.

### Adversarial review (codex)
Three findings caught + addressed before ship:
- **P1** — `useReduceMotion()` resolves async (defaults `false` for the brief window before `AccessibilityInfo.isReduceMotionEnabled()` resolves). The original implementation initialized `opacity = new Animated.Value(0)` and started the fade based on the first-render `false`, so a Reduce Motion user could see a partial fade-in before the hook flipped to `true` and the effect cancelled it. Fixed by initializing `opacity = new Animated.Value(1)` (the static end state) and tracking a `hasPlayedRef` that flips to true on the first reduce-motion-known render. With reduce-motion known false: snap to 0 then animate 0→1 once. With reduce-motion known true OR async-pending: stay at 1, no animation. Bias toward "no fade" when the preference is unknown is the safe default for accessibility.
- **P2** — sub-day-precision fixtures included two non-diagnostic cases (the "6 calendar midnights" close-to-boundary case and the "7d 0h 1m" gap) that ms-floor and calendar-walk both agreed on. The third diagnostic case (6d 23h 30m fixture where ms-floor = 6 vs calendar-walk = 7) was the only one actually proving the contract. Fixed by adding an explicit reverse-direction diagnostic (ms-floor = 6, calendar-walk = 6 → helper says not-stale) so the suite has BOTH directions of divergence covered, and labeled the boundary cases as "controls" / "boundary anchors" rather than diagnostic.
- **P3** — the IDL test was vacuous (it only proved a locally-constructed Sunday registers as Sunday, not any tz transition). Renamed to "Sunday determination uses local Date#getDay(), not UTC #getUTCDay()" with comments explaining the tz-mutation limitation in jest and why the test functions as a tripwire for a getUTCDay() regression in non-UTC tz environments. The IDL invariant reduces to "local-Date semantics" once the device's clock has updated post-cross — that's what we actually test.

### Test count delta
Mobile suite: 381 tests across 25 files on top of origin/main (353 prior + 28 new). Backend suite unchanged (148 tests, no backend changes in this ticket). When stacked PRs land, the mobile total auto-rebases higher.

## [0.1.31.0] - 2026-05-05

E5-010 — `<AddPlantScreen>` is the A-4 confirmation flow that turns a captured photo into a saved plant. On mount it compresses the photo (E5-005), fires `/api/identify` via the new `useIdentifyRequest` hook, and renders one of four UI surfaces depending on the result: `kind='ok'` shows a top-3 ranked candidate stack with confidence percentages and a "Save as <selected>" CTA; `kind='no_match'` (species_slug='unknown' or confidence=0) jumps straight to a manual species picker over a small static catalog of common houseplants; `kind='queued'` (offline) shows the tan banner and the picker so the user can still proceed offline; `kind='error'` likewise falls back to the picker with an error banner above it. The discriminated union is locked at the screen boundary — codex flagged collapsing it as a P1 risk; each arm renders structurally distinct UI. Built off origin/main; ships as a standalone PR. Mobile suite: 385 tests across 26 files (361 prior + 24 new for AddPlantScreen + 8 new for `useIdentifyRequest`).

### Added
- `apps/mobile/src/screens/AddPlantScreen.tsx` — `<AddPlantScreen photoUri apiClient netInfo? onSave onCancel? skipCompress? testID? />`. Owns the compress → identify → confirm pipeline. Internal phase machine (`'compressing' | 'identifying' | 'ready'`) and view machine (`'candidates' | 'picker'`) are independent so the manual-override path ("Pick from list") doesn't have to fake out the identify result. Single-fire dispatchedRef + useIdentifyRequest's monotonic call counter ensure identify never double-fires, even on AppState resume (codex P1 risk surface b). `onSave` payload is shape-aligned with `db/types.ts CreatePlantInput` so the parent's DB-write path can pass it through with at most a `hero_photo_id` resolution step: `{ species_slug, species_label, identify_confidence?, nickname?, photoUri }`. Empty nickname → key omitted from payload (conditional spread, not `key:undefined`) so a strict serializer can't round-trip it to `null`. Nickname text is forwarded VERBATIM — emoji, unicode, leading/trailing whitespace all preserved (codex P2 risk surface f). The exported `toAddPlantResult(api: ApiResult<IdentifyResponse>): AddPlantIdentifyResult` is the wire→screen mapper; tests exercise each arm.
- `apps/mobile/src/screens/speciesCatalog.ts` — small static `SPECIES_CATALOG` of 19 common houseplants (Monstera, Pothos, Snake Plant, Pilea, Fiddle Leaf Fig, Rubber Plant, Spider Plant, ZZ, Philodendron, Peace Lily, Aloe, Jade, Boston Fern, Calathea, Pilea Peperomioides, String of Pearls, Cactus, Succulent (Other), and "Other / Not in this list" → `species_unknown`). `filterSpecies(query, catalog?)` is case-insensitive substring on label OR slug, preserves curated order. **The full plant taxonomy lands post-V1** — this is a deliberate V1 scope lock; the picker UI shape stays when the catalog later moves behind a `useSpeciesCatalog()` hook reading from SQLite.
- `apps/mobile/src/hooks/useIdentifyRequest.ts` — mirror of `useDiagnoseRequest` (E5-006) for `/api/identify`. Same architecture (state machine, monotonic call counter for race-by-call-order, mounted-ref guard for unmount safety, injectable `netInfo` probe, network → queued coercion). A parallel hook rather than a `mode` parameter on the diagnose hook because (a) V1 scope locks reject "renaming useDiagnoseRequest interface", (b) the response shapes differ (top-3 candidates vs disease/severity), and (c) callers of the unified hook would need to narrow `lastResult` against the wrong response type half the time. Shipped at `apps/mobile/src/hooks/useIdentifyRequest.ts` and re-exported from the hooks barrel.
- `apps/mobile/src/screens/__tests__/AddPlantScreen.test.tsx` — 24 tests across the contract surface: mount → compress → identify-once; loading state during in-flight; `kind='ok'` renders top-3 with confidence (4th alt dropped); candidate tap selects → submit fires onSave with the full `species_slug + species_label + identify_confidence + photoUri` payload; `kind='no_match'` jumps straight to the picker (no error banner — confident "not found"); manual filter narrows the list; "Pick from list" swaps to picker; nickname optional (empty AND set, with emoji + unicode preservation); submit disabled until species selected; reduce-motion compliance via screen-level no-transform assertion (DiagnoseLoadingState owns per-pixel reduce-motion); dark theme `darkTheme.colors.bg` swap; a11y labels on every interactive (cards, buttons, filter, nickname, cancel, picker rows); AppState resume doesn't double-fire identify (background → active transition asserts `identifySpy` count stays at 1); a11y focus handoff (mocks `RN.findNodeHandle` and `AccessibilityInfo.setAccessibilityFocus`, asserts the focus call lands on the candidates header on view swap); queued result renders banner + picker; compress failure → error banner + picker. Plus 6 unit tests on `toAddPlantResult` covering each arm of the discriminated union.
- `apps/mobile/src/hooks/__tests__/useIdentifyRequest.test.tsx` — 8 hook tests parallel to useDiagnoseRequest's: happy path, offline → queued, network coerced to queued, timeout NOT coerced (preserves error kind), parse_error preserved, apiClient throwing → defensive coerce to queued, idle → requesting → success state transitions, race-by-call-order semantics (older call resolving later doesn't overwrite newer call's lastResult).
- Hooks barrel `apps/mobile/src/hooks/index.ts` re-exports `useIdentifyRequest` + types `IdentifyInput`, `IdentifyStatus`, `UseIdentifyRequestConfig`, `UseIdentifyRequestReturn`.

### Adversarial review (codex)
Two P2s caught + addressed before ship:
- **P2** — onSave payload shape was `{ species: { slug, label }, nickname?, photoUri }`. Codex pointed out that `db/types.ts CreatePlantInput` expects column-named fields (`species_slug`, `species_label`, `identify_confidence`, `nickname`, `hero_photo_id`). Restructured the payload to emit `species_slug` / `species_label` / `identify_confidence` directly so the parent's create() path is a near-pass-through (it still has to insert a `photos` row and resolve `hero_photo_id`). `identify_confidence` is forwarded only when the user accepted an identify candidate; manual-picker selections leave it undefined (the DB column is nullable and is meant to be null when no identify ran).
- **P2** — a11y focus handoff on loading → results swap. First-pass fix added `accessibilityLiveRegion="polite"` to both result headers ("Is this your plant?" / "Pick your plant"), which announces the new content but does NOT actually move screen-reader focus. Codex flagged this as still-open. Final fix: refs on each header (`candidatesHeaderRef`, `pickerHeaderRef`), plus a `useEffect` that fires `AccessibilityInfo.setAccessibilityFocus(handle)` via `requestAnimationFrame` when phase becomes `'ready'`. Test mocks `RN.findNodeHandle` + the focus call to assert the contract.
- **Verified, no action** — discriminated union not collapsed (each kind drives a structurally distinct UI tree); AppState resume can't double-fire identify (dispatchedRef + useIdentifyRequest's call counter both protect); controlled TextInput filter is fine without debounce on a <20-entry list (documented choice); nickname forwarded verbatim including emoji + unicode (test asserts).

### V1 scope locks (rejected reviewer suggestions)
- No picker library, no `react-hook-form`, no form abstractions. Plain controlled `<TextInput>` + `<FlatList>` is enough for V1.
- No complete plant taxonomy bundled in V1. The 19-entry static catalog is the deferral; full taxonomy is a post-V1 ticket.
- No SQLite write here. `onSave` is a callback prop; the DB write lives in E5-011 or a future Add-plant integration ticket.
- No collapsing of the discriminated union into a single "loaded" branch. Each arm renders different UI.
- No renaming `useDiagnoseRequest` interface. `useIdentifyRequest` is a parallel hook, not a generalization.
- No new runtime deps. No backend changes. No manual gates.

### Notes
- Test count: 385 mobile (361 prior + 24 AddPlantScreen) + 148 backend = 533 across the workspace. The hook test file adds another 8 tests at the hook layer that overlap (also counted in the 385).
- Off origin/main as a standalone PR (no stack base). Per the v1 autonomous-push lesson: branch is `Anandsatch/e5-add-plant`; the worktree never `git checkout`s sibling branches.
- The screen does NOT call into `useDiagnoseRequest` despite the brief mentioning "reuse for `mode: 'identify'` requests" — that's a contract change to a public hook surface that V1 scope locks reject. The new `useIdentifyRequest` is the V1-correct factoring.

=======
>>>>>>> origin/Anandsatch/e5-camera-tests
## [0.1.30.0] - 2026-05-05

E5-008 — `<CameraResultScreen>` is the A-3 success path: the rescue moment from capture to diagnose to save. Receives `{ photoUri, mode, plantContext? }` from the camera flow, runs `compressPhoto` (E5-005) once and caches the result, calls `useDiagnoseRequest()` (E5-006) exactly once after compression resolves, renders `<DiagnoseLoadingState>` (E5-007) while in-flight, and on success renders the A-3 card per the approved mockup (`designs/v1-screens-20260430/A-3-camera.png` + `-dark/D-3-camera.png`): hero photo, small all-caps "92% CONFIDENT" tan accent line, Fraunces species/disease label, editorial 2-3 sentence narrative built from `fix_steps`, outline "Get a second opinion" CTA, and filled "Save to {nickname}" primary CTA that hands the full discriminated-union payload + compressed photo URI to `onSave`. **Scope lock:** this ticket ships ONE state — A-3 success. The 4 error / Layer-1 reject / low-confidence / queued / timeout / parse_error variants stay distinct in the type system and in the screen's runtime, but the styled UI for those branches lives in E5-009; until then they render a tiny "We'll improve this in E5-009 (kind: …)" placeholder so the screen never crashes and the discriminated union survives upstream verbatim. Stacks on the foundation primitives (`HeroPhoto`, `EditorialButton`, `DiagnoseLoadingState`, `useDiagnoseRequest`, `compressPhoto`, `useReduceMotion`); auto-rebases to main when those land. 23 new screen tests; mobile suite 376 (was 353).

### Added
- `apps/mobile/src/screens/CameraResultScreen.tsx` — the screen. Lifecycle: mount → fire `compressPhoto({ sourceUri: photoUri })` once (cached in screen state via a `phase` discriminator: `compressing | ready | compress-failed`); when `phase.kind === 'ready'`, fire `diagnose({ photoUri: phase.compressed.uri, plantContext })` exactly once via an `initialDiagnoseFiredRef` latch so React strict-mode double-invocation and parent re-renders don't double-fire the request. The compressed-photo URI is the load-bearing identity for the rest of the screen's lifetime — "Try again" / "Get a second opinion" re-fire `diagnose()` with the SAME URI, never recompress (a second `compressPhoto` call would re-encode the JPEG and produce a different file each time, throwing away the document-directory copy we just wrote). The success branch reads `lastResult` from the hook, narrows on `lastResult.ok === true`, and renders the A-3 card; the placeholder branch fires only when `!isLoading && lastResult !== null && !lastResult.ok` so the loading state owns the "no result yet" window. `onSave` payload: `{ result: { ok: true, data }, photoUri, mode, plantContext? }` — full preserved success shape so E5-011 can write to SQLite without re-narrowing. AppState resume listener registered (no-op on `'active'` — see "AppState resume" below). Reduce-motion gate is hard-disable on the success-card fade: `useRef(new Animated.Value(reduceMotion ? 1 : 0))` plus a guard that skips `Animated.timing` entirely when reduce-motion is on, mirroring the audit-defensible compliance pattern from `DiagnoseLoadingState` (omit the transform, do not damp it). Token verification: `theme.colors.tan` for the small all-caps confidence accent (the only tan accent in the palette — `theme.warn` does not exist, the closest existing accent is the soil/check-soil tan from DESIGN.md), `theme.colors.text` for the headline, `theme.colors.textMuted` for the narrative paragraph, `theme.colors.bg` for the scroll background. Hero photo composes `HeroPhoto` with `aspectRatio={1}` and `rounded={16}` — the A-3 mockup hero is square with smaller corner radius than the A-2 detail hero (24px), confirmed against the approved mockup.
- `apps/mobile/src/screens/index.ts` — screens barrel. Re-exports `CameraResultScreen`, `CameraResultScreenProps`, `CameraResultSavePayload`. Future screens (AddPlantScreen E5-010, PlantDetailScreen E4-005, etc.) re-export from here so navigation/route registries have a single import surface.
- `apps/mobile/src/screens/__tests__/CameraResultScreen.test.tsx` — 24 tests (377 total mobile, was 353). Coverage: mount fires compression once + diagnose once with the compressed URI; in-flight renders `<DiagnoseLoadingState>` while compression and request are pending; **fake-timer 3-bucket transition** (`Looking closely…` at 0s → `Almost there…` at 8s → `Trying a more careful look…` at 20s) flowing through the screen's loading dock; success card renders species + 92% CONFIDENT + narrative built from `fix_steps`; confidence rounds to nearest int (87.6 → 88% CONFIDENT); `onSave` fires with the full discriminated-union payload + compressed photoUri + mode + plantContext; save-CTA copy is "Save to {nickname}" when provided / "Save to my plants" otherwise; **try-again re-fires diagnose with the SAME compressed URI and does NOT call compressPhoto a second time**; non-success kinds (server / layer1_reject / queued) render the E5-009 placeholder with the kind preserved in the placeholder text so the discriminated union's identity is observable from outside the screen; reduce-motion: success-card opacity is 1 immediately with no `Animated.timing` registered; dark theme: headline color = `darkTheme.colors.text`, confidence accent = `darkTheme.colors.tan`; a11y: hero photo has `accessibilityLabel="Captured photo"`, both CTAs are reachable via `getByRole('button', { name })`, loading state announces via `accessibilityLabel="Diagnosing your plant photo"`; **AppState resume mid-request does NOT re-fire diagnose** (the listener is a no-op for the in-flight case; the loading-state bucket clock is wall-clock-anchored in `<DiagnoseLoadingState>` so it continues from where it was, not reset); saving on a non-success result is a no-op; `try-again` before compression resolves is a no-op (no retry button rendered); compression failure renders the placeholder without crashing or firing diagnose; parent re-render with the same `photoUri` does NOT auto-fire a second diagnose (the `initialDiagnoseFiredRef` latch). Plus 3 unit tests for the `buildNarrative` helper (joins fix_steps with periods, returns the all-clear copy when slug=`healthy`, returns empty when neither).

### Adversarial review
- **No P1 findings.** Codex + Claude self-review walked the 5 documented risk surfaces explicitly briefed for this ticket: (a) the discriminated union is preserved end-to-end — the `DiagnoseResponse` type flows through `useDiagnoseRequest` → screen → `onSave` payload with no widening or kind-collapsing; (b) AppState resume is non-destructive — the `addEventListener('change', ...)` callback only updates a tracking ref, never calls `diagnose` and never resets the loading-bucket anchor; (c) the `onSave` payload contract is the full success result + compressed photoUri + mode + plantContext, so E5-011 has every field SQLite needs without re-narrowing the union; (d) "Try again" reads `phase.compressed.uri` directly and feeds it back into `diagnose`, so `compressPhoto` is called exactly once per screen lifetime *per `photoUri`* regardless of how many retries the user fires; (e) reduce-motion is hard-disable: `Animated.Value(reduceMotion ? 1 : 0)` + a `Animated.timing` skip guard, no damped-but-still-running animation.
- **Two P2s caught + addressed before ship:**
  - **P2** — `compress-failed` rendered no user-visible recovery state. The original placeholder branch was gated on `lastResult !== null`, so a compression failure (which never fires diagnose) left the screen with just the hero image and nothing else. Fixed by splitting the placeholder into two branches: one fires on `phase.kind === 'compress-failed'` regardless of `lastResult`, the other fires on `!isLoading && phase.kind !== 'compress-failed' && lastResult !== null && !lastResult.ok`. Both surface the kind in the placeholder text so E5-009 has a clean handoff and the discriminated-union identity is observable from outside the screen.
  - **P2** — `initialDiagnoseFiredRef` was never reset when `photoUri` changed. A parent that reuses the same mounted screen instance for a new capture (FAB long-press → take photo → result; FAB long-press → take a *second* photo without unmounting; common in the Quick-diagnose flow) would have stayed permanently latched off after the first photo: compression re-runs on the new URI, but the diagnose call would never fire again. Fixed by resetting the latch + the `phase` to `compressing` in the same effect that re-runs compression on `photoUri` change. Locked under the new `photoUri change re-fires the initial diagnose` test.
- **One P2 caught + self-addressed during initial implementation:** the `useEffect` that fires the initial diagnose call would have run twice under React strict-mode double-invocation (the effect deps include `phase`, which transitions `compressing → ready` synchronously). Added the `initialDiagnoseFiredRef` latch so the request fires exactly once per screen lifetime per `photoUri`, regardless of effect re-invocation. Locked under the `does not auto-fire a second diagnose on parent re-render` test.

### V1 scope locks (rejected reviewer suggestions)
- **No collapsing of the discriminated union.** All 7 error kinds (`network`, `timeout`, `server`, `layer1_reject`, `low_confidence`, `parse_error`, `queued`) stay distinct in the type, in the hook output, and in the screen's runtime branching. The placeholder text leaks the `kind` so an E2E test (or a future debugger) can verify the union didn't collapse on its way through the screen.
- **No SQLite save here.** E5-011 owns that wiring. `onSave` is a callback contract, not a persistence side-effect. Adding the SQLite write here would couple two unmerged stacks (db schema lives behind a separate PR) and make the diff impossible to review in isolation.
- **No error / Layer-1-reject / low-confidence / queued / timeout / parse_error styled UI.** E5-009 owns those variants. The placeholder is intentionally minimal — a single italic line plus an optional Close CTA — so anyone reading the screen knows the real UI is missing and where it lands.
- **No interface changes to `useDiagnoseRequest` / `DiagnoseLoadingState` / `CameraView`.** All three were used as-is from their respective stacked PRs. The hook's `diagnose({ photoUri, plantContext })` shape, the loading-state's self-managed bucket clock, and the camera view's `onCapture(result)` shape are stable contracts.
- **No new dependencies.** Diff is additive within `apps/mobile/src` only; no `package.json` changes, no new RN modules, no navigation library.
- **No backend changes.**
- **No manual gates** (Maestro, EAS dev build, real OpenRouter eval) — autonomous-push session lock per the V1 push session memo.

### Notes
- **Token substitution:** the A-3 "92% CONFIDENT" small all-caps line uses `theme.colors.tan` (the soil/check-soil accent from DESIGN.md). DESIGN.md does not define a `theme.warn` token; tan is the closest existing palette accent that matches the warm cream/tan hue in the approved mockup. The substitution is identical in light + dark mode (`tan: #C9A873` is the same hex value in both themes per DESIGN.md, with passes-on-forest ratio of 5.57:1 in dark mode — well above WCAG 3:1 for graphical objects).
- **`buildNarrative` helper** is exported from the screen module via `__testing.buildNarrative` for unit-test access without rendering the screen. Lift it into `src/lib/narrative.ts` if a second consumer needs it; until then, premature abstraction.
- **Compression failure (`PhotoCompressError`) does not fire the diagnose request.** The screen's `phase` ends in `compress-failed` and the placeholder branch handles render. E5-009 will render a styled compress-failed card; this PR's branch is "no crash, no spurious LLM call."
- The A-3 mockup "Save to Steve" CTA copy is data-driven via the `plantNickname` prop — supplied by the parent (Plant Detail screen for the rescue path, FAB long-press flow for "Quick diagnose"). The fallback "Save to my plants" copy covers the no-plant-context branch (Quick diagnose path) without changing the screen's signature when E5-011 ships.
- **AppState listener is a no-op on resume by design.** The diagnose hook's mounted-ref + race-by-call-order guards already protect against stale results; the loading-state bucket clock is anchored at compression-resolve time inside `<DiagnoseLoadingState>` itself (its `startedAtRef`), so the user who backgrounds at the 12-second mark and resumes at the 18-second mark sees the bucket continue from where it was — not reset to "Looking closely…". The listener is registered so future telemetry / analytics can hook in via the same subscription contract.

<<<<<<< HEAD
<<<<<<< HEAD
## [0.1.29.0] - 2026-05-05

E6-003 — `<LocationPermissionPrePrompt>` Conservatory cream pre-prompt that fronts the iOS / Android foreground-location dialog, with a manual ZIP fallback for users who decline the OS prompt or who previously denied permanently. Parallel to the camera pre-prompt (E5-003) but with one extra branching state (`'zip-fallback'`) so the master plan §"Watering rules — missing inputs" guarantee — that V1 always has a working path to a weather modifier — holds even when the user refuses GPS. The component COLLECTS a 5-digit US ZIP and EMITS it via `onZipSubmit`; geocoding is owned by the backend Open-Meteo wrapper (E6-001) so the device never holds a third-party API key. Foreground only — V1 lock — and `requestForegroundPermissionsAsync` is called only on user-initiated taps so the `canAskAgain` budget is never burned by an AppState resume. Stacks on `origin/main`. 29 component tests pass; mobile suite 381 tests across 25 files.

### Added
- `apps/mobile/src/components/LocationPermissionPrePrompt.tsx` — the component. Six-state machine (`resolving | undetermined | requesting | denied | zip-fallback | granted`). Renders a single blank `theme.colors.bg` surface frame in `'resolving'` so already-granted users never flash the cream pre-prompt on mount (mirrors the codex P2 fix from E5-003). Cream pre-prompt: Fraunces "PlantCare wants your location" headline, Inter "To check the forecast for your watering decisions. Used only on this device." body, forest "Use my location" CTA (calls `Location.requestForegroundPermissionsAsync`), ghost "Enter ZIP instead" CTA. Denied state: tan-bordered card, `Linking.openSettings()` deep-link CTA, ghost "Enter ZIP instead" + "Not now" CTAs (codex P2 — denied state must always have a direct cancel affordance, not a forced detour through ZIP). ZIP fallback: `<TextInput keyboardType='number-pad' maxLength={5}>`, validates length=5 + digit-only, leading zeros preserved (06511 stays 06511), strips a leading "+1 " or "1 " US country-code prefix BEFORE clamping (codex P2 — paste of "+1 94110" must yield "94110", not "19411"), strips ZIP+4 suffix ("12345-6789" → "12345"). On grant + position fetch success, fires `onGranted({ latitude, longitude })` exactly once (single-flight `grantInFlightRef` + `grantedFiredRef`). On `getCurrentPositionAsync` rejection post-grant (airplane mode, GPS off), drops into ZIP fallback so the user has a working alternative — permission was granted, so no `canAskAgain` budget is burned. Component-scoped `unmountedRef` makes every async path cancel-safe. AppState `'active'` re-queries `Location.getForegroundPermissionsAsync()` (NEVER `requestForegroundPermissionsAsync` — that would burn the budget on a non-user-initiated event). Android `BackHandler` routes `hardwareBackPress` → `onCancel()` + returns true. Reduce-motion subscription wired (parity with E5-003 / E5-004) so future polish reads the value without re-plumbing. Screen-reader focus handoff via `AccessibilityInfo.setAccessibilityFocus(findNodeHandle(...))` on every state transition so VoiceOver / TalkBack lands on the new heading.
- `apps/mobile/src/components/__tests__/LocationPermissionPrePrompt.test.tsx` — 29 tests across all 6 states, the resolving-flash fix, request flow (granted, soft-denied retry, permanent-denied → settings, OS dialog dismissed, request rejection fallback, post-grant position rejection → ZIP fallback), ZIP validation (digit-only, +1 country-code strip, ZIP+4 suffix strip, leading-zero ZIPs, length=5 gate, `onZipSubmit` payload, denied → ZIP path), AppState resume (re-queries on `'active'` without burning `canAskAgain`), Android `hardwareBackPress` → `onCancel`, reduce-motion subscription, dark theme token swap, accessibility (header role, button roles, ZIP input hint, header preserved on every visible state for focus handoff), codex P1/P2/P3 contract locks (Not now CTA on denied, header on every state, single-flight `onGranted` under racing AppState transitions).
- `apps/mobile/package.json` — adds `expo-location@~55.1.9` (SDK 55 line, pulled by `bun add expo-location`).
- Barrel `apps/mobile/src/components/index.ts` exports `LocationPermissionPrePrompt`, `LocationCoords`, `LocationPermissionPrePromptProps`.

### Adversarial review (codex)
Two rounds; all findings addressed before ship:

Round 1:
- **P1** — `fetchAndEmitGranted` was not cancel-safe and not single-flight. The mount-path's `{ current: cancelled }` snapshot captured `false` literally at call-time, so unmount cleanup couldn't stop a late `getCurrentPositionAsync` from calling `setState` / `onGranted`. And there was no in-flight guard, so a racing AppState resume could pass the `grantedFiredRef` check before the mount path flipped it, double-firing `onGranted`. Fixed by introducing a single component-scoped `unmountedRef` (mutable across the component lifetime) and a `grantInFlightRef` short-circuit + try/finally reset. New regression test under "codex fixes" verifies multiple concurrent AppState `'active'` transitions still produce exactly one `onGranted` call.
- **P2** — Denied state had no direct cancel affordance. A user who refuses both location AND ZIP would be stranded on the denied card with no exit. Added a "Not now" Pressable wired to `onCancel` below the "Enter ZIP instead" CTA. New regression test verifies the press fires `onCancel`.
- **P3** — No screen-reader focus handoff on state transitions. VoiceOver / TalkBack focus could remain on a removed element when the card swaps undetermined → denied → ZIP fallback. Added a `focusTargetRef` forwarded to whichever heading container is currently rendered + an effect that calls `AccessibilityInfo.setAccessibilityFocus(findNodeHandle(node))` on every `state` change (skipping `'resolving'` and `'granted'` which have no semantic content). Test verifies the structural contract (every visible state renders an `accessibilityRole='header'`).

Round 2:
- **P2** — `handleZipChange` stripped non-digits but didn't normalize a leading US country-code prefix. A paste of "+1 94110" would become "194110" → clamp to "19411" (wrong). Fixed with a leading-only `replace(/^\s*\+?1[\s-]+/, '')` BEFORE the digit strip. Verified the regex doesn't break ZIP+4: "12345-6789" still yields "12345" because the country-code regex requires whitespace or dash AFTER the leading "1", which "12345-6789" doesn't have. New test covers the +1 case.
- **All clear** on round 2 final pass.

### Design review
The component uses no new tokens — it reuses the same `theme.colors.surface` / `theme.colors.stroke` / `theme.colors.tan` / `theme.colors.primary` / `theme.colors.text` / `theme.colors.textMuted` palette as `<CameraPermissionPrePrompt>`, the same `card` / `headline` / `body` / `ctaPrimary` / `ctaSecondary` style structure, and the same Fraunces 24/30 + Inter 15/22 type stack. Voice matches the master plan §"Camera permission" approval: editorial, present-tense, no AI-slop ("To check the forecast for your watering decisions. Used only on this device." not "We need your location to deliver personalized watering recommendations"). No new mockup needed because the layout is locked to the camera pre-prompt's already-approved A-3 cream card pattern.

### V1 scope locks (rejected reviewer suggestions)
- No background-location. V1 lock: foreground only via `requestForegroundPermissionsAsync`.
- No on-device geocoding. The backend Open-Meteo wrapper (E6-001) owns geocoding; this component just collects + emits a 5-digit ZIP via `onZipSubmit`.
- No maps SDK / map picker. Out of V1 scope.
- No `react-hook-form` / form library. Single ZIP input → plain controlled `useState` is enough.
- No persisting ZIP in component state. Emits upward via callback; persistence lives in the DB layer / parent screen.
- No compressing the snapshot states. Each of the six states has a distinct UI per the master plan.

### Notes
- Reduce-motion is wired (subscribe + cleanup) but the component currently has no animations to gate. The hook is in place so future polish can read the value without re-plumbing the listener — same pattern as `<CameraPermissionPrePrompt>` and `<PlantCareCameraView>`.
- `findNodeHandle` returns `null` for test-rendered Views under jest-expo's node env, so the focus-handoff effect is verified structurally (every state renders an `accessibilityRole='header'`) rather than via a direct `setAccessibilityFocus` spy.
- `onGranted` and `onZipSubmit` are mutually exclusive paths to a downstream weather call. The parent screen routes whichever lands first to `/api/weather` (lat/lon directly) or to the geocoding wrapper (ZIP → lat/lon) per E6-001.

## [0.1.28.0] - 2026-05-05

E3-001 — `<PlantCard>` is the per-row composite for the A-1 Plants list: square 64×64 hero photo on the left, Fraunces species headline + italic quoted nickname + Inter "last watered N days ago" stack in the middle, and a `<StatusChip>` on the right. The whole row is a single `<Pressable>` with the master-plan VoiceOver label ("Monstera Mona, water today, last watered 5 days ago") — identity first, then status, then the supporting context. The card takes a pre-computed `status: WateringStatus` prop rather than calling `useWateringEngine()` per row; E3-003 (`<PlantsListScreen>`) batches the SQLite reads at the screen layer. The relative-date copy uses the same local-tz calendar-day walk that already underpins `<PhotoTimeline>` and `<WateringLedger>` — millisecond math only, no `Math.floor` of UTC ms, no date-fns / dayjs / luxon / Temporal. 27 component tests, 380 mobile tests total (was 353 on main).

### Added
- `apps/mobile/src/components/PlantCard.tsx` — `<PlantCard speciesLabel nickname photoUri lastWateredAtMs status onPress nowMs? testID? />`. Maps the engine's `WateringStatus` (`'water' | 'skip' | 'check_soil'`) to the chip's `StatusKey` (`'water' | 'skip' | 'soil'`) inline; the `null` watering history case routes through the engine's `'check_soil'` fallback (which is what `useWateringEngine` returns during SQLite load anyway), so a list that hasn't finished loading still renders sensible chips. Nickname renders italic Fraunces wrapped in curly single quotes (`'Mona'`) per DESIGN.md "Italic for plant nicknames in quotes" + the A-1 mockup; the accessibilityLabel composer keeps the unquoted form so VoiceOver reads "Monstera Mona, …" not "Monstera 'Mona', …". Hairline divider lives at the screen layer (FlatList `ItemSeparatorComponent` in E3-003), not on the card — keeps the composite reusable. Reduce-motion: when `useReduceMotion()` returns true the pressed-state scale transform is **hard-disabled** (omitted entirely from the style array), not damped; opacity feedback (0.92) stays so the press is still acknowledged.
- `apps/mobile/src/components/__tests__/PlantCard.test.tsx` — 27 tests. Coverage: render with each of the four status states (`water`, `skip`, `check_soil`, plus null-history → fallback copy), `onPress` fires on row tap, master-plan accessibilityLabel composition for a watered plant + null-nickname / null-everything fallbacks ("Unidentified plant" for the queued-identify shape), each `formatLastWatered` bucket (null → "Not yet watered" / today / yesterday / 2-13 days / 14+ absolute), the **Hermes-without-Intl regression** that monkey-patches `Date.prototype.toLocaleDateString` to throw and asserts the static `MONTH_SHORT` fallback ("Last watered on May 1"), reduce-motion HARD disable + opacity preservation, dark theme tokens swap, nickname-omitted, inner Text rows non-accessible, and `accessibilityElementsHidden` wrappers around both the photo and the chip (the codex P1 fix below). Three sub-day-precision regression tests pin the calendar-day-vs-ms-floor invariant: a 3d 12h fixture where Math.floor agrees, a 3d 12h fixture where Math.floor and the calendar walk **diverge** (Apr 30 6pm → May 4 6am: ms-floor=3, calendar-walk=4, component reports 4), and a 36h fixture spanning 2 local calendar days reading "2 days ago".
- `apps/mobile/src/components/index.ts` — re-exports `PlantCard`, `PlantCardProps`, plus the helpers `composeAccessibilityLabel`, `composePressableStyle`, `formatLastWatered`, `localDayDelta`. The helpers are testable units; exporting them lets E3-003 reuse the formatters in screen-level sort/filter logic without duplicating the day-walk algorithm.

### Adversarial review (codex)
Three findings — one P1 fixed, one P2 fixed, two P3s addressed.

- **P1 (fixed)** — HeroPhoto and StatusChip were mounted as accessible descendants of the announcing Pressable. HeroPhoto's outer `<View>` carries `accessibilityRole="image"` + `accessibilityLabel="Photo of {species}"`; StatusChip's outer `<View>` carries `accessible accessibilityLabel="Water today"`. Without suppression, iOS VoiceOver can land on each as a separate stop and announce twice ("Photo of Monstera. Monstera Mona, water today, last watered 3 days ago. Water today."). Fixed by wrapping both children in a `<View importantForAccessibility="no-hide-descendants" accessibilityElementsHidden>`. Same shape `<PhotoTimeline>` already uses for nested HeroPhoto a11y; symmetry is the point. New test asserts at least two such wrappers exist in the rendered tree.
- **P2 (fixed)** — the 14+ absolute-date branch called `toLocaleDateString` with no Hermes/Intl fallback. Older Android RN builds ship Hermes without full Intl; the call would throw on those devices. `<WateringLedger>` already guards this exact class of failure with a `try/catch` + a static weekday table. Lifted the same pattern: a `MONTH_SHORT` table + `formatAbsoluteShort(ms)` helper that tries `toLocaleDateString` and falls through to "Mon D" on throw or empty. New test monkey-patches `Date.prototype.toLocaleDateString` to throw and asserts the static fallback ("Last watered on May 1").
- **P3 (fixed)** — `StyleSheet.create` was being recreated on every render. Wrapped in `useMemo` keyed on the four theme color tokens that the styles read. Avoidable churn caught before it became a perf footgun on the listing screen.
- **P3 (fixed)** — a `divider` style was defined but never rendered. Removed. The hairline lives at the screen layer (FlatList `ItemSeparatorComponent`).

**Verified, no action** (codex confirmed):
- DST/IDL math in `localDayDelta` is calendar-walk based; doesn't fall into the `setHours(0,0,0,0) + DAY_MS` trap E4-003 originally had. Walk uses `new Date(y, m, d + 1)` increments, which are DST-aware.
- The 3d 12h regression test fixture proves the calendar answer (4 days) differs from the ms-floor bug case (3 days) — not just an agreement test.
- Status pattern: prop, not hook, is the right call here. The screen needs to know status anyway for sorting/filtering; one DB read per plant at the screen layer beats N parallel reads from per-card hooks.
- accessibilityLabel composition order is right: identity first, then status, then context. Null-nickname / null-species fall through cleanly.
- Reduce-motion `composePressableStyle` HARD-disables `transform` when `reduceMotion=true` (no scale key in the returned array, not a damped value).
- Theme token usage is clean: only `bg`, `surface`, `text`, `textMuted` are read in styles. No `theme.warn` / `theme.danger` Wave 1 anti-pattern.
- Inner `Text` rows are correctly `accessible={false}` (Wave 1 lesson — prevents VoiceOver double-read of the species + nickname + last-watered against the parent label).

### V1 scope locks (rejected reviewer suggestions)
- No `date-fns` / `dayjs` / `luxon` / Temporal API. The local-tz calendar walk lifts the algorithm directly from `<PhotoTimeline>`, which already shipped without a date library. Swap to a tz-aware lib post-V1 if the surface area grows.
- No `Math.floor((now - last) / 86_400_000)` calendar-day diffing. The two divergent-fixture regression tests document why; same E4-002 invariant.
- No status-prop dropping in favor of in-component `useWateringEngine`. The screen needs status anyway for sorting/filtering; per-card hooks would create N parallel SQLite reads.
- No `<ThemeProvider>` wrapper. Hooks consume `useTheme()` directly.
- No swipe-to-delete / long-press menu / drag-to-reorder. Out of V1 scope; A-2 owns single-plant actions.
- No nickname normalization (lowercasing, whitespace trim). The card renders what the data layer hands it; normalization, if any, is a data-entry concern (E5-010 AddPlant flow).

### Notes
- The card is intentionally not memoized (no `React.memo`). On a list of ~20 plants, RN's `FlatList` already windows the render set; memoizing here adds reference-stability constraints to the parent without measurable benefit. Revisit if a profile pass on E3-003 shows render churn.
- `nowMs` defaults to `Date.now()` at render time (matches `<PhotoTimeline>`). Tests inject a fixed value for deterministic relative-date assertions; production callers don't pass it.
- Photo `null` (the queued-identify shape per the master plan) renders an empty cream skeleton in the photo slot. The accessibilityLabel routes through `nickname` if `speciesLabel` is null, falling through to the literal "Plant photo" only if both are null.

<<<<<<< HEAD
## [0.1.27.0] - 2026-05-05

E2-013 — Component Garden screen + primitives test coverage push to 100% lines. The Garden is a single dev-only screen (`apps/mobile/src/screens/ComponentGardenScreen.tsx`) that renders every primitive and shipped composite in two side-by-side columns: Conservatory (light) on the left, Midnight Conservatory (right). It is intentionally NOT routed in the production app shell — there is no entry in `apps/mobile/app/` that navigates here, the screen is simply importable. That keeps the V1 app surface clean while still giving design QA a single place to eyeball every primitive across both palettes. The load-bearing architectural decision is the new `ThemeOverrideContext` shim in `useTheme.tsx`: production code never wraps a Provider around any subtree, so `useTheme()` falls through to `useColorScheme()` exactly as before — but the Garden's ThemedColumn wrappers thread `lightTheme` / `darkTheme` through one column at a time, so each primitive renders in both palettes without spinning up a global `<ThemeProvider>` at the app root (V1 scope lock per the master plan's GSTACK REVIEW REPORT footer). The shim is the minimum-invasive shape that respects the lock — production trees see exactly the same two-line useTheme behavior they did before.

The other half of the ticket is the coverage push. Baseline on `apps/mobile/src/components/primitives/` was 91.03% lines / 84.29% branches with `EditorialBottomSheet.tsx` sitting at 68% — the PanResponder callbacks (`onPanResponderMove/Release/Terminate`) were untested because RTL can't reliably simulate native gesture events through jest-expo without coupling to private RN internals. The new `EditorialBottomSheet.gestures.test.tsx` suite intercepts `PanResponder.create` at the module-mock layer to capture the config object, then invokes the handlers directly with synthesized gesture states. That covers the dismiss path, the spring snap-back path, the reduce-motion-snap path, the terminate-reset path, and the live `reduceMotionChanged` listener path. The smaller `coverage-fill.test.tsx` suite picks up the remaining branch gaps (ToastBanner Android live-region cases for warn/info/pending, EditorialButton + FAB pressed-state styling, HeroPhoto onError silent-fallback) by reading the function-form `style` prop off the rendered Pressable instance. Final coverage on the primitives directory is 100% / 100% / 100% / 98.34% (lines / stmts / funcs / branches), with EditorialBottomSheet at 100% lines and 93.75% branches — the residual branches are the lazy-promise resolution path inside the mount effect, which RTL can't deterministically race.

### Added
- `apps/mobile/src/screens/ComponentGardenScreen.tsx` — `<ComponentGardenScreen />` plus `PRIMITIVE_SECTIONS`, `COMPOSITE_SECTIONS`, `ALL_SECTIONS` manifests. Each section is a self-describing `{ name, importPath, category, render }` record so a unit test can assert manifest parity against the primitives barrel without duck-typing. The screen wraps each section in a `<GardenSectionView>` that renders one Conservatory column and one Midnight column via `<ThemedColumn theme={lightTheme|darkTheme}>` — the `<ThemedColumn>` wrapper uses `<ForcedTheme>` (a thin `<ThemeOverrideContext.Provider>` passthrough exported from useTheme.tsx) so the override stays out of every primitive call site. Layout: a vertical `ScrollView` with a Fraunces page title, an Inter caption, and one section per primitive — each section has a Fraunces section header, an Inter "from '@/components/primitives'" import-path caption (so design QA knows where each primitive lives), and the two-column comparison row. Primitive coverage: Droplet (filled, outline, size variants), LeafIcon (3 sizes), HandOnSoilIcon (3 sizes), StatusChip (water/skip/soil), EditorialButton (filled/outline/loading/disabled), FAB (with and without long-press), HeroPhoto (1.6 aspect ratio with placeholder URI), EditorialBottomSheet (rendered closed — visual QA in E8-003 because portals and the garden don't compose), ToastBanner (warn/info/pending). Composite coverage: EmptyGardenWelcome, WateringLedger (empty + populated), PhotoTimeline (empty + populated), CameraPermissionPrePrompt (rendered with a placeholder note because the real permission flow needs the Expo native module), DiagnoseLoadingState (one instance per time bucket via injected `startedAtMs`).
- `apps/mobile/src/hooks/useTheme.tsx` (renamed from `.ts`) — adds `ThemeOverrideContext` (default `null`) and a `<ForcedTheme value={theme}>` Provider passthrough. Production behavior is unchanged: the context default is `null`, no production tree wraps a Provider, and `useTheme()` reads it via `useContext()` — when the override is null, it falls back to the existing `useColorScheme() === 'dark' ? darkTheme : lightTheme` line. The 7 existing `useTheme.test.tsx` tests still pass without modification. The hook barrel (`apps/mobile/src/hooks/index.ts`) re-exports `ForcedTheme` and `ThemeOverrideContext` alongside `useTheme` so the Garden can import from the canonical hooks barrel.
- `apps/mobile/src/screens/__tests__/ComponentGardenScreen.test.tsx` — 8 tests. The load-bearing one is the manifest parity assertion: it walks the primitives barrel runtime exports, filters out the explicitly-listed non-component helper (`shouldDismissOnDragRelease`), and asserts every remaining function-typed export has a matching `PRIMITIVE_SECTIONS` entry. If a future ticket ships a new primitive without a Garden section, this test fails. The render tests use a small subset of sections (Droplet/LeafIcon/StatusChip) to keep the smoke test fast and avoid pulling in expo-camera / Modal / Animated machinery from the larger composite sections.
- `apps/mobile/src/components/primitives/__tests__/EditorialBottomSheet.gestures.test.tsx` — 11 tests. Mocks `PanResponder.create` at the module layer to capture the config; runtime-patches `Animated.spring` so we can assert it was called without firing rAF chains. Covers `onStartShouldSetPanResponder` returning false (taps fall through to children), `onMoveShouldSetPanResponder` claim semantics (dy > 5 AND dy dominates dx), `onPanResponderMove` ignoring upward drags, `onPanResponderRelease` firing onDismiss above threshold, snap-back via `Animated.spring` below threshold (with `useNativeDriver: true` and bounciness 4 verified inline), instant snap-back when reduce-motion is on, `onPanResponderTerminate` resetting without dismiss, the `latestRef` pattern picking up a re-rendered onDismiss without re-creating the responder, the live `reduceMotionChanged` listener flipping the snap-back path mid-session, and the mounted-flag branch in the `addEventListener` cleanup.
- `apps/mobile/src/components/primitives/__tests__/coverage-fill.test.tsx` — 19 tests. Three groups: ToastBanner Android live-region (warn → 'assertive', info/pending → 'polite', iOS omits the prop entirely), EditorialButton + FAB pressed-state styling (read the function-form `style` off the rendered tree, evaluate at `{pressed: true}`, assert opacity 0.7 / 0.85 / inert 0.4), and HeroPhoto onError + onLoad pass-throughs + `rounded={false}` and `rounded={12}` resolution paths.

### Coverage outcome
On `apps/mobile/src/components/primitives/` after this PR: 100% statements / 100% lines / 100% functions / 98.34% branches across 9 source files. Every primitive file is at 100% lines on its own. The barrel `index.ts` is reported at 0% (re-exports only — TypeScript erases them at runtime, no executable lines). Mobile suite: 391 tests across 27 files (was 353 across 24). Backend suite still green at 148. Total workspace tests: 539.

### Adversarial review (codex)
Two P2 findings — both addressed before ship:
- **P2 (addressed)** — the EditorialButton dark-mode test claimed it "flips fill/label to Midnight tokens" but only asserted `backgroundColor`. A regression in the dark-mode label color (`darkColors.surface` → label) would have passed silently. Added a second assertion that reads the rendered Text's `style.color` and pins it to `darkTheme.colors.surface`. The corrected test now exercises both halves of the contract; the side-comment in the test was also tightened (the original "sage on forest" framing implied `colors.primary`, which is sage in dark mode but is NOT the bg of the filled variant — the filled variant uses `colors.text`, which is cream in dark mode).
- **P2 (addressed)** — the HeroPhoto error-fallback test claimed it "hides the Image and keeps the cream skeleton" but only verified the callback fired and the wrapper accessibility label stayed set. It did NOT actually assert the Image was unmounted or that the cream skeleton color survived. Added a pre-error child-count assertion (`hero.children.length === 1`), a post-error child-count assertion (`=== 0` — the conditional Image render is gone), and a post-error `containerStyle.backgroundColor === lightTheme.colors.surface` assertion that pins the cream skeleton.
- **No P1s.** Token verification: every `theme.colors.*` access in the new code (`bg`, `surface`, `text`, `textMuted`, `stroke`) is a real `ColorTokens` field per `packages/theme/src/colors.ts`. No assumed `theme.warn` (Wave-1 lesson held). Manifest parity: every primitive function-export in the barrel has a matching `PRIMITIVE_SECTIONS` entry, asserted by the parity test in `apps/mobile/src/screens/__tests__/ComponentGardenScreen.test.tsx`.

### V1 scope locks (rejected reviewer suggestions)
- No Storybook / Ladle install. Single-screen garden is the master-plan-codified shape; a real Storybook addon ecosystem pulls a non-trivial peer-dep tree we haven't budgeted for V1.
- No global `<ThemeProvider>` at the app root. The new `ThemeOverrideContext` is dev-only, default `null`, never wrapped in production trees.
- No manual theme toggle. DESIGN.md is explicit: "Mode follows OS via useColorScheme(). No manual toggle in V1."
- No new route to the garden in `apps/mobile/app/`. Garden stays unrouted; future dev-only entry (long-press FAB, hidden gesture in dev builds) lands in a separate ticket if/when the dogfood loop needs it.
- No Reanimated, gesture-handler swap, or vision-camera replacement. The PanResponder gesture tests honor the existing primitive shape; switching to gesture-handler is a different ticket entirely.

### Notes
- **Token verification:** every token reference in the new code (`bg`, `surface`, `text`, `textMuted`, `stroke`) was checked against `packages/theme/src/colors.ts`'s `ColorTokens` type at write time. There are no references to non-existent tokens — the Wave-1 `theme.warn` lesson held.
- **Why useTheme.ts → .tsx:** the `<ForcedTheme>` helper renders JSX, so the file needs the JSX-aware extension. The renamed file keeps the same module path (`./useTheme`); barrel re-exports continue to work without import-site changes anywhere.
- **Why `<ForcedTheme>` is a separate export:** the Garden could have used `<ThemeOverrideContext.Provider>` directly, but threading the context type through every call site leaks the Context API into the Garden's section list. The wrapper is one passthrough function. It's exported (rather than kept private) so future dev-only consumers (a Maestro-flow harness, a screenshot script) can use it without re-importing the context.
- **EditorialBottomSheet rendered closed in the garden:** the bottom sheet uses RN's `<Modal>` internally, which renders into a separate native container and can't be visually composed inside a `<ScrollView>`. The garden surfaces a placeholder note explaining this; real visual QA happens in E8-003 when `<AddNoteSheet>` lands as the primary consumer.
- **CameraPermissionPrePrompt rendered with a placeholder note:** the pre-prompt's state machine depends on `expo-camera`'s `useCameraPermissions()` hook, which can't be exercised in a static garden render without triggering the OS permission prompt. The garden test mocks the hook so the smoke render passes; real visual QA happens in E5-003's dev-build run.

<<<<<<< HEAD
=======
>>>>>>> origin/Anandsatch/e6-weather-endpoint
=======
>>>>>>> origin/Anandsatch/e5-result-errors
=======
>>>>>>> origin/Anandsatch/e7-sync-drainer
=======
>>>>>>> origin/Anandsatch/e3-plants-list
=======
>>>>>>> origin/Anandsatch/e10-dark-primitives
=======
>>>>>>> origin/Anandsatch/e6-watering-engine-v2
=======
>>>>>>> origin/Anandsatch/e5-camera-tests
## [0.1.26.0] - 2026-05-04

E5-004 — `<CameraView>` wraps `expo-camera` with permission-gated rendering (composes `CameraPermissionPrePrompt` from E5-003), a top mode-toggle pill (identify / diagnose), and a bottom shutter button. The mode prop discriminates downstream behavior in E5-008 (`CameraResultScreen`) and E5-010 (`AddPlantScreen`). Stacks on `Anandsatch/e5-camera-pre-prompt` (PR #18); auto-rebases to main when E5-003 lands. The wrapper is exported as `PlantCareCameraView` from `apps/mobile/src/components/CameraView.tsx` because `expo-camera` already exports a class component named `CameraView` from its modern API — the file name keeps symmetry with the master-plan spec, the export name avoids the import collision.

### Added
- `apps/mobile/src/components/CameraView.tsx` — `PlantCareCameraView` component. Permission gating via `useCameraPermissions()`: while the snapshot is `null`, render a single blank `theme.colors.bg` surface frame so already-granted users don't see the cream pre-prompt flash on mount (mirrors the codex P2 fix from E5-003); on undetermined or soft-denied, render `<CameraPermissionPrePrompt>` which owns the request flow and fires `onGranted` to flip into the camera UI; on permanent denial, the pre-prompt's denied state owns Settings recovery. Camera UI: full-bleed `expo-camera` `CameraView` (aliased `ExpoCameraView`), facing `back` (master plan: rear camera for plant photos, no flip in V1), a 36×36 close button at top-left with `hitSlop: 12` (effective 60×60 hit area), a pill mode toggle that flips between IDENTIFY and DIAGNOSE (active segment fills `theme.colors.text` with `theme.colors.surface` label; inactive sits on the surface fill in `theme.colors.text`), and an 80×80 shutter (4px outer ring stroked in `theme.colors.surface`, 64×64 inner solid in `theme.colors.surface`) at bottom center. Shutter calls `takePictureAsync({ quality: 0.85, skipProcessing: false })` and emits `onCapture({ uri, width, height })` on success; rejection is swallowed but logged via `console.warn` so the press handler never throws into RN's error boundary. Press-feedback scale animation on the shutter (Animated.spring 1 → 0.92 → 1) becomes a no-op when `AccessibilityInfo.isReduceMotionEnabled()` is true (inlined here because E2-004's `useReduceMotion` hook hasn't merged in this stacked base; pattern mirrors `CameraPermissionPrePrompt`).
- `apps/mobile/src/components/__tests__/CameraView.test.tsx` — 19 tests across permission gating (null-snapshot blank frame, undetermined renders pre-prompt, denied renders denied state, granted renders camera UI, undetermined → granted post-prompt transition), mode toggle (`accessibilityState.selected` flips with the active mode, `onModeChange` fires with the tapped segment, `hitSlop` declared on each segment so the 44×44 effective hit area is met), shutter (single `takePictureAsync` call with the right options, `onCapture` payload shape, rejection swallow + warn log + no `onCapture`, double-tap debounce via `inFlightRef` so only the first of three rapid taps fires `takePictureAsync`), close button → `onCancel`, accessibility labels (`accessibilityRole='button'` + label on shutter / close / each toggle segment), permission resume + Android back (AppState `'active'` re-queries permission via the hook's get-fn so the snapshot refreshes after the user grants in Settings; Android `BackHandler` routes hardware back to `onCancel` and returns `true`), dark theme token swap. Mobile suite: 66 tests across 6 files (47 prior + 19 new).
- Barrel `apps/mobile/src/components/index.ts` exports `PlantCareCameraView`, `CameraCaptureResult`, `CameraMode`, `PlantCareCameraViewProps`.

### Adversarial review (codex)
Two P2s caught + addressed before ship:
- **P2** — stale `useCameraPermissions()` snapshot on app resume. If the user backgrounds the app, grants permission in Settings, and returns, the hook can return the pre-grant snapshot and strand the user on the pre-prompt. Fixed by registering an `AppState` listener that calls the hook tuple's third value (`getPermission`) on transition to `'active'`. The hook re-renders us with the fresh snapshot. We never call `requestPermission` from this path because that would burn the `canAskAgain` budget.
- **P2** — Android hardware back / system back gesture bypassed `onCancel`. Fixed by registering a `BackHandler` listener (Android-only via `Platform.OS === 'android'`, gated on `permissionGranted` so the pre-prompt owns its own dismissal flow before grant) that calls `onCancel()` and returns `true` to tell RN we handled the press.
- **Verified, no action** — codex confirmed the modern `expo-camera` API surface (`import { CameraView, useCameraPermissions } from 'expo-camera'`) matches the SDK 55 docs; the in-flight ref + null-deref guards on the shutter are sufficient; the mode toggle accessibility (tablist container + button children) is acceptable.

### V1 scope locks (rejected reviewer suggestions)
- No `react-native-vision-camera`. `expo-camera` is the V1 lock per the master plan.
- No flash / torch UI. The A-5 mockup includes a flash icon for visual continuity, but it's deferred (not part of E5-004's L-size scope).
- No pinch-to-zoom. Out of V1 scope; users frame plants with the rear camera at a fixed FOV.
- No gallery picker. The V1 entry path is the FAB → camera; gallery import lands post-V1.
- No identify modal vs separate flow refactor. The mode toggle on a single screen is the master-plan-locked shape.

### Notes
- Reduce-motion: implemented inline (AccessibilityInfo + listener + cleanup) rather than waiting on E2-004. When E2-004 lands the `useReduceMotion` hook, this component will swap to it in a small follow-up; the press-feedback animation contract is unchanged.
- The mock `expo-camera` test module forwards a ref via `useImperativeHandle` so the imperative `takePictureAsync` call site can be observed without spinning up the native module under jest-expo's node env. The `AppState` and `BackHandler` `addEventListener` calls return `{ remove: jest.fn() }` from the global stub so component cleanup runs cleanly across all tests.
- Shutter animation owns its `Animated.Value` outside the render path (allocated once via a ref) so each press doesn't allocate a new node.

## [0.1.25.0] - 2026-05-04

E4-004 — `<PhotoTimeline>` 4-thumbnail row used at the bottom of A-2 (plant detail). Renders up to 4 most-recent photos as small `<HeroPhoto>` tiles (64×64 with `rounded={12}`) with relative-date labels below each ("Today" / "Yesterday" / "3 days ago" / "1 week ago" / "May 4"). Date formatting uses calendar-day deltas in the device's local time zone — same local-tz/UTC-ms boundary as the upcoming `<WateringLedger>` and the same `getFullYear/Month/Date` walk that survives DST and the international date line (the E4-002 watering-engine regression contract). Empty state explains how photos appear ("No photos yet — capture one when you Diagnose or Mark watered") so users understand how content fills in. Stacks on `Anandsatch/e2-hero-photo` (PR #15). 16 component tests pass; 59 mobile tests total.

### Added
- `apps/mobile/src/components/PhotoTimeline.tsx` — the composite. Props: `photos: PhotoEntry[]` (any length; component sorts desc by `takenAtMs` and slices the 4 most-recent), `onPhotoPress?` (when provided, each tile becomes a `Pressable` with `accessibilityRole='button'`; without it, tiles are image-only via `<HeroPhoto>`), `nowMs?` (default `Date.now()`; injected in tests for deterministic relative-date assertions), `accessibilityLabel?` (default `'Photo timeline'`), `testID?`. Local `PhotoEntry` type: `{ id, uri, takenAtMs, accessibilityLabel? }`. Empty state renders an italic Inter line in `theme.colors.textMuted`.
- `apps/mobile/src/components/index.ts` — top-level components barrel (alongside `./primitives`).
- `apps/mobile/src/components/__tests__/PhotoTimeline.test.tsx` — 16 tests covering empty state (italic copy + zero thumbnails), 1 / 4 / 5-photo render counts, sort-most-recent-first when input order is random, all 5 relative-date buckets ("Today" / "Yesterday" / "3 days ago" / "1 week ago" / 14+ absolute), future-timestamp clamp to "Today" (clock skew / bad EXIF), `onPhotoPress` fires with the correct `PhotoEntry`, dark theme caption uses `darkTheme.colors.textMuted`, default vs per-photo `accessibilityLabel` resolution, and the **DST/clock-skew regression**: a 36h fixture spanning 2 local calendar days still labels "2 days ago" — naive `floor((now - then) / 86_400_000)` would say 1 day ago (wrong); the calendar-day walk says 2 (correct). Same regression contract as the watering engine.

### Notes
- **Calendar-day, not 24h-since-now:** `formatRelativeDate(takenAtMs, nowMs)` extracts local Y/M/D from each timestamp and walks forward one calendar day at a time. A photo at 2pm yesterday relative to 9am today reads "Yesterday" — not "1 day ago" (which is reserved for the 2-6 day bucket). This is the same approach `<WateringLedger>` will adopt for its 7-day grid. DST fall-back fixtures stay correct because the day-walk doesn't depend on ms-per-day.
- **Why no shared `formatRelativeDate` helper yet:** E4-003 (`<WateringLedger>`) hadn't shipped at the time of this PR, so there's no common util module to extract into. The function is exported from `PhotoTimeline.tsx` and called out in the file header so the next ticket that needs it (likely E4-003) can lift it into a shared `src/lib/relativeDate.ts` without churn.
- **A11y identity when interactive:** when `onPhotoPress` is provided, the wrapping `Pressable` carries `accessibilityRole='button'` + the relative-date label. The inner `<HeroPhoto>` wrapper is hidden from screen readers via `importantForAccessibility='no-hide-descendants'` + `accessibilityElementsHidden={true}` so VoiceOver/TalkBack announces the tile once, not twice. When `onPhotoPress` is omitted, tiles fall back to `<HeroPhoto>`'s native `accessibilityRole='image'`.
- **Empty state copy:** "No photos yet — capture one when you Diagnose or Mark watered" is product-prescriptive on purpose. The empty state has to teach the reflective-care loop ("photos accumulate as you Diagnose and Mark watered"), which a generic "No photos" wouldn't. This is the same editorial-voice instinct DESIGN.md applies to other empty states.
- **Empty state still announces:** the wrapping `View` carries `accessibilityLabel="Photo timeline"` by default so a screen-reader user landing on the bottom of A-2 hears "Photo timeline" before reaching the empty-state copy.
- **No animations, no lazy image loading, no note-entry composition** — V1 scope locks. The note-row composition (note entries inline with photos) lands in E8-005, which writes to the timeline alongside this row.
- **No date-fns / luxon / dayjs:** V1 scope lock. `Intl.toLocaleDateString` covers the 14+ day branch; Hermes ships with full Intl on RN 0.74+ (we're on 0.83.6).

### Adversarial review (codex)
- One round of `codex exec` against the 7 documented risk surfaces (calendar-day vs 24h boundary, empty-state copy, `HeroPhoto` `rounded` prop wiring, sort stability for tied `takenAtMs`, `toLocaleDateString` Hermes/Intl fallback, `accessibilityRole` for non-interactive tiles, render-time date formatting). **No P1 or P2 findings.** Codex confirmed: bucketing is calendar-day-based with the 2pm-yesterday vs 9am-today fixture explicitly covered; identical `takenAtMs` values are deterministically ordered by `id`; interactive tiles use `'button'`, non-interactive tiles fall through to `HeroPhoto`'s `'image'`; date formatting on 4 thumbnails has no meaningful perf risk.

### Rejected reviewer suggestions
- "+N more" pill for plants with deeper photo history. Master plan keeps the 4-thumb row tight; the "see all photos" affordance lands in E4-005 (`<PlantDetailScreen>`) or later. Adding it here pulls scope forward.
- Note-entry composition (rendering `notes` alongside `photos` in the timeline). Master plan assigns this to E8-005. The shape of `Note` interleaving (avatar? icon? italic line?) is a separate design decision that the AddNoteSheet ticket owns.
- Animations on tile mount / fade-in. V1 scope lock — editorial voice over generic-app polish.
- Lazy image loading via `expo-image`. Same V1 scope lock as `<HeroPhoto>` (E2-010): FileSystem photos are local, no caching benefit.
- Extracting `formatRelativeDate` into a shared `src/lib/relativeDate.ts` ahead of E4-003 landing. Premature abstraction for one consumer; lift-and-shift when E4-003 needs the same helper.

## [0.1.24.0] - 2026-05-04

E5-006 — `useDiagnoseRequest` is the camera-flow hook that talks to `/api/diagnose`; it consumes the E2-005 `ApiResult<DiagnoseResponse>` discriminated union and adds an offline-degraded coercion (`network` kind → `queued`) so the UI surfaces a tan banner instead of an error card; actual persistence lives in E7. Stacks on `Anandsatch/e2-api-client` (PR #11) and will auto-rebase to main when E2-005 lands. The hook is intentionally minimal: a state machine, a monotonic call counter for race-by-call-order semantics, a mounted-ref guard for unmount safety, and an injectable `netInfo` probe whose default treats the client as online (real `@react-native-community/netinfo` is deferred to E7-003 per V1 scope locks). 12 new hook tests, 73 mobile tests total.

### Added
- `apps/mobile/src/hooks/useDiagnoseRequest.ts` — `useDiagnoseRequest({ apiClient, netInfo? })` returning `{ diagnose, status, lastResult }`. `diagnose({ photoUri, plantContext? })` returns `Promise<ApiResult<DiagnoseResponse>>`. State machine: `idle → requesting → success | error | queued`. The api client is injected (not defaulted to a singleton) to keep dependencies visible at every call site; a future `apps/mobile/src/api/singleton.ts` can add a default for screens to import without changing the hook contract. `PlantContext` (`{ species_slug?, nickname? }`) is defined locally rather than imported from db/types — db/types lives behind an unmerged PR and we don't want to couple stacks. The shape is forward-compat documentation today; the diagnose endpoint takes only an image on the wire.
- `apps/mobile/src/hooks/__tests__/useDiagnoseRequest.test.tsx` — 12 tests covering: happy path online → success, server-kind → error, low_confidence/timeout/parse_error/layer1_reject NOT coerced to queued, offline (`netInfo === false`) short-circuits without calling apiClient.diagnose, network kind coerced to queued, status transitions idle→requesting→success, unmount mid-request doesn't trip the React-18 setState-after-unmount warning, two in-flight calls resolving in reverse wall-clock order leave lastResult on the call-order-newer one (race-by-call-order, not race-by-resolution), plantContext acceptance, default netInfo (omitted) treats as online, apiClient throwing surfaces as queued (defensive coercion path).
- Hook barrel updated: `useDiagnoseRequest` + types (`DiagnoseInput`, `DiagnoseStatus`, `NetInfoLike`, `PlantContext`, `UseDiagnoseRequestConfig`, `UseDiagnoseRequestReturn`) re-exported from `apps/mobile/src/hooks/index.ts`.

### Network → queued coercion (the load-bearing decision)
When `apiClient.diagnose` resolves with `kind: 'network'`, the hook coerces that to `{ ok: false, kind: 'queued' }`. Codex adversarial review flagged this as a P2: `queued` semantically promises "we'll run it later," and the hook today has no actual persistence — `network` can also fire for DNS / TLS / captive-portal failures that aren't true offline, where coercing to `queued` is a small UX lie. The decision is to take the lie for V1 because: (a) the user-visible difference between "we couldn't reach the lab" and "you're offline" is nil for a non-technical user, (b) the master plan's desired UX (line 346) is the tan banner, not an error card with a retry button, and (c) E7-004 will wire the actual sync_queue persistence into this exact branch with no contract change at the hook boundary. `timeout` is NOT coerced — that's "request reached the lab, we waited too long," which is a different user intent (retry now) and a different error state in the A-3 spec.

### Race semantics (most-recent-by-call-order, not by-resolution)
The hook tags each `diagnose()` call with a monotonic counter and only commits a result if its id is >= the most recent committed id. A slow first call resolving after a faster second call cannot clobber the second call's `lastResult`. This is a real bug, not theoretical: the second call is often a retry of the first, and if the first call's tardy response wins, the user sees the result they were trying to get away from. Test 9 ("two diagnose() calls in flight resolve independently") locks the contract.

### Unmount safety: mountedRef vs AbortController
Considered threading `AbortController.signal` through `apiClient.diagnose`, but the E2-005 client doesn't currently expose `signal` on per-call options. Adding it is an api-client change, not a hook change. The mountedRef approach is correct for what the hook needs (suppress setState after unmount); the in-flight fetch will complete and its result is just discarded by the same call-order guard.

### Adversarial review (codex)
- **P2 (acknowledged, not changed)** — `network → queued` coercion makes the UI promise persistence the hook doesn't yet have. Counter-argument and rationale captured inline at the coercion site and in the section above. The spec ("any 'network' kind from the client should return queued") wins over the literal-truth concern for V1.

### Rejected reviewer suggestions
- Pull forward E7 sync_queue persistence into this PR. V1 scope lock; E7-001..E7-004 own that layer. Folding persistence into the camera-flow hook would couple two stacks and bloat the diff that should live behind the dedicated `SyncDrainer` review.
- Add `@react-native-community/netinfo` as a runtime dep here. V1 scope lock — E7-003 wires the real NetInfo subscription via the `netInfo` injection point this hook already exposes. Adding the dep now buys nothing the default ("always online + rely on api client's `network` kind for offline detection") doesn't already provide.
- Switch state management to `react-query` / `swr` / `tanstack-query`. The diagnose flow is one-shot per camera capture, not a long-lived cache; a query library would be more abstraction than value, and would also pull a non-trivial peer-dep tree.
- Default `apiClient` to a singleton. Hides the dependency at every call site and makes tests worse. Injection is more verbose, but the verbosity is exactly the dependency-visibility we want.

### Notes
- Test count: 73 mobile (was 61) + 148 backend = 221 across the workspace.
- This PR is stacked on `Anandsatch/e2-api-client` (PR #11). When E2-005 merges to main, GitHub will auto-rebase the base branch.

## [0.1.23.0] - 2026-05-04

E4-003 — `<WateringLedger>`, the 7-day droplet row anchoring A-2 plant detail and the per-plant block on A-6 weekly review. The component uses **local-tz day bucketing for display** while the watering engine math (E4-001) uses **UTC ms** — this is the deliberate display/logic boundary. Users see "watered Tuesday" under the TUE column on their phone regardless of how that instant maps to UTC; the engine, by contrast, computes `dueAtMs = lastMs + intervalDays * 86_400_000` so DST flips and international-date-line travel can't shift watering reminders. The empty state renders the row of seven outline droplets *plus* the italic Inter caption "No watering recorded yet — tap Mark watered to start your record" from day one — so when the first watering arrives the chart shape is already established and no relearning happens. Stacks on `Anandsatch/e2-icons` (PR #14); will auto-rebase to main when E2-006 lands. 16 new mobile tests, 61 total mobile.

### Added
- `apps/mobile/src/components/WateringLedger.tsx` — `<WateringLedger events startDate? nowMs? accessibilityLabel? testID? />`. Renders a flex row of seven equal columns, oldest-on-left through today-on-right. Each column is a small all-caps weekday label (fontSize 10, letterSpacing 0.5, marginBottom 4, theme.textMuted color) above a `<Droplet size=20>` (fills with `theme.water` if any event lands in that day's local-tz window, else `theme.surface` outline-only). Today's column carries a 3px dot beneath the label in `theme.text`; non-today columns reserve the same vertical space via a transparent placeholder so droplets stay baseline-aligned across the row. Each column is wrapped in `<View accessible accessibilityLabel="Monday, watered"|"Monday, no watering recorded">` so VoiceOver reads each day as a single composed stop. Empty-state caption renders **only** when `events.length === 0`, deliberately not when events exist but all are out-of-window (the latter is normal mid-history navigation, not a first-day state).
- `apps/mobile/src/components/index.ts` — top-level component barrel. Re-exports `WateringLedger` + the locally-defined `WateringEvent` and `WateringLedgerProps` types. `WateringEvent` is defined at the component for now (`{ wateredAtMs: number }`) rather than imported from `@plantcare/api-types` or the db schema — the db row type lives in an unmerged stack, and the field-shape contract (`wateredAtMs`) is the agreed name everywhere it's referenced.
- `apps/mobile/src/components/__tests__/WateringLedger.test.tsx` — 16 tests (61 total mobile). Coverage: empty-events renders 7 outline droplets + caption; one event today fills the rightmost column only; one event 3 days ago fills column 3; one-per-day fills all 7; an event 8 days ago is silently dropped (out of window); future events are silently dropped (defensive); the today indicator dot renders on exactly one column; column ordering is oldest-left / today-right (events 6 days ago and today both fill, middle stays outline); day-of-week labels reflect actual weekdays (anchored to Mon 2026-05-04 noon to remove tz ambiguity from the anchor); dark theme reskins droplet stroke to cream and caption color to Midnight `textMuted`; per-column accessibilityLabel reads "Monday, watered" / "Monday, no watering recorded"; two events same local day is idempotent (one filled column); root accessibilityLabel defaults to "7-day watering history". **DST regression tests** (the addressed P2 from codex review): an event on US-spring-forward Sunday 2026-03-08 buckets to the correct column with anchor Tue 2026-03-10; an event on US-fall-back Sunday 2026-11-01 (25-hour day) buckets to the correct column with anchor Tue 2026-11-03 — exactly one filled column in each case, no duplicate or missed bucket at the local-midnight boundary. **Intl-fallback test** (the second addressed P2): mocks `Date.prototype.toLocaleDateString` to throw, asserts the visible label and per-column accessibilityLabel both fall through to the hard-coded `SUN/MON/TUE/...` and `Sunday/Monday/Tuesday/...` tables — covers older Android RN builds where Hermes ships without full Intl.

### Adversarial review (codex)
Codex flagged two P2s; both addressed before ship.
- **P2 (addressed)** — visible labels also went through `toLocaleDateString` with no fallback, while only the long-form VoiceOver label had a try/catch. Centralized into a single `formatWeekday(d, 'short' | 'long')` helper that catches Intl failure and falls back to a static weekday table indexed by `Date#getDay()`. Visible labels and accessibilityLabels now share the same fallback policy.
- **P2 (addressed)** — the suite did not exercise the DST forward/backward boundary the component documents as load-bearing. Added two regression tests (US spring-forward + fall-back). The fall-back test caught a real bug in the first implementation: `dayStart.getTime() + DAY_MS` followed by `setHours(0,0,0,0)` collapses the column on a 25-hour day (Sunday midnight + 24h = Sunday 23:00 in fall-back tz; setHours(0,0,0,0) snaps that BACK to Sunday midnight, giving a zero-width column). Switched from millisecond stepping to `setDate(getDate() ± n)` calendar-walking, which is DST-aware. Without the regression test this would have shipped silently and only manifested twice a year for users in DST-observing tz.
- No P1s.
- **Rejected suggestions:** none — both P2s were addressed in the same patch, no scope-violating recommendations surfaced.

### Notes
- The local-tz / UTC-ms boundary is the load-bearing architectural call. The component module header documents it explicitly so anyone editing the ledger sees the contract before changing day-bucket semantics. Replacing local-tz day bucketing with calendar-naive UTC would split days at the wrong wall-clock hour for the user (e.g. an event watered Monday 9pm PT would render under Tuesday's column for any user east of UTC) — explicitly a V1 reject per ticket scope.
- Performance: seven cells x linear scan over `events.length` per render. At ~1000 events (multi-year plant lifetime) this is sub-millisecond on iPhone hardware. No memoization — `useMemo` overhead at this scale is roughly equal to the rebuild cost. If a plant ever exceeds ~10k events the hot path becomes the parent's full render anyway.
- Today's dot uses `theme.text` (deep forest in light, cream in dark). The non-today placeholder is a transparent View with the same width/height/marginBottom — pure layout reserve, no token reference.
- Caption uses `fontStyle: 'italic'`. `fontFamily` is intentionally unset; system font fallback is acceptable until Inter wiring lands in E2-005. Token color (`textMuted`) carries the editorial register.
- V1 scope locks honored: no animations, no Pressable columns (no day-detail drilldown), no `<ThemeProvider>`, no date-fns/luxon/dayjs (`Date` + ms math only), no horizontal scroll past 7 days. Only `Date#setHours`, `Date#setDate`, and `Date#getDay`/`getTime` are used — the surface area is small enough that a future swap to a tz-aware lib (post-V1) is mechanical.

## [0.1.22.0] - 2026-05-04

E5-007 — `<DiagnoseLoadingState>`. 3-bucket pulse loading copy ("Looking closely…" / "Almost there…" / "Trying a more careful look…") makes the LLM round-trip feel like a craftsperson at work, not a freezing app; reduce-motion swaps to static dots. First UI component in `apps/mobile/src/components/`. 15 new mobile tests, 53 total. Stacks on `Anandsatch/e2-reduce-motion` (PR #9); auto-rebases when E2-004 merges.

### Added
- `apps/mobile/src/components/DiagnoseLoadingState.tsx` — the A-3 loading line. Three time-bucketed Fraunces italic copy variants (0–8s `Looking closely…`, 8–20s `Almost there…`, 20s+ `Trying a more careful look…`) sourced verbatim from DESIGN.md and the master plan. Each copy variant sits above a horizontally-centered row of three 6px dots that fade between 0.3 → 1.0 opacity over 600ms with a 150ms stagger across dots, producing a left-to-right wave rather than a synchronized blink. Reduce-motion path skips `Animated.loop` entirely (not opacity-clamped — fully bypassed; the only audit-defensible compliance) and renders the dots at a flat 0.6 opacity. Default `startedAtMs` is captured once via `useRef` at first render so the bucket clock survives parent re-renders without restarting. `accessibilityLabel` defaults to `'Diagnosing your plant photo'`; `accessibilityLiveRegion='polite'` set on the root View so Android announces bucket transitions (iOS ignores the prop on View, which is fine — the visible copy carries the rest).
- `apps/mobile/src/components/index.ts` — barrel re-exporting `DiagnoseLoadingState` and `DiagnoseLoadingStateProps`.
- `apps/mobile/src/components/__tests__/DiagnoseLoadingState.test.tsx` — 15 tests covering bucket boundaries (0/7999/8000/19999/20000/60000), self-managed clock auto-advance via fake timers (mount → advance 8500ms → bucket 1 copy lands), interval cleanup on unmount, reduce-motion gating (Animated.loop spy assertions: 0 calls when reduce-motion is on, 3 calls per dot when off), accessibility prop forwarding (custom + default labels, `accessibilityLiveRegion='polite'`), dark/light theme token resolution, and the codex-P2-driven re-render suppression test that mounts a probe wrapper and asserts the wrapper does not re-render across 12 interval ticks within a single bucket.

### Adversarial review (codex)
- **P2** — first-pass implementation stored a raw `clockTick: number` in state and recomputed the bucket via `useMemo`. `useMemo` only memoizes the bucket value; setting state every 250ms still reconciled the entire subtree four times per second, defeating the stated churn-reduction goal. Fixed by storing the bucket index directly (`useState<0 | 1 | 2>`) and calling `setInternalBucket(next)` on every interval — React's setState bail-out skips the re-render when `next === current`, so the subtree only reconciles on actual bucket flips (twice over the 0→1→2 lifecycle). Added a render-count probe test to lock the behavior.
- **Documented, not fixed** — `accessibilityLiveRegion='polite'` is a no-op on iOS (RN ignores the prop on View). Inline comment notes the platform asymmetry; the visible Fraunces copy carries the announcement on iOS via the standard accessibility focus path. Bucket boundary inclusivity is locked: elapsed === 8000 lands in bucket 1, elapsed === 20000 lands in bucket 2 (each boundary is the start of the next bucket). Tested explicitly.
- **Rejected** — none. No reviewer suggested spinner-replacement, Reanimated upgrade, LottieView swap, or percentage progress bar (per V1 scope locks).

### Notes
- Stacks on `Anandsatch/e2-reduce-motion` (PR #9). The PR base is `Anandsatch/e2-reduce-motion`; when E2-004 merges to `main`, GitHub auto-rebases the base of this PR to `main`.
- The 250ms interval cadence is the minimum that lets bucket transitions land within ~one frame of the canonical boundary without burning render budget. Once bucket 2 is reached the interval auto-stops — final state, no further transitions to drive.
- No `<ThemeProvider>`, no Reanimated, no Lottie, no spinner, no percentage progress bar — V1 scope locks. The pulse uses RN's built-in `Animated` API with `useNativeDriver: true` so the animation runs on the UI thread.

## [0.1.21.0] - 2026-05-04

E2-007 — `<StatusChip type='water'|'skip'|'soil' />`, the right-edge per-row state pill on A-1 (Plants list) and the header-adjacent indicator on A-2 (Plant detail). Three types, three icons, three accent strokes; one component. The icon-to-type mapping is locked: `'water'` → filled `<Droplet />` + `WATER TODAY`, `'skip'` → `<LeafIcon />` + `SKIP`, `'soil'` → `<HandOnSoilIcon />` + `CHECK SOIL`. Stacks on Anandsatch/e2-icons (PR #14); auto-rebases when E2-006 merges to main.

### Added
- `apps/mobile/src/components/primitives/StatusChip.tsx` — `<StatusChip type accessibilityLabel? testID? />`. Surface fill (cream in light, forest in dark), hairline 1px border in the type's accent token (water/sage/tan), icon at 14px on the left, all-caps Inter 11/600/0.5 letter-spacing label on the right. Pill shape (`borderRadius: 999`) per the approved A-1 mockup. `alignSelf: 'flex-start'` so the chip's intrinsic width is preserved when laid out inside a row.
- `apps/mobile/src/components/primitives/__tests__/StatusChip.test.tsx` — 17 tests. Per-type: icon component + label string + surface/accent border tokens + default accessibilityLabel. Shared shape: custom accessibilityLabel override, non-interactive (no Pressable/onPress, exactly 2 children), pill `borderRadius: 999` + `borderWidth: 1`, all-caps Inter sizing per DESIGN.md, dark-mode token swap (mock `useTheme` → `darkTheme`, surface flips cream→forest, label color flips dark→cream, accent token water is mode-stable per DESIGN.md), `accessible=true` on chip + `accessible=false` on inner Text (single VoiceOver announcement), all three types render icon at size 14, testID forwarding. Total mobile tests: 62.
- Barrel export updated: `StatusChip` + `StatusChipProps` re-exported from `apps/mobile/src/components/primitives/index.ts`.

### Type → icon → accent → label mapping (the three-color metaphor lock)

| `type` | Icon | Accent token | Label | Default a11y |
|--------|------|--------------|-------|--------------|
| `'water'` | `<Droplet filled />` | `water` (#A8C5D9) | `WATER TODAY` | `"Water today"` |
| `'skip'` | `<LeafIcon />` | `sage` (#B8C5A6) | `SKIP` | `"Skip watering"` |
| `'soil'` | `<HandOnSoilIcon />` | `tan` (#C9A873) | `CHECK SOIL` | `"Check soil"` |

The `colorKey`, `iconName`, and `label` for each type are sourced from `@plantcare/theme/statusTokens` rather than redefined here, so future tickets touching the status vocabulary (Quick Diagnose suggestion chips, weekly review summary chips) read the same single table and can't drift.

### Notes
- Surface fill matches DESIGN.md "Status icon system": all three chips share `surface` fill so legibility lives in the **icon shape + label + hairline accent stroke**, not the chip background. The status fills on cream are 1.6-2.1:1 (below WCAG 3:1 graphical threshold) — color is decorative reinforcement; the icon shape and the all-caps label are the load-bearing semantic signals.
- Non-interactive in V1, by design. The parent `<PlantCard>` row is the pressable surface; the chip is a status badge, not a button. No `Pressable`, no `onPress`, no animation, no Reanimated — those are explicit V1 scope locks. If a future ticket needs a tappable chip (e.g. "tap to log this water"), it'll be a separate `<StatusChipButton>` primitive, not a flag on this one.
- `accessible=true` on the outer View groups the icon + label into one VoiceOver utterance; `accessible=false` on the inner Text prevents the iOS double-read where VoiceOver announces "WATER TODAY, Water today". The chip's own label is a fallback — the parent `<PlantCard>` typically announces `"Monstera Mona, water today, last watered 5 days ago"` and VoiceOver reads the card label first.
- All token reads go through `useTheme()` at render time, so a mid-session OS theme flip from Conservatory → Midnight reskins every chip with no per-component dark-mode code. The `water`/`sage`/`tan` accent hues are mode-stable (same hex in both themes; DESIGN.md verifies they each pass 5.57:1+ on the dark forest surface).
- Codex adversarial review (read-only sandbox): no P1/P2 findings. Verified token coupling across theme flips, default accessibility-label strategy, and the `accessible=false` double-read prevention. One non-blocking residual note (no `flexShrink`/`maxWidth` on the chip means very large dynamic-type scales could overflow a narrow parent row) — defer to E3-001 `<PlantCard>` row layout, where the chip's parent provides the actual constraint.
- No `<ThemeProvider>` introduced (the E2-001 lock holds). No icon font, no Lottie, no animation. The chip ships as static surface + 1px border + line-drawn SVG + Text.

## [0.1.20.0] - 2026-05-04

E3-002 — `<EmptyGardenWelcome>`. Day-1 Welcome card is the single CTA when zero plants exist (the floating "+" FAB is hidden until 1+ plants exist, per the master plan's "no choice paralysis" lock). First component in the mobile primitives lane: lives at `apps/mobile/src/components/EmptyGardenWelcome.tsx`, consumes `useTheme()` directly (no `<ThemeProvider>` per V1 lock), and exposes a single `onAddFirst` prop the parent screen wires to camera capture (A-5). Two interim scaffolds documented in the file header with explicit swap-when triggers: the inline-styled CTA swaps to `<EditorialButton variant='filled' />` when E2-008 merges, and the Unicode "🪴" emoji illustration swaps to a forest-stroke SVG when E2-006 lands `react-native-svg`. Both swaps are mechanical and do not invalidate the component's API.

### Added
- `<EmptyGardenWelcome onAddFirst testID? />` at `apps/mobile/src/components/EmptyGardenWelcome.tsx`. Renders a centered cream card (`theme.colors.surface`, 32×32 padding, 16px radius, `maxWidth: 360`, `alignSelf: 'center'`) with a 64px potted-plant emoji illustration, Fraunces semibold "Welcome to your garden" headline at 28/34, Inter "Snap a photo of any plant to add it." body at 16/24 in `colors.textMuted`, and a forest-fill Pressable CTA labeled "Add my first plant". Headline carries `accessibilityRole='header'`; CTA carries `accessibilityRole='button'` with the visible label echoed as `accessibilityLabel`. `hitSlop: 8` is the belt-and-braces guard against future label shrink dropping the hit area below 44px.
- Components barrel `apps/mobile/src/components/index.ts` re-exporting the component, copy constants (`HEADLINE_TEXT`, `BODY_TEXT`, `CTA_LABEL`), and the `EmptyGardenWelcomeProps` type. Future primitives (E2-006..012) layer onto the same barrel.
- 11 component tests at `apps/mobile/src/components/__tests__/EmptyGardenWelcome.test.tsx`: headline/body/CTA copy verbatim, CTA tap fires `onAddFirst` exactly once, headline `accessibilityRole='header'`, CTA `accessibilityRole='button'` with mirrored label, light-mode cream surface (`#FAF6EE`), dark-mode forest surface (`#1F3826`), illustration node renders with accessibility label, narrow-viewport self-containment (280px+), and the single-CTA contract (one button, no FAB sibling). `useTheme` is mocked directly so we can flip schemes without rendering the AppearanceProvider tree. Total mobile tests: 41 (up from 30).

### Notes
- **Codex adversarial review (P2 addressed):** the initial implementation combined `width: '100%'` with `marginHorizontal: 16`, producing a footprint of parent-width + 32 that would overflow a 280px viewport regardless of parent layout. Fixed by sizing the card to content with `maxWidth: 360` + `alignSelf: 'center'` and dropping both `width: '100%'` and `marginHorizontal`. The card now never overflows its own parent on its own. The narrow-viewport test asserts both removals to lock the contract. No P1 findings.
- **Inline CTA — interim, not a code smell.** `<EditorialButton variant='filled' />` ships in E2-008. Inline-styled Pressable here matches the filled variant from DESIGN.md (forest fill, cream label, 14×24 padding, 8px radius). The file header documents the mechanical swap.
- **Emoji illustration — interim, not a long-term choice.** The spec calls for a line-drawn forest-stroke potted plant. That ships with the icon primitives in E2-006, which depend on `react-native-svg` (not yet in `apps/mobile/package.json`). The "🪴" emoji at fontSize 64 is the cross-platform-acceptable Day-1 fallback for a one-off surface; the screen reader gets "Potted plant illustration" rather than the emoji's verbose default.
- **No `<ThemeProvider>`, no embedded navigation, no animations.** All three are V1 scope locks the master plan made explicit. The component reads `useTheme()` directly, takes `onAddFirst` as a prop, and renders zero motion (reduce-motion is a non-concern by construction).

## [0.1.19.0] - 2026-05-04

E5-005 — photo compression helper. Photos are local-only in V1 (no cloud upload, no R2, no sync queue for photos); compression is bounded at 1024px @ 0.7 quality so a 12MP iPhone source becomes ~150-300 KB and the device storage curve stays predictable across long-term users (10 plants × 5 photos = ~10-15 MB total local). The helper is the on-disk gate every camera capture flows through: `expo-image-manipulator` resizes + recompresses, then `expo-file-system/legacy` copies the result into `documentDirectory + plants/<id>/` (NOT cacheDirectory — survives app restarts, not OS-evictable). E5-006's `useDiagnoseRequest` and the upcoming Add Plant flow both consume the same compressed asset for upload, so there's one re-encode per capture, not two.

### Added
- `apps/mobile/src/photos/compress.ts` exporting `compressPhoto({sourceUri, plantId?, nowMs?})` → `{uri, width, height, sizeBytes}`. Pipeline: probe dimensions via no-op `manipulateAsync(uri, [], ...)` → branch on orientation (`{width: 1024}` for landscape/square, `{height: 1024}` for portrait) only when the longer edge exceeds 1024 → recompress at quality 0.7, JPEG → copy into `documentDirectory + plants/<id>/<nowMs>-<rand8hex>.jpg` → best-effort delete the manipulator's cache file. Source-already-small inputs skip the resize step but still get the quality-0.7 recompress; per spec the goal is bounded size, not source-of-truth detection.
- `getPhotoDirectory(plantId)` helper for callers that need the photo directory before any photo exists (e.g. listing existing captures, pre-creating the directory ahead of a bulk import). Path-traversal-safe: `plantId` is sanitized to `[A-Za-z0-9_-]` and clamped to 64 chars before being interpolated, so `'../../../etc/passwd'` collapses to a directory inside `plants/`.
- `PhotoCompressError` class with a typed `kind` discriminator (`'source-missing' | 'manipulate-failed' | 'storage-unavailable' | 'copy-failed'`) and ES2022 `cause` preservation. Wrapping `manipulateAsync`/`copyAsync`/`makeDirectoryAsync` failures keeps the original RN native-module error reachable via `err.cause` for observability without leaking the unstable native message into product UX.
- 18 unit tests in `apps/mobile/src/photos/__tests__/compress.test.ts` covering: landscape resize-by-width, portrait resize-by-height, ≤1024 no-resize, quality 0.7 boundary, JPEG format always (never PNG), output under `plants/<plantId>/`, plantId-omitted fallback to `unattached/`, filename `<nowMs>-<8hex>.jpg` shape, documentDirectory prefix, sizeBytes from `getInfoAsync`, empty-source `source-missing` throw, manipulate-failure cause preservation, `intermediates: true` directory creation, post-copy cache cleanup, null-documentDirectory `storage-unavailable`, and plantId path-traversal sanitization. Mobile test count: 30 → 48.
- `expo-image-manipulator@~55.0.15` and `expo-file-system@~55.0.17` added to `apps/mobile/package.json` — both pinned to the SDK 55 tags published by Expo's release channel.

### Notes
- `expo-file-system/legacy` is the deliberate import path. SDK 55 ships a class-based `Paths`/`File`/`Directory` API as the default export, but the legacy entry exposes the `documentDirectory` + `*Async` helpers the spec is written against. Both are supported through the V1 build window; revisit if Expo signals removal.
- The dimension probe (`manipulateAsync(uri, [], ...)`) decodes the source twice in the worst case — once to read width/height, once to actually compress. Codex flagged this as a residual perf/memory risk. Accepted: the probe is the simplest correct way to know orientation without bringing in a separate metadata lib, and a 12MP shot through ImageManipulator runs in well under 1s on Anand's iPhone. If real-device dogfooding shows hitches on large photos we'll revisit.
- `sizeBytes` falls back to `0` rather than `undefined` when `getInfoAsync` reports `exists: false` or omits the size field. Type stays `number` for caller ergonomics; `0` is the documented "couldn't measure" sentinel.
- Adversarial review (codex): zero P1/P2 findings. HEIC handling is delegated to ImageManipulator's native side; the helper outputs JPEG regardless of source format, so HEIC/panorama come through as standard 1024-edge JPEGs.

### Rejected (out of V1 scope)
- Cloud upload / R2 sync of photos. V1 lock: photos local only.
- EXIF stripping. Not in spec; revisit post-V1 if a privacy concern surfaces.
- Watermarking. Not in spec.
- `sharp` / native node image lib. RN-incompatible by definition.
- Photo upload queue. E7 owns `sync_queue`; V1 doesn't enqueue photos at all.

## [0.1.18.0] - 2026-05-04

E5-003 — Conservatory cream pre-prompt that fronts the OS camera dialog. The reason this exists: a pre-prompt wins on conversion + trust because users who refuse the pre-prompt can be re-asked later, but a denied iOS dialog with `canAskAgain=false` is permanent. We never want to burn that path on someone who isn't ready. The component is shaped like a screen but exported as a card so a parent (E5-004 `<CameraView>`) can compose it without the routing lock-in implied by a dedicated route.

### Added
- `apps/mobile/src/components/CameraPermissionPrePrompt.tsx` — the V1-locked Conservatory pre-prompt. Five-state machine (`resolving | idle | requesting | denied | granted`) keyed off `useCameraPermissions()` from `expo-camera`. The cream card pairs a Fraunces "PlantCare wants your camera" headline with an Inter body that addresses the privacy concern directly ("We never upload anything you don't ask us to.") plus a forest filled CTA "Allow camera access" and a quiet ghost "Not now" cancel. The denied state swaps to a tan-bordered card "Camera blocked" + "Open the Settings app to enable camera access" CTA → `Linking.openSettings()` (works on both iOS and Android per Expo docs).
- `apps/mobile/src/components/index.ts` — barrel for the new components directory. First entry; future E5/E6 components extend it.
- `apps/mobile/src/components/__tests__/CameraPermissionPrePrompt.test.tsx` — 14 tests covering initial idle render, the V1 lock that the OS dialog never fires before the user taps the CTA, the request flow (granted, soft-deny with retry, hard-deny → settings recovery, in-flight spinner with `accessibilityState.busy`, request rejection → idle fallback), the already-granted-on-mount short-circuit, the null-then-granted no-flash guarantee from the codex P2 below, the dark-theme token swap (Midnight Conservatory surface + cream stroke), the cancel path (fires `onCancel`, never calls `requestPermission`), and the a11y contract (`accessibilityRole='header'` on the headline, `'button'` with explicit labels on every CTA, "Open the Settings app to enable camera access" mentions Settings explicitly so VoiceOver users know the next surface). Mobile suite total: 47 tests across 5 files.
- `expo-camera@~55.0.17` dependency in `apps/mobile/package.json`. Pinned to the SDK 55 line to match the existing `expo` major and avoid the SDK 56 canaries.

### Changed
- State machine starts in `'resolving'` instead of `'idle'` so the first render returns `null` while `useCameraPermissions()` is still `null`. Without this, an already-granted user briefly saw the cream pre-prompt before the effect collapsed it — the codex P2 from the E5-003 adversarial review. The transition rule is "only graduate from 'resolving' to 'idle' on an undetermined snapshot"; `'requesting'` and `'granted'` are never clobbered by the snapshot effect.

### V1 scope locks (rejected reviewer suggestions)
- No bypass of the pre-prompt → the OS dialog. The pre-prompt is the entire reason this ticket exists.
- No `react-native-permissions`. `useCameraPermissions()` from `expo-camera` covers both `granted` and the `canAskAgain=false` permanent-denial signal we need.
- No analytics / telemetry on grant rate. We dogfood first; if a measurable problem shows up we'll add it post-V1.
- No "Privacy policy" link in the body. The body copy already names the privacy stance ("never upload anything you don't ask us to") and an extra link diverges from the editorial Conservatory voice.

### Notes
- `Linking.openSettings()` is the React Native built-in. Expo docs confirm it dispatches to `app-settings:` on iOS and the package settings intent on Android, so we don't need the platform branch the master plan flags as a fallback.
- The component is intentionally stateless w.r.t. navigation. Parents (E5-004 `<CameraView>` is the first one) decide whether `onCancel` pops a route, dismisses a sheet, or returns to a list.

## [0.1.17.0] - 2026-05-04

E4-001 + E4-002 — `useWateringEngine` v1 plus the mandatory DST/IDL regression test, bundled in one PR. The deterministic core of the daily-glance loop: given a plant + its last-watered timestamp + the current time, return one of three statuses ('water' | 'skip' | 'check_soil') that drives the A-1 list chip and the A-2 detail chip. V1 scope: species defaults table for ~10 common houseplants + per-plant override + check-soil bias zone between half-interval and full-interval. No weather, no location, no humidity/temp modifiers — those land in E6 on top of this engine. The whole thing is ~70 lines of pure function and it's covered by 21 new unit tests including the non-skippable timezone regression suite (E4-002) that this CLAUDE.md and the master plan both flag as the build's most likely shipping bug.

### The fundamental invariant

`computeWateringStatus` does millisecond math. Not calendar-day math. Not `setHours(0,0,0,0)` bucketing. Not `Math.floor((nowMs - lastWateredAt) / 86400000)`. Not `Date.getDate()` differencing. `intervalMs = intervalDays * 24 * 60 * 60 * 1000`, `elapsedMs = nowMs - lastWateredAt`, compare. The function is pure with respect to UTC ms and is NOT influenced by the local timezone, DST forward/backward transitions, IDL crossings, or Intl calendar fields. The hook layer (`useWateringEngine`) is the only place `Date.now()` appears, and it passes the result straight through to the pure function. This is the whole point: a user who waters at 12:00 LA time on Friday, flies to Sydney, opens the app at 12:00 Sydney time on the next non-existent Saturday — wall-clock days lie due to the IDL crossing, but UTC ms doesn't.

### Added
- `apps/mobile/src/watering/species.ts` — hardcoded `SPECIES_INTERVAL_DAYS` table for ten common houseplants (monstera 7d, fiddle leaf 7d, pilea 7d, pothos 10d, snake plant 14d, ZZ 14d, peace lily 5d — thirstier — spider plant 7d, epipremnum 10d, rubber plant 10d). `getIntervalDays(slug)` returns `null` for unknown slugs (engine treats null as "rules disabled, fall back to check_soil"). Uses `Object.prototype.hasOwnProperty.call` rather than `slug in map` to guard against prototype-chain pollution from a hostile slug like `'toString'` or `'__proto__'`.
- `apps/mobile/src/watering/engine.ts` — `computeWateringStatus({ plant, lastWateredAt, nowMs })` pure function. Logic: resolve interval (override wins over species, species_unknown with no override → null → check_soil), null lastWateredAt → check_soil (just-added plant), `elapsedMs >= intervalMs` → 'water' (boundary inclusive), `elapsedMs < intervalMs / 2` → 'skip', else → 'check_soil' (the bias-toward-checking zone). The factoring matters: the function takes `nowMs` as input rather than calling `Date.now()`, so the entire DST/IDL test surface is reachable as plain UTC unix ms inputs without `jest.setSystemTime` or global Date mocking.
- `apps/mobile/src/hooks/useWateringEngine.ts` — `useWateringEngine(plant)` hook. Reads the most recent `watering_events.watered_at` for the plant via `openDb()` + `idx_water_plant_date`, calls the pure engine with `Date.now()` as `nowMs`, returns the `WateringStatus`. Async-load window returns 'check_soil' (matches the no-history fallback — never a false-positive 'water' on an unloaded row). Cancellation flag in the effect blocks stale state writes after unmount or `plant.id` change.
- `apps/mobile/src/watering/__tests__/engine.test.ts` — 14 unit tests. Boundary semantics (`== interval` is 'water', `== interval/2` is 'check_soil' NOT 'skip'), null lastWateredAt → 'check_soil', species_unknown fallback (no override → 'check_soil' regardless of elapsed; with override → behaves like a known species), unknown slug fallback, override precedence (override=3 vs species=10 → override wins; override=20 vs species=7 → override wins).
- `apps/mobile/src/watering/__tests__/timezone-regression.test.ts` — **the E4-002 critical regression suite**, 11 tests across six fixtures: (1) US Eastern DST forward (2026-03-08, 23h day) — wall-clock "7 days later" is actually 6.958d elapsed → 'check_soil', not 'water', proving the engine doesn't trust the wall clock; (2) US Eastern DST backward (2026-11-01, 25h day) — wall-clock "7 days later" is actually 7.041d elapsed → 'water', boundary correctly past; (3) IDL flight LAX→SYD — same UTC elapsed regardless of `process.env.TZ` change between LA and Sydney; (4) IDL flight reverse SYD→LAX — same invariance; (5) Europe/London DST forward (2026-03-29) — same shape as US DST forward but with a different timezone, proving no hardcoded TZ assumptions; (6) sub-day precision (3d 12h 1ms elapsed against a 7-day species → 'check_soil') — the codex adversarial review catch: this distinguishes ms-math from a `Math.floor((nowMs - lastWateredAt) / 86400000)` rewrite that would bucket the elapsed time as 3 whole days and decide 'skip'; (7) a property test that loops 100 deterministic-random `(lastWateredAt, nowMs, intervalDays)` tuples under three timezones (UTC, LA, Sydney) and asserts the verdict is identical — the structural proof that the engine has zero TZ dependence. All TZ-toggling tests use `process.env.TZ` save/restore wrapping; the engine itself never reads it because pure ms arithmetic doesn't.
- Barrel exports: `apps/mobile/src/watering/index.ts` and one extra line in `apps/mobile/src/hooks/index.ts`. Mobile test count: 32 → 53.

### Adversarial review (codex)
One round of `codex exec` adversarial review caught one issue before ship:
- **P2** — the timezone-regression file as initially shipped did not stand alone against a `Math.floor((nowMs - lastWateredAt) / 86400000)` rewrite of the engine. The DST/IDL fixtures all landed on whole-day boundaries, so a floor-based bug would coincidentally pass them. The exact half-interval coverage in `engine.test.ts` was the only catch. Resolved: added a dedicated "sub-day precision" describe block to `timezone-regression.test.ts` with four cases targeting the floor-bucketing bug directly. The lead test (3d 12h 1ms elapsed against a 7-day species) ms-says 'check_soil' but a floor implementation would compute 3 whole days, see 3 < 3.5 (half-interval), and return 'skip' — caught.
- **Rejected** — pulling weather forward into V1 (E6 owns that), pulling in date-fns/dayjs/luxon (millisecond arithmetic is sufficient for this engine), switching to Temporal API (V1 scope lock).

### Notes
- Just-added plant convention: `lastWateredAt === null` → 'check_soil'. Master plan ("just-added plant shows 7 outline droplets + hint") aligns: the A-2 ledger renders all 7 outline droplets and the chip nudges the user to feel the soil. We don't claim 'water' without evidence.
- `species_unknown` is treated as rules-disabled unless `override_interval_days` is set. The user can identify their plant later (or set a custom interval) to flip from 'check_soil' to active rules. This matches the master plan's "Other → species_unknown flag and disables watering rules until the user picks a species later".
- The hook deliberately returns 'check_soil' during the SQLite load window rather than 'water'. A user opening the app on a plant they last watered 6 days ago would briefly see "water today" if we returned 'water' as the default — that's a false positive that materially affects user behavior. 'check_soil' is the safe default; the load resolves in single-digit ms in practice (idx_water_plant_date is selective on plant_id).
- Sub-day precision tests use `Date.UTC(2026, 5, 1, ...)` for fixture timestamps. JavaScript `Date.UTC` is the only Date API used anywhere in the regression suite — and it's a pure UTC-ms constructor with no timezone semantics. Future engine refactors that introduce `new Date(ms)` parsing would surface in the property test by diverging across the three TZ runs.
- The watering interval table values are research-backed defaults, not user-tested. Tune in dogfooding if a plant consistently underwaters or overwaters — the table is one file (`species.ts`) and each entry is one line.

## [0.1.16.0] - 2026-05-04

E2-012 — `<ToastBanner>` primitive. The third Epic E2 mobile-foundation ticket. A primitive only — positioning, queueing, and animation orchestration belong to parents (E7 sync queue surface, E11 budget meter, Day 1 nudges). Three types per the master plan: `'warn'` (tan, transient), `'info'` (sage, transient Day 1 nudges), `'pending'` (tan, persistent — for the offline sync queue's "{N} items syncing" banner). 18 new component tests pass; mobile suite is now 73 green.

### Added
- `apps/mobile/src/components/primitives/ToastBanner.tsx` — `<View>` + `<Text>` + optional action `<Pressable>`. Background derives from `useTheme()` per type — warn and pending use `theme.colors.tan` (master plan: both surface as tan), info uses `theme.colors.sage` (the "info → cream/sage variant" hint resolved as sage since cream is the surface and would be invisible). Padding 16, border radius 12, flex row, message gets `flex: 1` and `numberOfLines={3}` so a long message wraps without pushing the action off-screen.
- Auto-dismiss: `'warn'` and `'info'` default to 4000ms; `'pending'` defaults to undefined (persistent — the queue is the source of truth and the parent removes the banner when drained). Explicit `autoDismissMs={0}` disables auto-dismiss for any type. Timer fires `onDismiss` on EXPIRY only — the `useEffect` cleanup just clears the timeout, so an unmounted parent never receives a phantom dismiss. Re-arm semantics: the timer resets when `type`, `message`, `dismissMs`, or `visible` changes, so a flickering message gets the user a full read window on the new content rather than racing to fire on the old one.
- `accessibilityRole='alert'` on warn (interrupts VoiceOver, matches the master plan's "warn announces immediately"); `accessibilityRole='status'` on info/pending (master-plan-spec, set as a literal — RN's typed `AccessibilityRole` omits 'status' since it's a web-platform role and the native bridges ignore unknown roles harmlessly). Android-only `accessibilityLiveRegion`: `'assertive'` on warn, `'polite'` on info/pending. iOS doesn't have a parallel View prop; per-event announcements there are a parent concern (`AccessibilityInfo.announceForAccessibility`).
- `apps/mobile/src/components/primitives/index.ts` — barrel re-exporting component + types so screens can import `ToastBannerType` / `ToastBannerProps` / `ToastBannerAction` from a single path.
- `apps/mobile/src/components/primitives/__tests__/ToastBanner.test.tsx` — 18 tests using `jest.useFakeTimers()` for the timer paths. Coverage: per-type backgrounds (warn=tan, info=sage, pending=tan), message renders verbatim, action wires through, no-action variant skips the button, default 4s auto-dismiss for warn + info, pending stays put under 30s, explicit 0 disables, custom 2000 fires at 2000, unmount before expiry skips onDismiss, message change re-arms the timer (advance 3000 → change → advance 3000 → not yet → +1000 fires once), accessibilityRole per type, dark theme tokens swap (tan, sage), `visible={false}` skips render and skips the timer.

### Adversarial review (codex)
One round of `codex exec` adversarial review across the seven risk surfaces (timer leak, multi-fire, alert role + VoiceOver, hit target ≥44, reduce-motion, token coupling for pending, long-message wrap). Returned **no P1/P2 findings**.

### Rejected (out of V1 scope, per ticket spec)
- Adding a `<ToastProvider>` / queue manager — primitive only, parent owns positioning + stacking.
- Swipe-to-dismiss gesture — master plan calls out auto-dismiss + tap-action only.
- Slide-in / fade animations — V1 stays simple; reduce-motion compliance is automatic when there are no animations.
- `react-native-toast-message` dependency — adds a new top-level provider model and a runtime not aligned with the per-context placement the screens need.

### Notes
- The `'info' → sage` mapping is one read of the master plan's "cream/sage variant" hint. Cream IS the surface in light mode (`#FAF6EE`), so a cream banner over cream surface would be invisible. Sage carries enough contrast against both light cream and dark forest surfaces and matches the master plan's "info/pending are non-disruptive" tone (sage is the calm/healthy hue in the three-color metaphor lock). If E11's Day 1 nudge mockups land with a different visual, the swap is a one-line change in `bannerColor()`.
- The action button uses `hitSlop={8}` — combined with the 16px container padding and 4px button padding, the effective hit target reaches the 44×44 floor for typical action labels (3-6 chars). Visually-smaller labels can grow `hitSlop` at the call site; the primitive ships a sane default rather than forcing every consumer to specify one.

## [0.1.15.0] - 2026-05-04

E2-011 — `<EditorialBottomSheet>` primitive. The most complex of the V1 primitive set: focus trap, swipe-to-close, reduce-motion compliance, and a deliberate no-extra-deps ship. RN's built-in `Modal` is the foundation — it already handles Android back-button (`onRequestClose`), the iOS focus trap (`accessibilityViewIsModal`), and cross-platform mount/unmount. PanResponder owns the swipe-down gesture; we explicitly chose it over `react-native-gesture-handler` (already a workspace dep but requires `<GestureHandlerRootView>` at the app root) and `@gorhom/bottom-sheet` (rejected by V1 scope locks). The result: ~250 lines, zero new deps, 19 new tests on top of the 30 existing for 49 mobile tests total.

### Added
- `apps/mobile/src/components/primitives/EditorialBottomSheet.tsx` — `Modal(transparent) → backdrop Pressable + Animated.View sheet`. Sheet height = `Dimensions.window.height * heightFraction` (default 0.7). Backdrop tint is `theme.colors.text + '80'` (50% alpha) so it composes against both Conservatory cream and Midnight forest. Sheet uses `theme.colors.surface` for fill, `theme.colors.stroke` for the 36×4 drag-handle bar. `accessibilityRole='dialog'` + `accessibilityLabel` (default `'Bottom sheet'`) + `accessibilityViewIsModal=true` make VoiceOver / TalkBack announce the dialog and trap focus inside it. Android back button → `Modal.onRequestClose` → `onDismiss`.
- `shouldDismissOnDragRelease({ dy, sheetHeight })` — pure helper extracted so the threshold logic is unit-testable without PanResponder simulation. Threshold = `max(100px, sheetHeight * 0.3)`. The percentage rule prevents premature dismissal on tall sheets where 100px is nothing; the absolute floor handles small sheets where 30% of the height is too sensitive.
- Reduce-motion compliance via inline `AccessibilityInfo.isReduceMotionEnabled()` + `'reduceMotionChanged'` subscription. When on, `Modal.animationType` flips from `'slide'` to `'none'` (no slide-in transition), and the spring snap-back on a sub-threshold drag is replaced with an instant `setValue(0)`. Inlined the 5 lines rather than depending on E2-004's `useReduceMotion()` which hadn't merged at the time of E2-011.
- `apps/mobile/src/components/primitives/__tests__/EditorialBottomSheet.test.tsx` — 19 tests using `@testing-library/react-native`. Coverage: open/closed visibility, backdrop press → onDismiss, Modal.onRequestClose → onDismiss (Android back), tap-on-content does NOT propagate to backdrop, accessibilityLabel pass-through + default, accessibilityViewIsModal=true, accessibilityRole='dialog', heightFraction reflected in style, light/dark token usage, animationType slide vs none, drag-handle hidden from a11y tree, and the dismiss-threshold helper exhaustively tested for the 100px floor / 30% rule / upward / zero / negative drag cases.
- `apps/mobile/src/components/primitives/index.ts` barrel.

### Adversarial review (codex)
- **P2** — original implementation attached PanResponder to the entire sheet wrapper, which would steal vertical scroll gestures from any nested `ScrollView` / `FlatList` inside the sheet. Fixed by binding the PanResponder ONLY to a top "drag-zone" band that wraps the handle, with a generous `hitSlop` to keep the touch target ≥44px. Children render below the drag-zone in a sibling layout slot — their gestures are untouched. The test now explicitly asserts the panResponder lives on the drag-zone and is absent from the sheet wrapper.
- **P3 (addressed proactively)** — codex noted that the PanResponder closes over `sheetHeight`, `reduceMotion`, and `onDismiss` at mount, which would go stale if those props change while the sheet is open (system reduce-motion toggle is a real path). Wrapped the captured values in a `latestRef` that re-points every render — the responder identity stays stable (good for `.panHandlers` spread) but the release closure reads fresh values. Tests stay green.
- **Rejected** — `@gorhom/bottom-sheet` (V1 dep-light scope lock). `react-native-gesture-handler`-based gesture (already a dep but requires `GestureHandlerRootView` at app root, which we haven't added). `react-native-reanimated` (not a dep). Global Portal / Provider (Modal handles platform mount).

### Notes
- Drag-zone gesture-only-on-handle decision: the standard bottom-sheet UX is "drag the handle to dismiss"; tapping the handle is a no-op. This is what consumers expect and what keeps nested scrollables alive. If a future ticket genuinely needs full-sheet drag-to-dismiss with composed scroll behavior, switch to `react-native-gesture-handler`'s `Gesture.Native()` composition at that point.
- Backdrop colour formula `theme.colors.text + '80'`: hex two-digit alpha 80 = decimal 128 ≈ 50% opacity. In light mode (text=#2A2A2A) this gives a near-black scrim; in dark mode (text=#FAF6EE) it gives a cream-tinted scrim that reads correctly against the deep-forest bg. Tested via the dark-theme assertion in the suite.
- React 19 `act()` warning fires from the post-mount `isReduceMotionEnabled()` promise resolution; an `afterEach` that drains microtasks inside `act()` keeps subsequent tests clean. The warning text in the first test's stream is cosmetic — all 49 tests pass.
- `accessibilityRole={'dialog' as 'none'}` cast: the legacy `Animated.View` types omit `'dialog'` from their `AccessibilityRole` union (the modern `types_generated/` definitions include it). Cast through to keep the runtime contract correct without widening the prop type.

## [0.1.14.0] - 2026-05-04

E2-010 — `<HeroPhoto>` primitive. The editorial photo block used on A-2 (full-bleed plant detail hero), A-3 (camera result), and A-4 (add plant confirmation). One primitive, one rounded-corner contract, one cream-skeleton-while-loading behavior. Built on RN's legacy `Image` (no `expo-image` — V1 ships lean; FileSystem-stored photos don't need network caching or progressive JPEGs). The accessibilityLabel is required at the TypeScript prop level so callers can't ship an unlabeled hero photo, even though RN's underlying `Image.accessibilityLabel` is optional. 13 component tests pass; 43 mobile tests total.

### Added
- `apps/mobile/src/components/primitives/HeroPhoto.tsx` — the primitive. Props: `source` (RN `ImageSourcePropType` — `{uri}` for FileSystem photos, `number` for required() static assets), `aspectRatio?` (default 1; A-2 overrides for full-bleed), `rounded?` (`true`→24, `false`→0, `number`→explicit radius; default `true`), `accessibilityLabel` (REQUIRED), `testID?`, `onLoad?`, `onError?`. Uses `useTheme()` for the cream skeleton color (light: `#FAF6EE`, dark: `#1F3826` forest).
- `apps/mobile/src/components/primitives/index.ts` — barrel.
- `apps/mobile/src/components/primitives/__tests__/HeroPhoto.test.tsx` — 13 tests covering default radius (24), `rounded={false}` → 0, numeric `rounded` passthrough, default aspectRatio (1), custom aspectRatio (4/3), accessibilityLabel forwarded to wrapper View, accessibilityRole='image' on wrapper, source object passed through to Image unchanged, onLoad fires through, onError fires + Image is unmounted (silent cream rect fallback), a11y identity preserved on the wrapper after error so screen readers still announce, dark-mode skeleton uses Midnight surface token, light-mode skeleton uses cream surface token.

### Notes
- **Why a wrapping `View` with `overflow: 'hidden'`:** documented Android RN gotcha — direct `borderRadius` on `<Image>` does not consistently clip the underlying bitmap on Android across all `resizeMode` + image-format combinations. A View wrapper with `overflow:'hidden'` makes the rounded shape behave identically across iOS and Android with one visual contract. Codex review confirmed this pattern.
- **Silent-failure design:** on `onError`, the Image is unmounted and the cream-rect wrapper stays. No broken-image icon, no placeholder text, no spinner. The accessibilityLabel still rides on the wrapper so VoiceOver/TalkBack announces "image, Photo of …" rather than going silent — the cream rectangle is the announced element. This preserves the editorial voice (anti-AI-slop: no generic-app polish noise).
- **Single accessibility identity:** the wrapper View owns `accessible`, `accessibilityRole='image'`, and `accessibilityLabel`. The inner Image carries `accessible={false}` and `importantForAccessibility='no-hide-descendants'` so screen readers announce once, not twice.
- **No `expo-image`:** V1 scope lock. FileSystem photos are local — no network round-trip, no caching benefit. expo-image's progressive JPEG and disk cache become valuable post-V1 if remote photos enter the picture; for now the dep cost isn't earned.
- **No spinner overlay, no progressive loading, no caching layer:** all V1 scope locks. The cream skeleton (visible until `onLoad`) is the editorial-voice loading state.

### Adversarial review (codex)
- One round of `codex exec` adversarial review against the 6 documented risk surfaces (TypeScript-required label vs RN's optional underlying type, iOS+Android borderRadius clipping, skeleton flash on slow devices, large-image RAM teardown, silent error-state a11y, general implementation defects). **No P1 or P2 findings.** Codex confirmed: the prop-layer enforcement of accessibilityLabel works, the `overflow:'hidden'` wrapper is the correct cross-platform clipping pattern, and the cream-rect error state remains the accessible element rather than going silent.

## [0.1.13.0] - 2026-05-04

E2-009 — `<FAB>` primitive. The forest-circle floating action button anchored to the bottom-right of the Plants list (A-1). Tap launches the camera in identify mode; long-press opens the "Add a plant / Quick diagnose" popover wired in E3-004. This PR ships only the primitive (the circle, the press handlers, the a11y plumbing). 56×56 (Material default — exceeds the 44×44 a11y minimum, no `hitSlop` needed). Forest fill (`theme.colors.text`) with cream "+" glyph (`theme.colors.surface`); cross-platform shadow (iOS `shadow*` + Android `elevation: 3`); pressed state is opacity 0.85, no Reanimated, no scale transform — V1 lock against tween dependencies. 15 component tests added — total mobile suite is now 45.

### Added
- `apps/mobile/src/components/primitives/FAB.tsx` — `Pressable`-backed circle with `onPress` (required) + optional `onLongPress`. Default icon is a Fraunces "+" via RN `Text` (chosen over SVG because `react-native-svg` isn't a dep yet; "+" reads correctly even before Fraunces finishes loading because system "+" has equivalent glyph metrics). `icon?: ReactNode` slot lets callers swap in an SVG once `react-native-svg` lands. Positioning is the parent's responsibility — the primitive does not absolutely position itself, so it composes cleanly inside any layout.
- `apps/mobile/src/components/primitives/index.ts` — barrel.
- `apps/mobile/src/components/primitives/__tests__/FAB.test.tsx` — 15 tests: 56×56 dimensions, tap fires `onPress`, long-press fires `onLongPress` (and does NOT also fire `onPress` — the tap-vs-hold race), `accessibilityActions` absence/presence keyed off `onLongPress`, `onAccessibilityAction` routes the `'longpress'` action to `onLongPress` (Android TalkBack actions-menu fallback), `disabled` blocks every path including the accessibility-action path, default + custom `accessibilityLabel`, conditional `accessibilityHint`, dark-mode `backgroundColor` resolves to `darkTheme.colors.text`, custom `icon` prop replaces the default, idle render has no opacity override + the pressed-state contract is opacity 0.85 (no transform).

### A11y notes
- `accessibilityRole='button'`, `accessibilityLabel` defaults to `'Add a plant'`, `accessibilityHint` defaults to `'Long-press for quick diagnose'` ONLY when `onLongPress` is provided — announcing a long-press affordance that does nothing would be worse than silence.
- The `'longpress'` accessibility action is documented by RN as the Android TalkBack local-context-menu surface. iOS VoiceOver does not surface it as a discoverable rotor action; the iOS-side affordance is the hint announcement, and the E3-004 popover provides the accessible Quick Diagnose alternative path. Documented in the file header.
- 56×56 is the Material FAB default and exceeds the 44×44 a11y minimum, so `hitSlop` is not needed.

### Adversarial review (codex)
One round of `codex exec` adversarial review caught two P2 issues before ship:
- **P2** — `disabled` initially only gated the touch path. The accessibility-action path was still wired whenever `onLongPress` existed, meaning a TalkBack user could invoke the action via the actions menu while the visual control was gated off. Fixed: `accessibilityActions` and `onAccessibilityAction` are now also gated on `!disabled`. Test added.
- **P2** — comment originally implied VoiceOver could surface the `'longpress'` action as a screen-reader fallback. RN docs document this as Android-only (TalkBack actions menu); iOS VoiceOver has no equivalent rotor action. Comment + ticket footer updated to be accurate; the iOS-side affordance is the hint announcement, and the accessible Quick Diagnose path lands in E3-004's popover composition.
- **No P1s.** Tap-vs-hold race, 44×44 target size, cross-platform shadow, and conditional hint behavior all check out. Suggestions to add a draggable FAB / Reanimated press animation / speed-dial menu / replace `Pressable` with `TouchableHighlight` were rejected per V1 scope locks.

### Notes
- `fabStyles` is exported alongside `FAB` so the pressed-state contract (opacity 0.85, no transform) can be asserted directly. RN's `Pressable` resolves its function-style on render with `pressed: false`, so structurally inspecting the rule is the cleaner contract test than driving the press state through events that the test renderer doesn't propagate cleanly.
- The default `delayLongPress` (RN's 500ms) is preserved — no override. Faster delays misfire on slow taps; slower ones make the long-press feel unresponsive.

## [0.1.12.0] - 2026-05-04

E2-008 — `<EditorialButton>` primitive. The only button vocabulary in V1: outline (cream surface + hairline forest border + Fraunces forest label) and filled (forest fill + cream Fraunces label). Used on A-2's "Mark watered" / "Add note", A-3's "Save to garden" / "Done", A-4's "Add to garden", and the weekly review's "Got it." CTA. Built on RN's `Pressable` (the modern API; no TouchableOpacity). Tokens invert correctly between Conservatory (light) and Midnight (dark) by reading `theme.colors.surface`/`theme.colors.text` — no per-variant dark-mode branch, the token table in `DESIGN.md` is the source of truth and `darkTheme` already inverts surface/text.

### Added
- `apps/mobile/src/components/primitives/EditorialButton.tsx` — typed `EditorialButtonProps` with `variant`, `label`, `onPress`, optional `disabled`, `loading`, `accessibilityLabel`, `accessibilityHint`, `testID`. Border radius 8, padding 12v × 24h, Fraunces 600 semibold @ 16/20. `accessibilityRole='button'` always; `accessibilityState={{ disabled: disabled||loading, busy: loading }}` so VoiceOver/TalkBack announce the loading + disabled states correctly. Loading state renders `ActivityIndicator` colored to the button's foreground token instead of the label and disables `onPress`.
- `apps/mobile/src/components/primitives/index.ts` barrel.
- `apps/mobile/src/components/primitives/__tests__/EditorialButton.test.tsx` — 13 tests (43 total in the mobile suite). Coverage: outline + filled token resolution, onPress fires on tap, disabled + loading both block onPress, ActivityIndicator replaces the label while loading, accessibilityState surfaces both `disabled` and `busy`, hitSlop math reaches the 44px floor, accessibilityLabel defaults to label and accepts override, dark-mode token inversion via mocked `useTheme(darkTheme)`, testID forwarding, same-render double-tap latch (codex P2 — see below), and post-loading-flip press lockout.

### Adversarial review (codex)
One round of `codex exec` adversarial review found one issue before ship:
- **P2** — same-render double-tap race: two synchronous presses fired before the parent could flip `loading=true` would both invoke `onPress`, double-submitting mutations (e.g. two `watering_events` rows for one tap). Fixed via an internal `useRef` latch that drops every press after the first within a single tick; the latch clears on the next macrotask so the button stays responsive once the parent's `loading` state has settled. Test exercises both the synchronous double-tap drop and the post-clear recovery using `jest.useFakeTimers()`.
- **No P1s.** Token inversion path, Pressable's pressed-state callback typing, both `disabled` and `accessibilityState.disabled` being set, and the 44px hit-target math all checked clean. Font-loading is gated at the app shell (`app/_layout.tsx` already blocks render until `useFonts()` resolves), so the button doesn't need an independent fallback branch.

### Notes
- Hit target math: padding 12v + Fraunces line height 20 + padding 12v = 44px visual height; horizontal padding 24 × 2 = 48 + label width comfortably exceeds 44 on every real-copy CTA. `HIT_SLOP_V` is computed as `ceil((44 - (paddingV*2 + fontSize)) / 2)` so any future tighter padding (e.g. dropping line height to 18) keeps the effective target at 44 by widening hitSlop. With current values that resolves to 2 top + 2 bottom = a 48px effective touch area on the vertical axis. Test asserts `minHeight + top + bottom ≥ 44`.
- The double-tap latch lives inside the button rather than in every parent screen so the contract is "calling `onPress` is at-most-once-per-tick by construction." Parent screens still own the `loading=true` flip on the result of `onPress` to handle the slower async path; the latch only handles the synchronous burst.
- Pressable's `style` callback type (`StyleProp<ViewStyle> | ((state: PressableStateCallbackType) => StyleProp<ViewStyle>)`) is used directly — annotating the parameter with `PressableProps['style']` would lose the `{ pressed }` narrowing, so we let the function-form callback infer naturally.
- V1 scope locks held: no styled-components, no nativewind, no `<ThemeProvider>`, no additional variants beyond outline + filled, no leftIcon/rightIcon, no ghost or destructive variant. Pressable stays (TouchableOpacity is RN's legacy API).

## [0.1.11.0] - 2026-05-04

E2-006 — the three icon primitives that anchor PlantCare's visual vocabulary: `<Droplet>` (the watering metaphor, used by both the WATER TODAY chip on A-1/A-2 and the 7-day ledger row on A-2/A-6), `<LeafIcon>` (the SKIP-state metaphor on A-1/A-2), and `<HandOnSoilIcon>` (the CHECK-SOIL metaphor). All three are line-drawn deep-forest-on-cream — the "botanical field guide" treatment from `DESIGN.md` that's the load-bearing anti-AI-slop signal for this product. Implemented with `react-native-svg` rather than an icon font: V1 ships custom shapes that match the approved mockups exactly, not a re-skin of someone else's icon set.

### Added
- `apps/mobile/src/components/primitives/Droplet.tsx` — `<Droplet filled?: boolean; size?: number; testID?: string; accessibilityLabel?: string />`. Default size 16. Single classic teardrop path on a 24-unit canvas. `filled=true` paints the pale-blue `water` token (a watered day on the ledger / the active WATER TODAY chip); `filled=false` paints the `surface` token (cream in light, forest in dark) for outline-only droplets — same shape, different fill, one icon vocabulary.
- `apps/mobile/src/components/primitives/LeafIcon.tsx` — `<LeafIcon size?: number; testID?: string; accessibilityLabel?: string />`. Default size 14. Asymmetric leaf outline + central vein (the curve on the right side is fuller than the left — botanical, not heraldic). Always outline-only; the SKIP chip wrapper carries the sage accent.
- `apps/mobile/src/components/primitives/HandOnSoilIcon.tsx` — `<HandOnSoilIcon size?: number; testID?: string; accessibilityLabel?: string />`. Default size 14. Open palm + four short finger strokes above a shallow soil mound. At 14px the palm + soil pairing carries silhouette legibility; the fingers register as texture rather than countable digits.
- `apps/mobile/src/components/primitives/index.ts` — barrel export for the three primitives + their prop types. Future E2-007/008/009 primitives will land alongside.
- `apps/mobile/src/components/primitives/__tests__/icons.test.tsx` — 15 tests across the three icons. Mocks `useTheme()` per-suite (matching the `src/hooks/__tests__/useTheme.test.tsx` pattern), asserts default + custom sizes, verifies `filled` flips the Droplet's fill between `water` and `surface` tokens, asserts `strokeWidth=1` is preserved at every state, asserts the dark-mode case (mock returns `darkTheme`, fill payload swaps from cream to forest with no code path change), and verifies `accessibilityLabel` forwards to the Svg root on every primitive. Total mobile tests: 45.
- `react-native-svg@15.15.3` — pinned exact to match Expo SDK 55's bundled native module rather than allowing patch drift via `^`. SDK-bundled native modules can behave differently in EAS dev clients vs Expo Go when the JS package version drifts from the pinned native one.

### Notes
- All token reads go through `useTheme()` at render time, so a system-theme flip from Conservatory → Midnight reskins every icon with no per-primitive dark-mode code. The fill on outline-only icons resolves to `theme.colors.surface` (cream in light, forest in dark) and the hairline resolves to `theme.colors.stroke` (forest in light, cream in dark) — the same Path nodes, mirrored treatment.
- Default sizes (16 for Droplet, 14 for Leaf and HandOnSoil) match the approved A-1/A-2 mockups: Droplet anchors the WATER TODAY chip and ledger row, the leaf and hand sit slightly tighter inside the SKIP and CHECK SOIL chips. Callers needing a different density (the watering ledger may bump Droplet to 20+ at a future iteration) pass `size`.
- `strokeWidth=1` is the hairline weight from the design system. At very small sizes on lower-DPR devices the hairline can fade slightly; the 14-16px defaults sit safely above that threshold and the chip wrapper carries semantic legibility independent of stroke contrast.
- Path constants live at module top, not inlined in JSX. Design QA can read the path data directly without grepping JSX.
- No `<ThemeProvider>` introduced — `useTheme()` reads `useColorScheme()` directly, consistent with the E2-001 lock.
- No icon font, no Lottie, no animation. V1 ships static SVG line drawings, full stop. Animation lives in E2-004 (`useReduceMotion()`); these primitives don't move.
- Codex adversarial review surfaced one P2 (version pin) which was addressed by pinning `react-native-svg` exactly to 15.15.3 instead of `^15.15.4`. No P1s.

## [0.1.10.0] - 2026-05-04

E2-005 — API client + `ApiResult<T>` discriminated union. Third mobile-foundation ticket of Epic E2. Locks the single transport surface every screen will call into for the four LLM proxy endpoints (identify, diagnose, consult, review). The reason this PR is short on lines but long on commentary is the kind enumeration: `'network' | 'timeout' | 'server' | 'layer1_reject' | 'low_confidence' | 'parse_error' | 'queued'`. Seven kinds, no `'unknown'` fallthrough. Each one drives a different copy block in the A-3 / A-5 error states; collapsing two of them would force the screen layer to re-derive the distinction from `message` strings, which is exactly the per-screen drift the master plan calls out (lines 682-705). Same rationale as the E1-003 `ConsultResponse` fork — when the parser, the router, the route handler, and the screen all need to switch on the same dimension, the dimension belongs in the type. 29 new client tests, 59 mobile tests total.

### Added
- `apps/mobile/src/api/types.ts` — locked `ApiResult<T>` discriminated union, plus the request types `IdentifyRequest`, `DiagnoseRequest`, `ConsultRequest` (= `ConsultRequestBody`), `ReviewRequest` (= `ReviewRequestBody`), and re-exports of the four response shapes from `@plantcare/api-types`. Two `ApiResult<T>` types coexist intentionally: the wire one in `@plantcare/api-types` (kinds: `success | queued | error | rate_limited | rejected_off_topic`) describes what the Cloudflare Worker emits as JSON; the mobile one in this file (kinds: the locked seven) describes what every screen sees after the client classifies network conditions and normalizes the response. Each layer owns the concerns it can observe — the backend can't know about TCP errors; the client can't know about server-side off-topic detection. The mapping happens once, in `client.ts`, and is the only place the wire shape leaks.
- `apps/mobile/src/api/client.ts` — `createApiClient({ baseUrl, deviceId, fetch?, getDeviceId? })` returning `{ identify, diagnose, consult, review }`. Each method returns `Promise<ApiResult<T>>`. Identify + diagnose send `multipart/form-data` (Content-Type unset so the runtime appends the boundary token); consult + review send JSON. All requests include `X-Device-Id`. AbortController-based timeout with a default of 20s and per-call `timeoutMs` override; the `timedOut` flag is set inside an `onabort` handler synchronously when the timer fires, so distinguishing `kind: 'timeout'` from `kind: 'network'` doesn't depend on `AbortError` instanceof checks (RN's fetch is inconsistent on that). No axios — RN ships fetch, FormData, AbortController; a transport library buys nothing and adds bundle weight + a version surface that drifts.
- `apps/mobile/src/api/index.ts` — barrel.
- `apps/mobile/src/api/__tests__/client.test.ts` — 29 tests covering: 2xx success passthrough, X-Device-Id header on every request, fetch TypeError → `network`, hang past timeout → `timeout` (and that fetch is actually aborted, not just resolved-as-timeout), per-call `timeoutMs` override, 500 → `server`, 429 with Retry-After numeric → `server` + `retry_after`, 429 with HTTP-date Retry-After → seconds-until-then, 200 with non-JSON body → `parse_error`, 200 with JSON that isn't an `ApiResult` shape → `parse_error`, empty 4xx body → `server`, multipart construction asserting structural Blob equivalence (Node's FormData wraps Blob into File so reference equality breaks), JSON body construction with and without `plant_context` for consult, JSON body construction for review with the week summary + plants array, backend `rejected_off_topic` → client `layer1_reject`, passthrough of backend bodies that already speak client kinds (`layer1_reject`, `low_confidence`, `parse_error`, `queued`), backend `kind: 'queued'` (ok:true wire) → client `kind: 'queued'` (ok:false), DI for fetch + getDeviceId, construction throws when neither deviceId nor getDeviceId is provided, runtime throws when getDeviceId resolves empty (programming error, not a soft network kind), trailing-slash baseUrl normalization, and the codex-fix regression tests called out below.

### Backend wire mapping (the only place the two `ApiResult` shapes meet)
The classifier in `client.ts` follows status-code precedence: 429 → `server` + `retry_after` (header wins, body's `retry_after_seconds` as fallback); 5xx → `server`; 4xx → trust the body's `kind` if it's already a client kind, otherwise map known backend kinds (`rate_limited`/`rejected_off_topic`/`error`), otherwise `server` (unstructured 4xx is deploy skew, not a malformed user request); 2xx → `{ ok: true, data }` for `kind: 'success'`, `{ ok: false, kind: 'queued' }` for `kind: 'queued'`, passthrough for already-client-kind bodies, otherwise `parse_error`. Backend's `kind: 'rejected_off_topic'` (off-topic consult notes today, future Layer-1 image gate) maps to client `kind: 'layer1_reject'` — same semantics, different name.

### Adversarial review (codex)
First-pass codex review surfaced three P2 findings, all addressed before ship:
- **P2** — `getDeviceId()` was awaited *before* the AbortController timer started, so a hung SQLite read could bypass `timeoutMs` entirely. Moved device-id resolution inside the timeout budget; added a regression test that resolves `getDeviceId` slower than `defaultTimeoutMs` and asserts `kind: 'timeout'`.
- **P2** — backend `kind: 'error'` (used by `_imageUpload.ts`, `_consultRequest.ts`, `_reviewRequest.ts` for `missing_image`, `invalid_json`, `service_unconfigured`, `missing_or_invalid_device_id`) was being mapped to client `kind: 'parse_error'`. These are real server-side rejections, not malformed responses. Remapped to `kind: 'server'` so the screen renders "we'll be back" copy instead of "garbled response" copy.
- **P2** — 429 responses dropped `retry_after_seconds` from the body when the `Retry-After` header was absent. The header is RFC-authoritative, but the wire-level `rate_limited` shape carries the field in the body for callers that can't read headers. Now: header preferred when both are present, body field as fallback when only the body has it.

Second-pass codex review came back clean — no new P1 or P2 findings. Type alignment with `packages/api-types/src/index.ts` verified.

### Rejected reviewer suggestions
- Collapse `'network'` and `'timeout'` into a single `'network_error'` kind. Locked per master plan. They differ in user intent (timeout = request reached the lab; network = couldn't reach it), in retry strategy (immediate vs check-connection-first), and in the copy the A-3 error states render. The screens forking on the difference is the whole reason the dimension is in the type.
- Drop `'queued'` because E7 (sync queue) hasn't shipped. Locked. Adding the variant later would force every screen's switch statement to grow simultaneously — a breaking change masquerading as an additive one. Locking the type now means E7 is purely additive on the runtime side.
- Add an `'unknown'` / `'other'` fallback kind. Defeats the union: any new failure mode would ship silently as "unknown" rather than forcing a structured response. Every fetch path lands in exactly one of the seven kinds, and the classifier has no fall-through arm.
- Replace fetch with axios. RN ships fetch + FormData + AbortController; the only thing axios buys is interceptors, which we don't want at this layer (retry/backoff is E7's job per V1 scope locks).

### Notes
- `'low_confidence'` is in the locked union but not generated by the client today. The current free→paid router on the backend hides low-confidence behind a degraded-fallback `data` payload (`source: 'free_failed'`, `species_slug: 'unknown'`); the kind is reserved for a future server-side gate that surfaces low confidence at the wire boundary. Locking it now means the screen switch already has the case when that gate ships.
- `'queued'` is similarly inert in this PR. The client surface accepts it from both the wire (forward-compat with a backend that adopts the client union) and from a future E7 sync drainer that short-circuits fetches when offline. The `200 + ok:true + kind:'queued'` wire shape (per `@plantcare/api-types`) is mapped to client `ok:false + kind:'queued'` because the screen needs to know "no data yet" without parsing a `queue_id`.
- Retry-After parsing supports both numeric delta-seconds and HTTP-date format per RFC 7231. HTTP-date is parsed via `Date.parse` and clamped at 0 (no negative retries) — covers backends that emit `"Retry-After: Tue, 04 May 2026 16:00:00 GMT"` instead of `"Retry-After: 30"`.
- No retry / no backoff in the client itself — that's E7's contract. The client surfaces `kind` faithfully and exits. Same for request signing / hmac / auth headers beyond `X-Device-Id` — V1 scope locks rule them out.

## [0.1.9.0] - 2026-05-04

E2-004 — `useReduceMotion()` hook. Third mobile-foundation ticket of Epic E2 (S size, depends only on E0-001). Reads the OS-level Reduce Motion preference via `AccessibilityInfo` and re-renders when the user toggles it in Settings. This is the seam the A-3 diagnose loading state will use to swap the 3-bucket pulsing copy (0-8s / 8-20s / 20s+) for a static line, and the gate every future transition/animation in the app will call before opting in to motion. 8 new mobile tests, 38 total.

### Added
- `apps/mobile/src/hooks/useReduceMotion.ts` — `useReduceMotion(): boolean`. Reads the initial value via `AccessibilityInfo.isReduceMotionEnabled()` (Promise<boolean>), subscribes to `'reduceMotionChanged'` for live updates, and removes the subscription on unmount. State defaults to `false` during the brief async window before the initial value resolves — matches the iOS factory default and is the harmless biased-toward-motion fallback per the master plan. A `mounted` flag guards the resolve path so an unmount mid-flight doesn't trigger a setState-on-unmounted warning. Promise rejection falls through to the `false` default rather than going unhandled, so a platform that fails to report a preference still produces a clean log.
- `apps/mobile/src/hooks/__tests__/useReduceMotion.test.tsx` — 8 tests against a `jest.mock('react-native', …)` of `AccessibilityInfo`, mirroring the `useColorScheme` mock pattern in `useTheme.test.tsx`. Coverage: default `false` before the async fetch resolves, `true` resolution propagates, `false` resolution stays put, listener flip true and back to false, subscription `.remove()` called exactly once on unmount, rejected initial fetch falls through to `false` with no console.error noise, late resolution after unmount doesn't fire setState (asserted via `console.error` spy — the warning React emits when this regresses).
- `apps/mobile/src/hooks/index.ts` — exports the new hook alongside `useTheme`.

### Adversarial review (codex)
- No P1/P2 findings. Codex verified the cleanup contract against `react-native@0.83.6` (the version pinned in `apps/mobile/package.json`): `AccessibilityInfo.addEventListener()` returns an `EmitterSubscription` with `.remove()`, so the destructor in the effect matches the runtime. The mounted-flag guard blocks the post-unmount-setState race. The `false` initial default and the lack of re-render churn (state only flips when the boolean actually changes) both stand.

### Notes
- No `Animated` polyfill, no global motion-context provider, no Storybook — V1 scope locks. Consumers will read `useReduceMotion()` directly at the call site (the diagnose loading component is the first consumer in E5-007).
- The `'reduceMotionChanged'` event name is the RN-stable identifier — same on iOS and Android. No platform branching needed.

## [0.1.8.0] - 2026-05-04

E2-003 — `usePlants()` CRUD hook. Third mobile-foundation ticket of Epic E2. With the schema landed in v0.1.7.0, the next thing every screen needs is a parameterized read/write surface over the `plants` table. This PR ships a stable callbacks object — `list` / `getById` / `create` / `update` / `archive` / `unarchive` / `remove` — backed by raw `expo-sqlite` (no ORM, per V1 scope lock). Two non-obvious decisions: (1) the data API is split into `createPlantsApi(executor)` so tests drive the same SQL bytes through a `better-sqlite3` adapter without booting Jest's RN environment, mirroring the migration test pattern; (2) `create()` and `update()` use `INSERT ... RETURNING` / `UPDATE ... RETURNING` rather than a write+read pair, so the returned row is by-definition the row produced by THIS write — codex's adversarial review caught the racey UPDATE-then-SELECT shape and the fix was a single-statement collapse. 53 mobile tests now passing (23 new for plants).

### Added
- `apps/mobile/src/db/types.ts` — `Plant` (post-marshalling, `is_indoor: boolean`), `PlantRow` (raw SQLite shape, `is_indoor: number`), `CreatePlantInput`, `UpdatePlantPatch`. snake_case throughout to mirror `schema.ts` exactly so a typo on either side is a type error rather than a silent column-not-found at runtime. `species_label` / `location` / `identify_confidence` are surfaced because they're already in the canonical schema even though the ticket spec only listed the trimmed column set; the type matches the table, not the spec.
- `apps/mobile/src/hooks/usePlants.ts` — `createPlantsApi(executor)` is the pure-data surface; `usePlants()` is a one-line `useMemo` over it that resolves the executor lazily via `openDb()`. Every value is bound (`?` placeholders), never interpolated. `update()`'s dynamic SET-fragment list is built from a closed allow-list (`UPDATE_COLUMNS`) so a hostile or mistyped patch key throws before any SQL is built. Boolean `is_indoor` marshals to `0|1` on write and back to `boolean` on read. `archive` / `unarchive` are the only entry points to the soft-delete state machine — `archived_at` isn't patchable via `update()`.
- `apps/mobile/src/hooks/__tests__/usePlants.test.tsx` — 23 tests against a `better-sqlite3`-backed `PlantsExecutor` adapter (same backend the migration tests use, chosen because CI runs on Node 20). Coverage: create round-trip + DB row matches returned Plant, `is_indoor` defaults to true → 1, `is_indoor=false` round-trips through `false ↔ 0`, `override_interval_days` persists when set + null when omitted, UUID auto-generation + caller-supplied id, empty species_slug rejected, list excludes archived by default, list({includeArchived: true}) returns all, list orders by `added_at DESC`, getById null on missing + round-trips on present, update returns the patched row + DB matches, update toggles `is_indoor` with INTEGER marshalling, update throws on unknown patch keys, update throws on missing id, empty patch is no-op re-read, archive sets `archived_at` + list excludes the row, unarchive clears `archived_at`, remove triggers FK CASCADE on `watering_events` (insert two events, remove plant, assert events gone), parameter binding defends against hostile id strings (`' OR '1'='1` no-ops), boolean round-trip across all four entry points, `PlantRow.is_indoor` is numeric per the type, plus the codex-P2 race-safety regression: a wrapping executor fires a hostile UPDATE between this caller's UPDATE and any subsequent SELECT — the test asserts the returned row reflects THIS caller's intent, structurally locking in `RETURNING` over a future read-after-write regression.
- Hook + db barrels updated to export the new public surface (`usePlants`, `createPlantsApi`, `Plant`, `PlantRow`, `CreatePlantInput`, `UpdatePlantPatch`, `PlantsExecutor`, `SqlBindValue`).

### Adversarial review (codex)
One round of `codex exec` adversarial review caught one issue before ship; a second round confirmed the fix.
- **P2** — `update()` was UPDATE-then-SELECT, not atomic: a concurrent `update()` on the same row could land between the two awaits and the returned `Plant` would reflect the other caller's patch instead of this one's. Fixed by switching `create()` and `update()` to single-statement `INSERT/UPDATE ... RETURNING` (SQLite has supported `RETURNING` since 3.35; both expo-sqlite's bundled SQLite and the better-sqlite3 test backend ship newer). Added a regression test that interleaves a hostile UPDATE through a wrapping executor and asserts the returned row still reflects this caller's intent — if the implementation ever regresses to UPDATE-then-SELECT the assertion fails.
- **Rejected** — none. V1 scope locks held: no Drizzle/Prisma/Kysely (raw SQL is the master plan stance), no `<ThemeProvider>`-style DB context (`openDb()`'s module memoization is the concurrency contract), no pulling forward E7 sync_queue logic into `usePlants` (E2-003 is plants-only).

### Notes
- The ticket spec listed `created_at` for the plants column; the canonical schema (shipped in E2-002) uses `added_at`. The type matches the schema. The watering engine still does millisecond math on `added_at` for E4-002 timezone-correctness.
- `crypto.randomUUID()` resolves through `globalThis.crypto` so the same code path works on Hermes (RN 0.74+) and Node 20+ (the test backend). If a target without it ever appears, swap to `expo-crypto`'s `randomUUID()`.
- `update()` re-reads via a plain SELECT only on the empty-patch path — there's no write to race against, so the read-only path can't return another caller's intent. The hot path (non-empty patch) is single-statement.
- `usePlants()`'s `useMemo` dep array is empty: `openDb()` is module-memoized, so the executor identity is stable across renders and components. Two simultaneous `usePlants()` calls share the same connection, and expo-sqlite serializes statements per its native module contract — no extra coordination needed at this layer.

## [0.1.7.0] - 2026-05-04

E2-002 — SQLite schema + migrations. Second mobile-foundation ticket of Epic E2. Local SQLite via `expo-sqlite` is the V1 storage layer — no cloud sync, no ORM, raw SQL. This PR ships all six tables (`plants`, `watering_events`, `photos`, `notes`, `diagnoses`, `sync_queue`), every index from the master plan plus two cascade-companion indexes surfaced in adversarial review, a versioned migration runner using SQLite's built-in `PRAGMA user_version`, and an `openDb()` that sets `PRAGMA foreign_keys = ON` at connection time so the FK constraints actually enforce. 30 mobile tests now passing (15 new for db).

### Added
- `apps/mobile/src/db/schema.ts` — single-source-of-truth SQL string for the V1 schema. Includes the WORKBACK additions on `plants` (`is_indoor INTEGER NOT NULL DEFAULT 1` with a `CHECK (is_indoor IN (0, 1))` bool guard, `override_interval_days INTEGER` nullable for the custom-watering-schedule toggle). All cascade FKs spelled out: `watering_events`, `photos`, `notes`, `diagnoses` ON DELETE CASCADE from `plants`; `diagnoses.photo_id` ON DELETE CASCADE from `photos`; `plants.hero_photo_id` ON DELETE SET NULL from `photos`. `sync_queue` is intentionally unconstrained (`ref_table` / `ref_id` are dynamic strings — the worker dispatches by table name).
- `apps/mobile/src/db/migrations.ts` — versioned migration runner. `runMigrations(db, migrations)` reads `PRAGMA user_version`, runs every pending migration in `version` order, each inside its own transaction so a partial failure rolls back both the schema mutation and the version bump. Validates the migration list at startup (positive integers, monotonic, no duplicates) — silently corrupted upgrade graphs are the worst kind of bug.
- `apps/mobile/src/db/db.ts` — `openDb()` opens `plantcare.db` via `expo-sqlite`'s `openDatabaseAsync`, runs `PRAGMA foreign_keys = ON` (SQLite default is OFF — without this, every FK constraint in the schema is inert), then runs migrations. Memoized: subsequent calls return the same `SQLiteDatabase` instance. Cache is dropped on failure so the next call retries instead of permanently returning a rejected promise.
- `apps/mobile/src/db/index.ts` — barrel.
- `apps/mobile/src/db/__tests__/migrations.test.ts` — 16 tests (30 total in the mobile suite). Drives the migration runner against a `better-sqlite3`-backed `SqlExecutor` adapter (chosen over `node:sqlite` because CI runs on Node 20 and `node:sqlite` is Node 22.5+). Coverage: all 6 tables created, all 8 indexes created, FK declarations match the contract per `PRAGMA foreign_key_list`, FK violation rejected when `foreign_keys = ON`, plant→watering_events and photo→diagnoses cascade deletes, `is_indoor` defaults to 1 + CHECK rejects out-of-range values, `idx_plants_active` and `idx_queue_drainable` partial indexes selected by the query planner via `EXPLAIN QUERY PLAN`, `idx_plants_hero_photo` selected for the photo-cascade lookup path, hero_photo_id correctly set to NULL on photo delete, `user_version` advances to target on first run and is no-op on re-open, transactional rollback when migration up() throws (verified via `user_version` stays at 0 + dropped table), and migration validation rejects duplicate / out-of-order / non-positive versions.
- `expo-sqlite@~15.2.0` runtime dep + `better-sqlite3@^12.9.0` dev dep (test backend only — the on-device build still goes through expo-sqlite).

### Adversarial review (codex)
Two rounds of `codex exec` adversarial review caught three issues before ship:
- **P1** — first-pass test imported `node:sqlite` which would have failed on CI's Node 20 runner. Switched to `better-sqlite3` (Node 20+ compatible). Local Node 25 hides this gap; CI doesn't.
- **P2** — missing `idx_diagnoses_photo` for the `diagnoses.photo_id REFERENCES photos(id) ON DELETE CASCADE` cascade lookup. Added.
- **P2** — missing `idx_plants_hero_photo` for the `plants.hero_photo_id REFERENCES photos(id) ON DELETE SET NULL` cascade lookup. Added as a partial index (`WHERE hero_photo_id IS NOT NULL`) since the SET NULL path only ever matches non-null rows.
- **Rejected** — codex's third-pass suggestion to remove `diagnoses.plant_id` as redundant with `photo_id -> photos.plant_id` violates V1 scope locks. The master plan explicitly defines both columns: `plant_id` is nullable for the Quick Diagnose flow where a photo isn't attached to a plant yet, and photos aren't reassigned between plants in V1 (no UI for that).

### Notes
- All timestamps are unix milliseconds (`INTEGER`). The watering engine does millisecond math, not calendar-day math, to survive DST flips and the international date line (E4-002 critical regression). INTEGER handles years 1970-2262 — well beyond any V1 user retention window.
- Soft-delete on `plants` uses `archived_at` rather than DELETE, so the `ON DELETE CASCADE` on watering_events / photos / notes / diagnoses doesn't wipe history when a plant is archived. The CASCADE only fires on the (currently uncommanded) hard delete path, where wiping history is the right behavior.
- `idx_plants_active` and `idx_queue_drainable` are partial indexes — they only contain the rows the app actually queries (`archived_at IS NULL` plants, `status = 'pending'` queue entries). Smaller than full indexes, and the planner picks them automatically when the query's WHERE clause matches the index predicate. EXPLAIN QUERY PLAN tests guard against accidental full-table scans.
- expo-sqlite's `SQLiteDatabase` class is structurally compatible with the `SqlExecutor` interface defined in `migrations.ts` — no cast needed at the `db.ts` call site. Tests exploit the same shape via a thin `better-sqlite3` adapter.

## [0.1.6.0] - 2026-05-04

E2-001 — `useTheme()` hook. First mobile-foundation ticket of Epic E2. Wraps `@plantcare/theme`'s already-shipped `lightTheme` (Conservatory) and `darkTheme` (Midnight Conservatory) constants in a one-line hook driven by RN's `useColorScheme()`. No `<ThemeProvider>` and no `useMemo` — module-level frozen constants give stable identity across renders, so RN re-renders consumers automatically when the OS appearance flips. Per the master plan: no manual theme override in V1; the OS decides.

### Added
- `apps/mobile/src/hooks/useTheme.ts` — `useColorScheme() === 'dark' ? darkTheme : lightTheme`. Anything else (`null`/`undefined` during cold start, `'unspecified'` on Android with no preference, `'light'`) falls through to light, matching the iOS factory default and DESIGN.md's cream-as-canonical-surface stance.
- `apps/mobile/src/hooks/__tests__/useTheme.test.tsx` — 7 tests: light → Conservatory, dark → Midnight, null/undefined/'unspecified' → light, reference stability across same-scheme renders, reference flip on scheme change. Mock widens RN's legacy `ColorSchemeName` (which incorrectly omits null in the public types) so the cold-start branches are exercised.
- `apps/mobile/src/hooks/index.ts` barrel.

### Notes
- The mock cast (`as unknown as jest.Mock<WidenedScheme, []>`) works around `react-native`'s legacy public-types entry declaring `useColorScheme(): ColorSchemeName` without `null | undefined`. The newer `types_generated/` entry is accurate, but the package's `"types"` field still resolves the legacy version. The runtime contract — null during cold start, undefined on background-resume — is real and tested.
- DOD criterion "<100ms theme switch via system setting" is structurally satisfied: RN re-renders `useColorScheme` consumers on `Appearance` change events, and the hook does no async work. Empirical measurement waits on a live mockup screen (E2-013 Component Garden).

## [0.1.5.0] - 2026-05-04

E1-005 — LLM eval harness. With all four endpoints (identify, diagnose, consult, review) sharing the tiered router, this PR adds the regression suite that catches contract drift on the parser + router state machine without burning OpenRouter quota on every push. Two-mode design: the default mock mode drives canned LLM JSON through the real router and asserts the parsed shape; a future EVAL_REAL_API=1 mode (scaffolded as `.todo`) will hit live OpenRouter and use the same fixtures as the ±10 confidence band baseline. The mock-mode bug-catchers are the snapshot files plus a per-endpoint `expectCallArgs` that validates the OpenRouter wire shape (kind, imageDataUrl/userMessage, system-prompt fragment, apiKey).

### Added
- `apps/backend/evals/` directory with 26 fixtures: identify (10 — 6 healthy + 2 partial + 1 low-light + 1 cat reject), diagnose (10 — 7 sick + 2 healthy + 1 cat reject), consult (5 — 4 plant-related + 1 off-topic reject), review (1 mixed-week with 6 plants). Each fixture pairs canonical LLM JSON with a baseline confidence used by the ±10 band assertion in real-API mode.
- `evals/eval-runner.ts` with `mockCalls(...outcomes)` (plays canned outcomes through `vi.fn<typeof callFree>()`), `assertConfidenceBand(name, actual, baseline)` (named drift errors that survive CI-log diffs), and `expectCallArgs(spy, i, expected)` — the silent-pass guard from the adversarial review. Argument-shape assertions catch regressions in `router.toCallArgs` (wrong `kind`, missing `imageDataUrl`, wrong system prompt, missing `apiKey`) that canned-response mocks would otherwise paper over.
- `apps/backend/vitest.eval.config.ts` — separate Node-pool config so evals run in 400ms without spinning up workerd. Default `vitest.config.ts` now scopes its workers-pool include to `test/**/*.test.ts` so the two suites stay separate.
- `bun run eval` and `bun run eval:watch` scripts on `@plantcare/backend`.
- Per-endpoint paid-escalation success tests for identify and diagnose (E1-005 adversarial review): free returns garbage, paid returns a parseable response, asserts `source='paid_escalated'` plus the parser shape contract on the paid arm. Without these, the paid-side parser could regress and every other eval would still pass.
- Aggregate snapshot per endpoint: parsed shape across all fixtures in a single `__snapshots__/*.snap` file, `latency_ms` stripped so timing nondeterminism doesn't pollute the diff. Surfaces parser drift in one visible block.
- Review-eval per-plant alignment assertion (`per_plant.length === request.plants.length`) — the catch from E1-004 adversarial review, now structurally enforced. The parser permits mismatched lengths; only the eval enforces the prompt-mandated 1:1 echo.
- Review-eval narrative length contract: 100 ≤ length ≤ 400 per the test plan and system prompt.
- Diagnose-eval reject-branch `fix_steps.length === 0` assertion (E1-005 adversarial P1): a parser bug returning instructions on an unidentified plant ('unknown' slug) would have shipped silently otherwise.

### Deferred (post-V1)
- Real-API mode (`EVAL_REAL_API=1`): scaffolded as `it.todo` placeholders for each endpoint. Image fixtures land alongside this mode in a follow-up PR. Requires real OpenRouter quota during runs.
- Eval coverage of `buildUserMessage` for consult/review: covered today by `test/consult.test.ts` and `test/review.test.ts` via the full Hono route handler with fetchMock. Eval scope is parser + router state machine, not request-body composition.
- Vitest eval-pool isolation: low practical risk at the current fixture count; revisit if the suite grows past 50 fixtures.

### Notes
- The off-topic consult fixture wording — "What's the best dog food for a labrador?" — was invented per the autonomous-build prompt's leeway. The original test plan example was "a question about my dog"; flag for review if a different shape better matches your dogfooding mix.

## [0.1.4.0] - 2026-05-04

E1-004 — last of four LLM proxy endpoints. The backend can now generate a weekly garden letter: mobile POSTs the past 7 days of plant counts (per-plant watering + skips + diagnoses), backend asks the free model first, escalates to paid on parse failure or low confidence, and returns the editorial Fraunces headline + one-paragraph narrative + per-plant observations that A-6 needs. Not yet wired to mobile (E9 will consume it). All four E1 endpoints — identify, diagnose, consult, review — now share the same tiered router; E1-005 (eval harness) is next.

### Added
- `POST /api/review` on the Cloudflare Worker. Accepts JSON body `{week_summary: {plants_total, watering_events, skip_events, diagnoses}, plants: [{species_slug, nickname?, watering_count, skip_count, had_diagnosis}]}` plus `X-Device-Id` header. plants array capped at 50, per-plant counts capped at 30, week totals capped at 300 — bounded so a hostile body can't blow up the prompt token count. Returns `ApiResult<ReviewResponse>`.
- `ReviewResponse` shape: `{headline, narrative, per_plant: [{species_slug, observation}], confidence, source, latency_ms}`. Headline ≤ 80 chars, narrative ≤ 400 chars (per the test plan's 100-400 range), each observation ≤ 200 chars. The mobile A-6 weekly review screen renders the headline + narrative as the editorial voice and pairs each observation with the local 7-day ledger for that plant.
- `SYSTEM_PROMPT_REVIEW` requiring `headline + narrative + per_plant + confidence`. Voice is editorial: "names the week's character, not its numbers"; narrative addresses the reader as 'you'; per_plant entries use the plant's nickname when present and interpret the data rather than echoing it. Empty plants array still returns a valid response with a "Ready when you are" nudge to add the user's first plant.
- `parseReview` with strict `per_plant` array validation: hostile entries (null, missing species_slug, missing observation, whitespace-only fields) are dropped silently; oversized observations truncated; missing or non-array `per_plant` triggers escalation rather than passing through.
- `routes/_reviewRequest.ts` JSON-body validator: `isBoundedInt` rejects fractional counts (would shift the LLM prompt), negative numbers, and over-cap values; nickname trimmed and length-capped; species_slug required and trimmed.
- 21 backend tests covering router boundaries (escalation on parse fail, escalation on low confidence, paid recommendation, free_failed degraded fallback), parser hostile-input cases (oversized inputs truncated to spec caps, mixed-quality per_plant entries dropped, missing per_plant triggers escalation), and HTTP-level JSON validation (missing/fractional/negative counts, oversized plants array, invalid plant entries). Total backend tests: 148.

### Changed
- `apps/backend/src/index.ts` registers `/api/review` alongside identify, diagnose, and consult. All four endpoints now share the `createLlmRouter<T>` factory and the same `IdentifySource` discriminator (`'free' | 'paid_escalated' | 'free_failed'`).
- `routes/_consultRequest.ts` comment updated: `_validation/` subdirectory deferred. Three flat `_*.ts` helpers (`_imageUpload`, `_consultRequest`, `_reviewRequest`) is still easier to find than three under a subdirectory; the regroup waits until a fourth helper or a real navigation pain point shows up.

### Deferred (post-V1)
- Per-plant observation eval — E1-005 will assert observation length and species_slug echoing against a fixture week. Not wired in this PR.
- `ReviewResponse.per_plant` ordering: parser preserves model-emitted order. Mobile A-6 will pair observations to plants by `species_slug` lookup rather than relying on positional order, so a model that reorders entries doesn't misattribute observations.

## [0.1.3.0] - 2026-05-04

E1-003 — third of four LLM proxy endpoints. The backend can now answer a free-text question about a specific plant: mobile POSTs a note (e.g. "I just repotted it") plus optional plant context, backend asks the free model first, escalates to paid on parse failure or low confidence. First text-only endpoint, and the first one with a structural off-topic rejection path so a hostile or irrelevant note ("recommend bleach", "tell me a joke") gets refused without burning a paid call. Not yet wired to mobile (E8 will consume it).

### Added
- `POST /api/consult` on the Cloudflare Worker. Accepts JSON body `{note, plant_context?}` plus `X-Device-Id` header. Note is 1-2000 chars after trim with the length cap applied before trim so a 100KB whitespace blob can't burn CPU. plant_context is optional and strictly validated per field (species_slug, is_indoor, override_interval_days as positive integer 1-365, watering_history capped at 7 entries). Returns `ApiResult<ConsultResponse>`.
- `ConsultResponse` is a discriminated union: `{kind: 'recommendation', revised_interval_days, reasoning, confidence, source, latency_ms}` for plant-care answers, or `{kind: 'rejected_off_topic', reason, source, latency_ms}` for off-topic notes. The route handler maps `rejected_off_topic` to `ApiResult.rejected_off_topic` at the wire boundary, so mobile callers get the same `ok: false, kind: 'rejected_off_topic'` shape they already handle.
- `SYSTEM_PROMPT_CONSULT` requiring the model to return JSON in one of two shapes, each tagged with an explicit `rejected: <bool>` discriminator. Rejection triggers are enumerated in the prompt: not plant-related, instruction-override attempts, harmful actions (bleach, gasoline, salting soil), and ambiguous notes.
- `parseConsult` with the rejection discriminator winning over recommendation keys — a hostile model that emits `{rejected: true, revised_interval_days: 1, reasoning: "water with bleach"}` still routes to rejection. Parser-level defenses cover NaN/Infinity confidence, fractional `revised_interval_days` clamping to [1, 30], reasoning truncation at 500 chars, rejection-reason truncation at 200 chars.
- 49 backend tests across consult router boundaries (rejection bypasses both confidence gate and budget gate, paid-side rejection collapses correctly, abort signal threading, system prompt isolation), parser hostile-input cases (prompt injection wrapped around valid JSON, missing/non-boolean discriminator, oversized inputs), and HTTP-level JSON validation (note bounds, plant_context shape, fractional override rejection). Total backend tests: 127.

### Changed
- `RouterInput` widened to a discriminated union: `{kind: 'vision', imageDataUrl}` for identify/diagnose, `{kind: 'text', userMessage}` for consult and the future review endpoint. `CallArgs` in `openrouter.ts` mirrors the shape; `callOpenRouter` switches on `kind` to build either the OpenRouter image_url content array or a plain string. Identify and diagnose call sites updated; their existing 78 tests stay green.
- `createLlmRouter` parser contract widened from `T | null` to `ParseResult<T> | null` where `ParseResult<T> = {kind: 'ok', value} | {kind: 'final', value}`. `null` still escalates (parse fail), `ok` is confidence-gated as before, `final` is terminal — bypasses both the confidence gate and the paid-escalation budget gate. Identify and diagnose parsers wrapped via an `asOk()` adapter so their `T | null` test surface is unchanged. The `WithConfidence` generic constraint dropped in favor of a defensive runtime `readConfidence(value)` that returns 0 (escalates) on a malformed `ok` parse rather than silently passing it through.
- `routes/_consultRequest.ts` is the JSON-body counterpart to `routes/_imageUpload.ts` — same `{ok, request | response}` return shape, same defense-in-depth ordering (device-id check before body read, length cap before trim, strict per-field validation). Two helpers in `routes/` is fine; an `_validation/` subdirectory waits until E1-004 makes it three.

### Deferred (post-V1)
- Anti-injection delimiter framing around the user note in the system prompt (e.g. `<user_note>...</user_note>` wrapping). Parser provides the structural backstop today; harden the prompt itself when E1-005 eval fixtures show measurable injection success.
- `ConsultRejection`'s in-process `source` and `latency_ms` fields drop on the wire (the `ApiResult.rejected_off_topic` variant doesn't carry them). Revisit when E8 mobile surfaces a need for either field on rejections.

## [0.1.2.0] - 2026-05-03

E1-002 — second of four LLM proxy endpoints. The backend can now diagnose a sick plant from a photo: mobile POSTs an image, backend asks a free vision model first, escalates to a paid model only when the free model can't answer confidently. Same tiered router as identify, different system prompt + response shape. Not yet wired to mobile (E5 will consume it).

### Added
- `POST /api/diagnose` on the Cloudflare Worker. Same multipart contract as `/api/identify` (image file + `X-Device-Id` header, MIME + magic-byte validation, 8MB ceiling). Returns `ApiResult<DiagnoseResponse>` with disease + confidence + severity + fix steps + alternatives + which model answered.
- `DiagnoseResponse` shape: `{disease_slug, disease_label, confidence, severity, fix_steps[], alternatives[], source, latency_ms}`. Healthy convention: `disease_slug='healthy'`, severity `'low'`, empty fix_steps. Cannot-tell convention: `disease_slug='unknown'` with confidence below 30.
- `parseDiagnose` with tolerant severity normalization — synonym map (`severe`→`high`, `moderate`→`medium`, `mild`→`low`) and ASCII-letter-run extraction so prose tails like `"high — likely fatal"` still match. Defaults to `'medium'` (not `'low'`) on garbage so serious diagnoses can't be silently downgraded. Cap of 8 fix_steps and 5 alternatives, each with early-exit and per-entry validation against hostile model output.
- 48 backend tests across diagnose router boundaries, parser edge cases (severity drift, fix_steps cap, malformed alternatives), and HTTP-level multipart validation. Total backend tests: 78.

### Changed
- `router.ts` refactored to a generic `createLlmRouter<T extends {confidence:number}>` factory. Both `identifyRouter` and `diagnoseRouter` are built on it; the abort/escalation/fallback state machine lives in one place. Behavior preserved for identify (existing 16 router tests stay green).
- `routes/identify.ts` and `routes/diagnose.ts` share a `parseImageUpload(c)` helper covering multipart parse, MIME allowlist, 8MB ceiling, magic-byte sniff, base64 conversion. Security-critical validation now lives in one file.
- `OPENROUTER_API_KEY` check now runs before multipart body read on both routes — a misconfigured Worker no longer burns memory parsing 8MB uploads it would immediately reject.

### Deferred (post-V1)
- **PV1-001** in WORKBACK.md: server-side cheap-model verifier to normalize/sanity-check `/api/diagnose` output. V1 handles severity drift via deterministic string normalization; verifier earns its keep only if the eval suite (E5-002) shows free-tier accuracy below 70% on fixtures.

## [0.1.1.0] - 2026-05-02

E1-001 — first of four LLM proxy endpoints. The backend can now identify a plant from a photo: mobile POSTs an image, backend asks a free vision model first, escalates to a paid model only when the free model can't answer confidently. Not yet wired to mobile (E2/E5 will consume it).

### Added
- `POST /api/identify` on the Cloudflare Worker. Accepts multipart/form-data (image + `X-Device-Id` header), validates MIME by both header and magic bytes, returns `ApiResult<IdentifyResponse>` with species + confidence + alternatives + which model answered.
- Tiered LLM router: free Llama 3.2 11B Vision (OpenRouter free tier) first, escalates to Claude 3.5 Sonnet on JSON parse failure or confidence below 70.
- `safeJsonParse` — string-literal-aware parser that recovers JSON from ` ```json ` fences, leading prose, and trailing commas without damaging string content.
- Caller-abort propagation: a disconnected client cancels the upstream OpenRouter call instead of burning the full free + paid timeout budget.
- 45 backend tests across router boundaries, parser edge cases, route validation, and OpenRouter abort behavior.
- `IdentifySource` discriminator (`free` / `paid_escalated` / `free_failed`) so the mobile A-3 result screen can render "I'm not sure" UX correctly when the backend returns a degraded fallback.

### Changed
- WORKBACK.md Epic E1 restructured: 10 tickets collapsed to 5. The four LLM proxy endpoints (identify / diagnose / consult / review) are now all in E1, sharing one router built once. KV-backed rate limit, escalation budget, `/api/budget`, Layer-1 classifier, and Layer-5 image gate moved to a Post-MVP deferred section per V1 scope lock (rate limit + budget tracked client-side in SQLite for V1).

## [0.1.0.0] - 2026-05-02

E0 Foundation & Tooling — first shippable scaffold of the V1 build. After this version, you can `git clone && bun install && bun run mobile:ios` and see a Conservatory-styled empty Plants list on a real iPhone or Simulator within ~10 minutes.

### Added
- Bun-workspace monorepo with `apps/mobile`, `apps/backend`, `packages/api-types`, `packages/theme`.
- Expo SDK 55 + Expo Router 5 mobile app aligned to the SDK manifest.
- Hono on Cloudflare Workers backend skeleton with vitest + `@cloudflare/vitest-pool-workers`.
- EAS development build profiles for iOS Simulator and Android device.
- CI on GitHub Actions: typecheck + jest (mobile) + vitest (backend) on every push and PR.
- Conservatory design tokens in `packages/theme` — light + Midnight dark palettes ported from `DESIGN.md`.
- Fraunces + Inter font loading via `@expo-google-fonts/*`, gated until both load to prevent system-font flash. Falls through to system fonts on load error so offline cold-start doesn't hang.
- Maestro smoke flow `.maestro/smoke-empty-plants.yaml` asserting the empty Plants list renders on launch.
- Project rules in `CLAUDE.md`, execution plan in `WORKBACK.md`, design system docs in `DESIGN.md`, master architecture plan + test plan in `plans/`, approved mockups (light + dark) in `designs/`.

### Changed
- Code review runs locally (`/codex review` + Claude adversarial via `/gstack-ship`) rather than in CI — no `OPENAI_API_KEY` repo secret needed.
